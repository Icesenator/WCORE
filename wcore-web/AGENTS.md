# WCORE ÔÇö Wallet CORE

## Outils actifs (auto-activation pour Codex)

Ces outils sont configur├®s globalement dans `~/.codex/config.toml` + `~/.codex/AGENTS.md` et sont **auto-utilis├®s** dans ce projet :

| Outil | Trigger auto | Quand |
|-------|--------------|-------|
| **RTK** | Pr├®fixer commandes shell ├á gros output | `rtk git status`, `rtk grep`, `rtk ls`. Compression 60-90%. |
| **Context Mode** (`mcp__context-mode__*`) | Sandbox commandes >20 lignes | `ctx_execute` / `ctx_batch_execute`. Obligatoire pour logs, API calls, test runners. |
| **Token Savior** (`mcp__token-savior__*`) | Avant tout `Read` sur code | `find_symbol`, `get_function_source`, `get_changed_symbols`. |
| **Google Sheets MCP** (`mcp__gsheets__*`) | Lecture/├®criture spreadsheet WCORE | ID `1kxidZZoEM6fXubFpp54fKvzJeXFCSCWCfyMTPNwYRB4`. |
| **Playwright MCP** (`mcp__playwright__*`) | Debug UI Sheets, captures, inspection r├®seau | Browser automation (quand connect├®). |
| **n8n-MCP** (`mcp__n8n-mcp__*`) | Appel service externe non-natif | 400+ int├®grations (webhooks, APIs, social). |
| **Superpowers** | R├®flexion structur├®e avant code non-trivial | Brainstorming, writing-plans, TDD, systematic-debugging, verification. Trigger auto. |

**IMPORTANT** : les MCPs peuvent ne pas ├¬tre connect├®s selon la session Codex. Des alternatives **bash autonomes** existent (voir section "Autonomie sans MCP" ci-dessous).

**R├┤le Codex** : worker agent sous orchestration OMC (voir `~/.codex/config.toml` `[agents] role=worker`). Claude Code planifie, Codex ex├®cute, Claude Code v├®rifie.

## Projet

Syst├¿me de suivi de portefeuilles crypto multi-cha├«nes sur Google Sheets + Apps Script.
- **183 configurations suivies** (EVM, SVM/Solana, Cosmos SDK, TON); le nombre actif est dynamique et toutes sont extractibles depuis `wcore-gsheet`
- **120 combinaisons wallet-cha├«ne**
- Version actuelle : voir `ROADMAP.md` et `CHANGELOG.md` (web) / **v4.15.36** (Apps Script)
- Langue du d├®veloppeur : **fran├ºais** ÔÇö r├®pondre en fran├ºais
- Spreadsheet ID : `1kxidZZoEM6fXubFpp54fKvzJeXFCSCWCfyMTPNwYRB4`
- **GitHub** : `https://github.com/Icesenator/WCORE`
- **Email projet** : `wcorexyz@gmail.com`
- **Docker Hub** : `wcorexyz`
- **Domaine** : `wcore.xyz` (Railway)
- **Railway** : projet `WCORE` (`cbb16f4a-79c1-46ef-92b2-019c9c9940d7`)

## D├®marrage rapide (checklist autonomie)

Ces 4 commandes fonctionnent SANS MCP, SANS navigateur, SANS interaction utilisateur :

### 1. Lire la spreadsheet (diagnostic)
```bash
node -e "const {JWT}=require('google-auth-library');const k=require('C:/Users/strau/.config/gsheets-mcp/service-account.json');const c=new JWT({email:k.client_email,key:k.private_key,scopes:['https://www.googleapis.com/auth/spreadsheets.readonly']});(async()=>{await c.authorize();const r=await c.request({url:'https://sheets.googleapis.com/v4/spreadsheets/1kxidZZoEM6fXubFpp54fKvzJeXFCSCWCfyMTPNwYRB4/values/'+encodeURIComponent('Ledger - ZERO Network!A1:M10')});console.log(JSON.stringify(r.data.values,null,2));})();"
```

### 2. Ex├®cuter une fonction Apps Script (diagnostic avanc├®)
```bash
npx @google/clasp run DIAG_ZERO_WALLET -p '["0x..."]'
```

### 3. D├®ployer le code
```bash
npx @google/clasp push
```

### 4. Piloter Chrome (playwright CDP, si besoin d'interaction web)

**IMPORTANT (Google login)** : Google bloque la connexion dans les navigateurs lanc├®s par Playwright (`chromium.launch()`), m├¬me avec `channel:"chrome"`. La m├®thode fiable est : **d├®marrer Chrome normalement** avec `--remote-debugging-port` + un **profil d├®di├®** (`chrome-debug-profile`), puis piloter via `chromium.connectOverCDP()`.

Scripts disponibles :

| Script | R├┤le |
|--------|------|
| `scripts/chrome-cdp.js` | Helper Node.js (start/connect Chrome CDP) |
| `scripts/connect-google.js` | Remplit l'email Google et avance jusqu'├á la validation passkey (action utilisateur requise) |
| `scripts/clasp-login-auto.js` | OAuth clasp via serveur redirect local (action utilisateur possible) |

Infos cl├®s :

| Param├¿tre | Valeur |
|-----------|--------|
| Port CDP | **9222** (manifest = 9223) |
| Profil Chrome | `$env:USERPROFILE\chrome-debug-profile` |
| Playwright | `node_modules/playwright` (install├® dans le projet) |
| Compte Google | `straub.florian88.fs@gmail.com` |

```bash
# ├ëtape 1 - D├®marrer Chrome avec debug (si pas d├®j├á lanc├®)
node scripts/chrome-cdp.js start http://localhost:3001

# ├ëtape 2 - Connecter Playwright et interagir
node -e "const {chromium}=require('playwright');(async()=>{const b=await chromium.connectOverCDP('http://127.0.0.1:9222');const p=b.contexts()[0].pages()[0];await p.goto('https://google.com');console.log(await p.title());})()"

# Login Google (si session expir├®e)
node scripts/connect-google.js
# Puis l'utilisateur doit valider la passkey dans Chrome
```

**Si un outil ├®choue** ÔåÆ voir section "Autonomie sans MCP" pour les proc├®dures de r├®paration compl├¿tes.

## Structure du projet

```
wcore-gsheet/
Ôö£ÔöÇÔöÇ src/               ÔåÉ Fichiers .gs (source de v├®rit├®)
Ôö£ÔöÇÔöÇ pulls/             ÔåÉ Tirages depuis Apps Script (clasp pull)
Ôö£ÔöÇÔöÇ .backups/          ÔåÉ Sauvegardes automatiques
Ôö£ÔöÇÔöÇ .clasp.json        ÔåÉ Config clasp (lien projet GAS)
Ôö£ÔöÇÔöÇ scripts/
Ôöé   Ôö£ÔöÇÔöÇ chrome-cdp.js           ÔåÉ Helper Chrome CDP (start/connect)
Ôöé   Ôö£ÔöÇÔöÇ connect-google.js       ÔåÉ Login Google automatique
Ôöé   Ôö£ÔöÇÔöÇ clasp-login-auto.js     ÔåÉ OAuth clasp
Ôöé   ÔööÔöÇÔöÇ validate-static.js      ÔåÉ Validation statique
Ôö£ÔöÇÔöÇ safe-push.ps1      ÔåÉ Script PowerShell de d├®ploiement s├®curis├®
Ôö£ÔöÇÔöÇ pull-all.ps1       ÔåÉ Script PowerShell de r├®cup├®ration
ÔööÔöÇÔöÇ AGENTS.md          ÔåÉ Ce fichier
```

Les fichiers `.gs` sont dans `src/`. C'est l├á qu'on travaille.

## Architecture des fichiers core (ordre de chargement)

| Fichier | R├┤le |
|---------|------|
| `00_VERSION_REGISTRY.gs` | Registre de versions des modules |
| `00B_VERSION_SCANNER.gs` | Scanner de versions |
| `01_INIT.gs` | Config globale, `WCORE_VERSION`, `WCORE_HEALTH()` |
| `02_UTILS.gs` | Utilitaires de base |
| `03_HTTP.gs` | HTTP de base |
| `03B_HTTP_GUARD.gs` | Protection cache contre ├®crasement |
| `03E_QUOTA_CIRCUIT_BREAKER.gs` | Circuit breaker quota |
| `04A_CACHE_CORE.gs` | Cache de base (CacheManager, ScriptProperties) |
| `04B_CACHE_WALLET.gs` | Cache wallet packed (virtualisation 500KB) |
| `04C_CACHE_GLOBAL.gs` | Cache global (GlobalPriceCache, FX, Meta, WalletCache API) |
| `04D_CACHE_SHEET.gs` | Cache sheet |
| `05_RPC.gs` | Gestion RPC multi-endpoint + sant├® |
| `06_TOKENS.gs` | D├®tection et gestion tokens |
| `07_PRICES.gs` | Pricing multi-source avec fallbacks + L1 CacheService |
| `08_ASSETS.gs` | Gestion assets |
| `09_BUDGET.gs` | Budget dynamique |
| `09_SIMPLE_ROTATION.gs` | Rotation simplifi├®e |
| `10_OUTPUT.gs` | Formatage sortie standardis├® |
| `10A_BASE_ENGINE.gs` | Moteur commun ├á tous les engines |
| `10B_STATS_BUILDER.gs` | Construction des statistiques |
| `11_EVM_ENGINE.gs` | Moteur EVM (105+ cha├«nes) |
| `12_WALLET_NAMES.gs` | Noms de wallets |
| `13_DIAGNOSTIC.gs` | Fonctions de diagnostic |
| `14_SVM_ENGINE.gs` | Moteur Solana |
| `15_COSMOS_ENGINE.gs` | Moteur Cosmos |
| `16_REFRESH.gs` | Watchdog de refresh + triggers |
| `17_LISTING.gs` | Listing des onglets Ledger + SHEET_LINK() |
| `18_CLEANUP.gs` | Nettoyage de cache |
| `19_CHAIN_FACTORY.gs` | Factory pattern pour cr├®er des cha├«nes |
| `20_RPC_BENCHMARK.gs` | Benchmark RPC |
| `21_DASHBOARD.gs` | Dashboard Recap Chain |
| `24_DEGRADED_MODE.gs` | Circuit breaker, mode d├®grad├® |
| `26_OPTIMIZATIONS.gs` | Optimisations diverses |
| `26B_HTTP_SAVINGS.gs` | ├ëconomies HTTP (patches runtime) |
| `27_ACTIVITY_REFRESH.gs` | Activity-Based Refresh (nonce watchdog, _RpcLookup, wallet auto-discovery) |
| `30_SCAN_PRICING.gs` | Scan pricing |
| `32_MODULE_AUTOREGISTER.gs` | Auto-enregistrement modules |
| `33_DYNAMIC_RPC.gs` | Dynamic RPC (chainlist EVM + Cosmos Chain Registry + merge store) |

## Fichiers chain

Un fichier par cha├«ne : `BASE.gs`, `ETHEREUM.gs`, `SOLANA.gs`, `TERRA.gs`, etc.

Pattern standard (via ChainFactory) :
```javascript
var _CHAINNAME = ChainFactory.createEvmChain("CHAINNAME", {
  CACHE_VERSION: N,
  TIMEOUTS: { ... },
  RPC: { ENDPOINTS: [...], ... },
  CHAIN: { NAME, CHAIN_ID, NATIVE_SYMBOL, NATIVE_NAME, NATIVE_DECIMALS, NATIVE_LLAMA_ID, NATIVE_GECKO_ID, DEX_SLUG, GT_NETWORK },
  LLAMA_ID_MAP: { ... }
});

function GET_WALLET_ASSETS_CHAINNAME(a,r,t,f,g){return _CHAINNAME.getWalletAssets(a,r,t,f,g);}
function CACHED_WALLET_ASSETS_CHAINNAME(a){return _CHAINNAME.getCachedWalletAssets(a);}
function CHAINNAME_REFRESH_STATUS(a,r,t,f,g){return _CHAINNAME.getRefreshStatus(a,r,t,f,g);}
function CHAINNAME_STATS(a,t){return _CHAINNAME.getStats(a,t);}
// + fonctions DIAG_CHAINNAME_*
```

## Contraintes Apps Script (CRITIQUES)

| Limite | Valeur |
|--------|--------|
| ScriptProperties | **500 KB** max |
| Ex├®cution | **30 sec** max par appel |
| HTTP/jour | **20 000** (seuil interne ├á 99% = 17 820) |
| Ex├®cutions concurrentes | **30** max |

## Architecture Cache (3 couches)

```
Couche 1 ÔÇö L1 CacheService (volatile, 2h TTL)
  Cl├®s: DEX:{chainSlug}:{contract}, GT:{network}:{contract}, LLAMA:{id}
  Prix en USD. Lecture rapide. Contourn├® par skipL1 sur forceFull (v4.13.9+)

Couche 2 ÔÇö GlobalPriceCache (ScriptProperties, 6h staleness)
  Cl├®s priceMap: {contract} ÔåÆ prix EUR
  Cl├®s entries: {chainId}:{contract} ÔåÆ { price, ts, src }
  Partag├® entre toutes les cha├«nes. Sauv├® en fin de cycle.

Couche 3 ÔÇö WalletCache (ScriptProperties packed, 14j TTL)
  Format compact v5: [contract, balance, symbol, name, decimals]
  Virtualis├® dans GLOBAL_WALLET_CACHE (hash-based, 495KB max)
  Contient: assets, priceMap (EUR), priceTsMap, balanceTsMap, scanStats
```

**forceFull (C1=TRUE)** : clear WalletCache + bypass L1 (skipL1) + bypass consensus cache voting (v4.14.5) + bypass quota pre-check. Ne clear PAS GlobalPriceCache.

- **`packages/shared/src/cache-keys.ts`** ÔÇö Registry canonique des cl├®s cache (prix, scan, wallet) + helpers `cacheKey()` / `cacheKeyGsheet()` / `walletKey()`. Source unique pour toute construction de cl├® cache.

## Cascade Pricing

```
1. STABLECOINS ÔåÆ Fast-path (FX rate / 1.0)
2. CACHE (< 6h) ÔåÆ retourner directement
3. NATIFS ÔåÆ DefiLlama (priorit├®) ÔåÆ CoinGecko fallback
4. TOKENS ÔåÆ DexScreener (bulk) ÔåÆ GeckoTerminal (batch + per-token) ÔåÆ Jupiter (SVM) ÔåÆ CoinGecko (dernier recours)
```

**JAMAIS deviner un CoinGecko ID.** Toujours v├®rifier.

## Format de sortie

```
chain_name | token_ticker | token_name | contract_address | balance | price_eur | value_eur
```

Lignes sp├®ciales obligatoires : `INFO_FX`, `INFO_LAST_TX`, `INFO_ROT`, `INFO_NATIVE`, `INFO_TOTAL`, `META`

**Important** : `script_version` dans META vient de `BASE_ENGINE.gs` ou du fichier chain, PAS de `EVM_ENGINE.gs`.

## Profils de rotation

```
MINIMAL:      { maxTokensPerCall: 3,  maxRefreshPerRun: 3,  maxPriceLookups: 0  }
CONSERVATIVE: { maxTokensPerCall: 6,  maxRefreshPerRun: 8,  maxPriceLookups: 2  }
NORMAL:       { maxTokensPerCall: 10, maxRefreshPerRun: 10, maxPriceLookups: 4  }
AGGRESSIVE:   { maxTokensPerCall: 15, maxRefreshPerRun: 25, maxPriceLookups: 8  }
```

## Principes fondamentaux

1. **Ne JAMAIS ├®craser du cache valide** lors d'erreurs API
2. **Mode d├®grad├®** : retourner donn├®es cached avec `[DEGRADED]`
3. **Circuit breaker** : 2 min de blocage apr├¿s erreur quota
4. **Consensus RPC** : majorit├® stricte requise (`votes * 2 > total`) ÔÇö 2/4 est un match nul, pas un consensus (v4.15.1)
5. **Cache = 1 vote** quand pas d'activit├® r├®cente d├®tect├®e (d├®sactiv├® par forceFull ou activityForced v4.15.1)
6. **Pr├®server l'existant** : en cas de doute, garder les donn├®es cached plut├┤t qu'├®craser avec des z├®ros

## R├¿gles de contribution

### FAIRE
- Code `.gs` complet et pr├¬t ├á d├®ployer
- Version en t├¬te de fichier : `// v4.x.y - Description`
- Incr├®mental : un fix = un probl├¿me
- Noms exacts, cl├®s cache exactes
- Code harmonieux et universel (pas de hack chain-specific)

### NE PAS FAIRE
- Pas de renommage arbitraire
- Pas de refactor global non demand├®
- Pas de pseudo-code ou code g├®n├®rique
- Pas d'augmentation empreinte m├®moire
- Ne JAMAIS supposer un CoinGecko ID
- Ne JAMAIS ajouter de logique sp├®cifique ├á une cha├«ne dans les engines

## D├®ploiement

```powershell
# Depuis wcore-gsheet/

# IMPORTANT: rootDir dans .clasp.json doit ├¬tre "src" pour push (sinon erreur duplicate appsscript.json)
# Apr├¿s push, remettre rootDir ├á "."

# Option 1: Script PowerShell s├®curis├®
powershell -File safe-push.ps1

# Option 2: Push direct
# 1. Modifier .clasp.json: rootDir "." ÔåÆ "src"
# 2. npx clasp push
# 3. Modifier .clasp.json: rootDir "src" ÔåÆ "."

# Pull depuis Apps Script
powershell -File pull-all.ps1
```

## Backup DB

Backup automatique quotidien ├á 03:00 via t├óche planifi├®e Windows `WCORE_DB_Backup`.

| Fichier | R├┤le |
|---------|------|
| `scripts/backup-db.js` | Export SQL de toutes les tables (Prisma $queryRaw) |
| `scripts/backup-db.ps1` | Wrapper PowerShell (set BACKUP_DATABASE_URL ÔåÆ node backup-db.js) |
| `scripts/setup-backup-task.ps1` | Cr├®e la t├óche planifi├®e Windows (backup quotidien 03:00) |
| `scripts/check-backup-freshness.ps1` | V├®rifie que le dernier backup < 48h (anti-panne silencieuse) |
| `scripts/setup-backup-check-task.ps1` | Cr├®e la t├óche planifi├®e `WCORE_DB_Backup_Check` (quotidien 11:00) |
| `backups/` | Stockage des backups (rotation 7 jours) + `LAST_ERROR.txt` si check ├®choue |

**Backup manuel** : `powershell -File scripts/backup-db.ps1`

**Alerting freshness (P2-16, 2026-06-13)** : `scripts/check-backup-freshness.ps1` d├®tecte les pannes silencieuses (incident 24/05ÔåÆ12/06 : 19j sans backup, `.env.backup` supprim├®, `LastTaskResult: 2` ignor├®). Si le dernier backup > 48h ou absent ÔåÆ ├®crit `backups/LAST_ERROR.txt` (cause + fix) + toast Windows + exit 1 ; self-clear quand sain. T├óche `WCORE_DB_Backup_Check` (quotidien 11:00). **Gotcha** : enregistrer l'action de t├óche avec `-Command "& '<script>'; exit $LASTEXITCODE"` et NON `-File` ÔÇö `-File` peut retourner un faux `3221225786` (0xC000013A) au teardown du process host. La source de v├®rit├® de l'alerte reste `backups/LAST_ERROR.txt`, pas le `LastTaskResult`.

**Restore** : `psql $DATABASE_URL < backups/wcore-backup-*.sql` (apr├¿s avoir truncat├® les tables)

**V├®rifier** : `dir backups\` ÔÇö doit avoir Ôëñ 7 fichiers.

**Attention** : l'ordre des tables est alphab├®tique. En cas de FK constraints au restore, d├®sactiver les triggers ou restaurer les tables parentes d'abord.

**Gotcha `.env.backup` manquant (2026-06-12)** : la t├óche planifi├®e ex├®cute `scripts/backup-db-scheduled.ps1` qui lit `DATABASE_URL` depuis `scripts/.env.backup` (gitignor├® via `.env*`). Si ce fichier est supprim├® (ex: nettoyage de secrets), le script sort en `exit 2` ÔåÆ `LastTaskResult: 2` et **aucun backup pendant des semaines** (panne silencieuse, incident : aucun backup du 24/05 au 12/06). Diagnostic : `Get-ScheduledTaskInfo -TaskName "WCORE_DB_Backup"` (code 2 = `.env.backup` absent). R├®paration : r├®cup├®rer l'URL publique via `railway variables --service Postgres --json` ÔåÆ `DATABASE_PUBLIC_URL` (host `viaduct.proxy.rlwy.net`, PAS l'URL interne `postgres.railway.internal` inaccessible en local) et recr├®er `scripts/.env.backup` avec `DATABASE_URL=<url>`. Valider ensuite avec `Start-ScheduledTask` + v├®rifier `LastTaskResult: 0` (267009 = en cours).

## Maintenance AGENTS.md

En fin de session, si des gotchas, patterns ou apprentissages importants ont ├®t├® d├®couverts, **ex├®cuter automatiquement** le skill `Codex-md-management:revise-Codex-md` pour mettre ├á jour ce fichier avec les nouvelles connaissances.

## Gestion m├®moire (deux syst├¿mes)

Deux syst├¿mes de m├®moire persistante coexistent. **Toujours maintenir les deux en coh├®rence.**

### 1. Codex-mem (MCP ÔÇö base s├®mantique)
- Observations index├®es (#ID), recherchables par mots-cl├®s via `mcp-search`
- Contient les d├®tails : bugs, fixes, d├®cisions, diagnostics, code exact
- Utiliser `save_memory` pour sauvegarder les d├®couvertes importantes (bugs fix├®s, incidents, d├®cisions d'architecture)
- Utiliser `search` / `get_observations` pour retrouver le contexte des sessions pr├®c├®dentes
- Projet : `wcore-gsheet`

### 2. Auto memory (dossier local)
- Fichiers dans `~/.Codex/projects/C--Users-strau-wcore-gsheet/memory/`
- `MEMORY.md` charg├® automatiquement en contexte (max 200 lignes)
- R├®sum├®s structur├®s des patterns stables, pas les d├®tails ├®ph├®m├¿res
- Mettre ├á jour quand un pattern est confirm├®, supprimer quand invalid├®

### Quand sauvegarder o├╣ ?
| Quoi | Codex-mem | Auto memory |
|------|-----------|-------------|
| Bug fix├® (d├®tails, cause, code) | Oui | Non (sauf si pattern r├®current) |
| Pattern stable confirm├® | Oui | Oui (r├®sum├® dans MEMORY.md) |
| D├®cision d'architecture | Oui | Oui (r├®sum├®) |
| Incident / diagnostic ponctuel | Oui | Non |
| Pr├®f├®rence utilisateur | Oui | Oui |

## Autonomie sans MCP (fallbacks bash)

Si les MCPs ne sont pas connect├®s, ces alternatives bash fonctionnent :

### clasp (d├®ploiement + ex├®cution)

**Token** : `~/.clasprc.json` contient le token OAuth wcore-mcp (client `652188583224-i1qqdjip5f598p7ft67rvib751en3d9l.apps.googleusercontent.com`) avec TOUS les scopes requis (`script.scriptapp`, `script.external_request`, `script.storage`, `spreadsheets`, `userinfo.email`).

```bash
# D├®ploiement
npx @google/clasp push

# Ex├®cution de fonctions
npx @google/clasp run DIAG_ZERO_WALLET -p '["0x..."]'
npx @google/clasp run WCORE_HEALTH
```

**Si le token expire** : le refresh token est stock├® dans `.clasprc.json` et Google le renouvelle automatiquement. Si `clasp run` tombe en "Unable to run script function", refaire le login OAuth (une seule commande, ouvre Chrome, clique Autoriser) :

```powershell
$code = node -e "
const http=require('http');
const url='https://accounts.google.com/o/oauth2/v2/auth?client_id=652188583224-i1qqdjip5f598p7ft67rvib751en3d9l.apps.googleusercontent.com&redirect_uri=http://localhost:8888&response_type=code&scope=https://www.googleapis.com/auth/script.scriptapp%20https://www.googleapis.com/auth/script.external_request%20https://www.googleapis.com/auth/script.storage%20https://www.googleapis.com/auth/spreadsheets%20https://www.googleapis.com/auth/userinfo.email&access_type=offline&prompt=consent';
const s=http.createServer((r,res)=>{const c=new URL(r.url,'http://localhost:8888').searchParams.get('code');if(c){res.end('OK');s.close();console.log('CODE:'+c);process.exit(0)}res.end('...')});
s.listen(8888);
console.log('URL:'+url);
setTimeout(()=>process.exit(1),120000);
"
# Copier l'URL dans Chrome, cliquer Autoriser
# Le code est affich├®, l'├®changer contre des tokens :
node -e "
const CLIENT_ID='652188583224-i1qqdjip5f598p7ft67rvib751en3d9l.apps.googleusercontent.com';
const CLIENT_SECRET='<GOOGLE_OAUTH_CLIENT_SECRET>';
const CODE='<coller le code ici>';
(async()=>{
  const r=await fetch('https://oauth2.googleapis.com/token',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body:new URLSearchParams({code:CODE,client_id:CLIENT_ID,client_secret:CLIENT_SECRET,redirect_uri:'http://localhost:8888',grant_type:'authorization_code'})});
  const t=await r.json();
  const fs=require('fs');
  fs.writeFileSync(require('os').homedir()+'/.clasprc.json',JSON.stringify({tokens:{default:{client_id:CLIENT_ID,client_secret:CLIENT_SECRET,type:'authorized_user',refresh_token:t.refresh_token,access_token:t.access_token,token_type:'Bearer',expiry_date:Date.now()+t.expires_in*1000,id_token:t.id_token||''}}},null,2));
  console.log('Saved!');
})();
"
```

### Google Sheets API (service account)

Le service account `gsheets-mcp@wcore-mcp.iam.gserviceaccount.com` a acc├¿s en lecture/├®criture au spreadsheet. La cl├® est dans `C:\Users\strau\.config\gsheets-mcp\service-account.json`.

```bash
node -e "
const {JWT}=require('google-auth-library');
const key=require('C:/Users/strau/.config/gsheets-mcp/service-account.json');
const client=new JWT({email:key.client_email,key:key.private_key,scopes:['https://www.googleapis.com/auth/spreadsheets']});
(async()=>{
  await client.authorize();
  const r=await client.request({url:'https://sheets.googleapis.com/v4/spreadsheets/1kxidZZoEM6fXubFpp54fKvzJeXFCSCWCfyMTPNwYRB4/values/'+encodeURIComponent('Ledger - ZERO Network!A1:M20')});
  console.log(JSON.stringify(r.data.values,null,2));
})();
"
```

### Playwright via CDP (Chrome debug)

Chrome ne supporte le remote debugging qu'avec un r├®pertoire de donn├®es **explicite et non-d├®faut**. Le profil `C:\Users\strau\chrome-debug-profile` est d├®di├® au debug et **persiste les cookies Google** (login une fois, r├®utilisable).

```bash
# D├®marrer Chrome avec debug (si pas d├®j├á lanc├®)
& "C:\Program Files\Google\Chrome\Application\chrome.exe" `
  --remote-debugging-port=9222 `
  --user-data-dir="$env:USERPROFILE\chrome-debug-profile"

# Connecter Playwright
node -e "
const {chromium}=require('playwright');
(async()=>{
  const b=await chromium.connectOverCDP('http://127.0.0.1:9222');
  const p=b.contexts()[0].pages()[0];
  await p.goto('https://google.com');
  console.log(await p.title());
  await b.close();
})();
"
```

**Note** : Google bloque le sign-in sur les navigateurs lanc├®s par Playwright (`chromium.launch()`). La m├®thode CDP contourne cela car Chrome est d├®marr├® normalement. Ne PAS utiliser le r├®pertoire de donn├®es Chrome par d├®faut (`%LOCALAPPDATA%\Google\Chrome\User Data`) ÔÇö Chrome refuse le debug dessus.

## Gotchas


- **isNoMarketToken filtre les tokens sans prix (2026-07-14)** : sanitizeGsheetScanResult (gsheet.ts:619) appelle isNoMarketToken qui drop tout token avec priceEur=null non protege. Les tokens hors-registry sur chaines sans DEX/GT (ex: USDC.e sur Tempo) echouent au pricing cascade car isStable est gate par source === "registry" (evm-pricing.ts:95). Meme si le token est dans customTokens (I2:I), il peut etre filtre si le protectedContracts Set ne le contient pas pour une raison de casing ou de format. **Fix** : ajouter "USDC.E", "PATHUSD" et autres stablecoins connus dans knownTokenSymbols (gsheet.ts:584-592) + ajouter les tokens au registry chaine (egistry.ts) pour qu'ils beneficient du fast-path stablecoin.

- **LLAMA_ID_MAP case-sensitive (2026-07-14)** : getSymbolLlamaId (cascade.ts:260) fait un lookup exact puis uppercase. Si la config a "USDC.e" (lowercase e) mais que le contrat ERC-20 retourne "USDC.E" (uppercase E), le lookup echoue. Toujours ajouter les deux variantes dans LLAMA_ID_MAP. Fichiers : TEMPO.ts:41, cascade.ts:260-267.
- **gid === 0** : `getSheetId()` retourne 0 pour le premier onglet ÔåÆ falsy en JS. Utiliser `gid != null` au lieu de `gid ?`
- **forceFull + quota** : forceFull bypass le L1 cache ÔåÆ plus d'appels HTTP que d'habitude. Ne pas lancer en fin de journ├®e quand le quota est bas
- **Tokens LP non-stablecoin** : les tokens dont la seule liquidit├® est contre un quote non-USD (ex: TOKEN/CREATE) peuvent avoir des prix USD incorrects si le cache L1 est stale. Le skipL1 sur forceFull corrige ├ºa (v4.13.9+)
- **GlobalPriceCache partag├®** : toutes les cha├«nes partagent le m├¬me GlobalPriceCache. La derni├¿re cha├«ne ├á sauver ├®crase le priceMap entier
- **`*/` dans commentaires GAS** : ne JAMAIS ├®crire `*/` dans un commentaire multi-ligne `/* ... */` (ferme le bloc pr├®matur├®ment)
- **@customfunction dans ARRAYFORMULA+HYPERLINK** : Google Sheets cache agressivement les r├®sultats de @customfunction. HYPERLINK ignore silencieusement les URLs vides ÔåÆ liens absents sans erreur. Utiliser RichTextValue API pour les hyperlinks programmatiques (v4.5.5+)
- **Recap Chain colonne A** : script-managed via `_setRecapHyperlinks_()` dans 17_LISTING.gs. Mise ├á jour par `REFRESH_LEDGER_CACHE()`, pas de formule
- **GT API paths** : `/token_price/` vit sous `/api/v2/simple/networks/`, PAS `/api/v2/networks/`. `_pxGetGtBaseUrl()` pour Try 2+3 (/tokens/, /pools/), `_pxGetGtSimpleBaseUrl()` pour Try 1 + batch (/token_price/). Mauvais path = 404 silencieux (v4.14.4)
- **GT_NETWORK slugs** : certains r├®seaux GT utilisent des underscores, pas des tirets. Toujours v├®rifier via `https://api.geckoterminal.com/api/v2/networks`. Ex: `polygon_pos` (pas `polygon-pos`), `arbitrum_nova` (pas `arbitrum-nova`). Mauvais slug = 404 silencieux ÔåÆ metadata (symbol/name) jamais r├®solue
- **attemptTsMap cooldown** : `PRICE_ATTEMPT_COOLDOWN_MS` (2h via 26B_HTTP_SAVINGS.gs) bloque les retries Llama/CG pour un token apr├¿s une tentative ├®chou├®e. Les 46+ cl├®s dans `Cache.attemptTsMap` = tokens en cooldown. Nettoyage auto apr├¿s cooldown*2 (4h)
- **GT throttle** : `_GT_THROTTLE.max` dans 26B_HTTP_SAVINGS.gs contr├┤le le nombre d'appels GeckoTerminal par ex├®cution. Actuellement 40/run (v4.14.2). Base a 11+ tokens GT-only
- **Sheet formules A1/I1** : `A1=CACHED_WALLET_ASSETS_*(addr;J1)` (lecture cache seule, pas d'HTTP). `I1=*_REFRESH_STATUS(addr;"";I2:I;C1;B1)` (live scan, C1=forceFull, B1=trigger). WATCHDOG pulse B1 ÔåÆ I1 scanne ÔåÆ cache MAJ ÔåÆ WATCHDOG pulse J1 ÔåÆ A1 relit le cache. **Colonne I (I2:I) = liste des contrats tokens**, PAS l'adresse wallet
- **_RpcLookup multi-RPC** : `_RpcLookup` dans 27_ACTIVITY_REFRESH.gs stocke RPCs/REST URLs par cha├«ne + VM type (v4.15.0). `getRpcs(chain)` retourne le tableau, `getVm(chain)` retourne "EVM"/"SVM"/"COSMOS". `BUILD_RPC_LOOKUP()` utilise `eval()` ÔåÆ ne fonctionne PAS en contexte trigger. Cosmos chains utilisent `cfg.API.REST_URL`, SVM/EVM utilisent `cfg.RPC.ENDPOINTS`. Ex├®cuter `BUILD_RPC_LOOKUP()` depuis l'├®diteur Apps Script apr├¿s ajout de cha├«nes
- **WATCHDOG batch timing** : BATCH_SIZE=20, ~107 wallets EVM, tri├® par lastCheck ascending. Cycle complet ~25-30 min. Wallets r├®cemment v├®rifi├®s sont en fin de queue
- **Cloudflare ETH RPC cass├®** : `cloudflare-eth.com` retourne "Internal error" pour `eth_getTransactionCount`. `publicnode.com` fonctionne. `ankr.com/eth` requiert API key. Toujours tester avec fallback RPCs
- **Nonce tracking multi-VM (v4.15.0)** : EVM utilise `eth_getTransactionCount` (nonce entier), SVM utilise `getSignaturesForAddress(limit:1)` (signature tx string), Cosmos utilise `/cosmos/auth/v1beta1/accounts/` (sequence entier). WATCHDOG classe les wallets par VM et utilise la batch function d├®di├®e
- **Consensus + force/activity** : `force=true` OU `state.activityForced=true` ÔåÆ `hasRecentActivity=true` ÔåÆ cache ne vote pas. WATCHDOG pulse B1 (pas C1), donc `force=false` ÔÇö c'est `activityForced` qui d├®sactive le cache vote (v4.15.1)
- **budget.force** : L'objet SimpleBudget dans EVM Engine doit inclure `force: !!force` pour que INFO_ROT affiche `forceFull=YES`. Sans ├ºa, toujours NO (v4.14.5)
- **ModuleVersions** : `BaseEngine.VERSION` est affich├® en priorit├® dans STATS (via `||`). S'il est stale, il masque la version correcte du ModuleRegistry. Toujours synchroniser les deux
- **Toujours des fixes permanents** : ne jamais proposer de workaround temporaire (ex: forcer `hasRecentActivity=true`). Toujours investiguer et corriger la cause racine
- **WATCHDOG et [NO_CACHE]** : `[NO_CACHE]` dans I1 = wallet jamais scann├® avec succ├¿s. Trait├® comme "empty" (cooldown 10 min) depuis v4.5.14. Avant ├ºa, ignor├® ÔåÆ attente 5h
- **Thundering herd Recap Chain** : `REFRESH_LEDGER_CACHE()` r├®├®crit 119 cells colonne A ÔåÆ d├®clenche ~9500 recalculs INDIRECT ÔåÆ saturation queue @customfunction ÔåÆ risque ├®puisement quota. Ex├®cuter en heures creuses
- **Quota reset CET** : le quota UrlFetch Google reset vers **10h-10h30 CET**, pas minuit UTC. Planifier les op├®rations lourdes apr├¿s cette heure
- **@customfunction queue** : Google Sheets limite les ex├®cutions concurrentes. Quand des fonctions HTTP (REFRESH_STATUS) saturent la queue, les fonctions cache-only (CACHED_WALLET_ASSETS) sont aussi bloqu├®es avec "Chargement en cours..."
- **ACTIVITY_WATCHDOG normalisation chain keys** : les chain keys sont normalis├®es en MAJUSCULES + underscores (`"Arbitrum One"` ÔåÆ `"ARBITRUM_ONE"`). `_activity_pulseB1ForChain_` et `ForceRefreshManager` doivent utiliser la M├èME normalisation partout. `getSheetByName()` est **case-sensitive** en GAS. Avant v4.14.8, B1 n'├®tait jamais puls├® pour les cha├«nes multi-mots (Arbitrum One, ZERO Network, zkLink Nova, etc.)
- **ForceRefreshManager expiry** : le flag expire quand `last_full_scan_ms > requestedAt` (v4.15.2) ÔÇö c'est-├á-dire quand un full scan complet a eu lieu apr├¿s la d├®tection de TX. Safety TTL de 2h pour les flags orphelins. `BaseEngine.checkActivityForceRefresh` v├®rifie `state.lastFullScanMs` et clear le flag si le full scan est post├®rieur
- **SVM native balance sans consensus** : contrairement ├á l'EVM, le SVM Engine n'a PAS de consensus RPC pour le native balance. Si `getBalance` ├®choue mais les tokens r├®ussissent, le nativeBalance=0 ├®crase le cache. Fallback ajout├® en v4.14.10 : si RPC ├®choue et cache contient un balance>0, le cache est pr├®serv├®
- **clasp run / Execution API** : OK depuis 2026-04-29. Token OAuth wcore-mcp (`652188583224-i1qqdjip5f598p7ft67rvib751en3d9l.apps.googleusercontent.com`) dans `.clasprc.json` avec TOUS les scopes (`script.scriptapp`, `script.external_request`, `script.storage`, `spreadsheets`, `userinfo.email`). Client secret dans `.playwright-mcp/`. Si le token expire, suivre la proc├®dure dans "Autonomie sans MCP".
- **Trigger mensuel GAS** : pas de trigger "mensuel" natif dans Apps Script. Pattern : trigger hebdomadaire + staleness check interne (25j). `UPDATE_DYNAMIC_RPCS()` s'auto-skip si donn├®es < 25j ÔåÆ ex├®cution effective ~28j
- **UrlFetchApp `deadline` et `timeout` NON-FONCTIONNELS** : ni `fetch` ni `fetchAll` ne respectent ces param├¿tres dans GAS. Aucune protection timeout native n'existe. Les erreurs quota ("Service invoked too many times") prennent ~10s par appel avant de throw. v4.15.4 contourne avec early-abort et budget temps dans batchWithConsensus (03_HTTP.gs + 05_RPC.gs)
- **ChainFactory config** : acc├®der via `chain.getConfig()`, PAS `chain._config` ni `chain.config` (undefined). Le global RPC est `RpcClient` (pas `Rpc`). `pickForConsensus` a `minRpcs=2` qui override le count demand├® ÔÇö truncate rpcList apr├¿s si besoin de 1 RPC
- **UPDATE_DYNAMIC_RPCS consomme ~250 HTTP calls** : tester 241 RPCs + latency. Ex├®cuter t├┤t apr├¿s le quota reset (10h30 CET). Les appels de test/debug s'accumulent vite ÔÇö le circuit breaker peut se tripper en milieu de session
- **GAS 6 min pour fonctions admin/trigger** : les fonctions non-@customfunction (UPDATE_DYNAMIC_RPCS, BUILD_RPC_LOOKUP, etc.) ont une limite de 6 min, pas 30s. `_testRpcLatency` (v4.15.5) utilise un budget de 4 min pour rester dans cette limite
- **`fetchAll` parall├¿le = latence trompeuse** : tous les RPCs partagent le m├¬me `t0` ÔåÆ tri par latence inefficace. v4.15.5 utilise des fetch individuels s├®quentiels pour une mesure r├®elle par RPC
- **return invisible dans l'├®diteur GAS** : quand on ex├®cute une fonction depuis l'├®diteur Apps Script (bouton Run), les `return` ne sont PAS affich├®s dans les logs. Seuls `Logger.log()` et `console.log()` apparaissent. Pattern : ajouter `console.log()` dans les fonctions admin/diagnostic pour visibilit├® ├®diteur
- **safe-push brace warnings** : le compteur d'accolades de safe-push.ps1 est un simple compteur de `{`/`}` qui ne distingue pas strings/commentaires. Les warnings sur 04B_CACHE_WALLET.gs, 33_DYNAMIC_RPC.gs et 18_CLEANUP.gs sont des faux positifs (accolades dans des strings comme `"{"`, `"${"`)
- **GAS file load order** : les fichiers GAS sont charg├®s par ordre alphab├®tique (00_ avant 01_ avant 32_). Les IIFEs `(function(){...})()` s'ex├®cutent imm├®diatement au chargement ÔåÆ les variables des fichiers ult├®rieurs ne sont pas encore d├®finies. C'est pourquoi VERSION_SCANNER (00B) ne peut pas lire les `*_VERSION` variables et doit d├®f├®rer ├á MODULE_AUTOREGISTER (32_)
- **WCORE_VERSION source unique** : `WCORE_VERSION` est d├®fini UNIQUEMENT dans `01_INIT.gs` (objet avec MAJOR/MINOR/PATCH + toString()). Ne JAMAIS red├®clarer dans 00_VERSION_REGISTRY.gs. L'enregistrement ModuleRegistry("WCORE") est fait dans 32_MODULE_AUTOREGISTER.gs (car 01_INIT n'est pas charg├® quand 00_ s'ex├®cute)
- **_mergeAssetsPreservingCached et activityForced (v4.15.33)** : le merge NE DOIT JAMAIS ├¬tre skip├®, m├¬me en forceFull. `activityForced` (WATCHDOG ÔåÆ `state.force=true` ÔåÆ `_forceFull=true` sur cacheObj) faisait skipper tout le merge ÔåÆ tokens absents du scan (consensus RPC fail) disparaissaient du cache. Fix : suppression du `if(force)return cacheObj` dans `_mergeAssetsPreservingCached` (04C_CACHE_GLOBAL.gs:982-984). Seuls les tokens **explicitement** rescann├®s avec balance=0 (pr├®sents dans newSet) sont supprim├®s. Repro : ZERO Network WBTC (3 RPC fragiles, 1 timeout, pas de majorit├®).
- **Balances microscopiques (E-12) sur single-RPC chains (v4.15.34)** : quand un RPC free tier limite les batchs (drpc.org = max 3 req/batch), le call `decimals()` peut ne pas ├¬tre dans le batch r├®ussi. Fallback ├á 18 sur un token ├á 6 d├®cimales ÔåÆ balance divis├®e par 10^12 (2.83E-12 affich├® comme 0). **Fix legacy** : ajouter `RPC.TOKEN_DECIMALS` dans la config chain pour les d├®cimales connues. **Fix core** : utiliser `chain.RPC?.TOKEN_DECIMALS` comme override dans `getEvmWalletAssets` et `getErc20Metadata`. **Bump CACHE_VERSION** pour invalider les vieux caches.
- **Sentinel eth_getBalance (v4.15.34)** : certaines cha├«nes EVM retournent une valeur sentinel fixe pour `eth_getBalance` (ex: Tempo ÔåÆ 424242424242424... pour toutes les adresses). Cela pollue le cache native et fausse le total. **Fix** : `FLAGS.DISABLE_NATIVE_BALANCE = true` dans la config chain + `NATIVE_SYMBOL=""` pour skip la ligne native. Dans le core, garde-fou `disableNative ? 0n : await readNativeBalance(...)`.
- **Cache vote bloque correction decimals (v4.15.34)** : dans `_scanBatch` (09_SIMPLE_ROTATION.gs), le cache est ajout├® comme "vote" au consensus RPC. Si le cache a ├®t├® calcul├® avec un mauvais nombre de d├®cimales (balance microscopique), il bloque la valeur correcte retourn├®e par le RPC. **Fix** : `skipCacheVote` quand `cachedBalance > 0 && cachedBalance < 1e-12` ET pas de `decimals` valide dans metaMap. Ainsi le RPC seul peut corriger la balance sans ├¬tre contrecarr├® par le cache erron├®.
- **GM off-chain vs on-chain (v0.1.13+)** : le GM off-chain (POST /api/gm) met ├á jour le streak quotidien mais **n'emp├¬che PAS** le GM on-chain. Le GM on-chain valide les deux (streak quotidien + streak per-chain). Le frontend doit refl├®ter ├ºa : le bouton "Off-chain" se grise apr├¿s un GM off-chain OU on-chain (daily streak done), le bouton "On-chain" n'est jamais gris├® par un GM off-chain ÔÇö seulement par `sending` (tx en cours). L'├®v├®nement `wcore-gm-done` sans `chain` = off-chain ÔåÆ GmButton l'utilise pour griser Off-chain uniquement. Avec `chain` = on-chain ÔåÆ Cha├«nCard grise cette cha├«ne sp├®cifique.
- **ChainCard localStorage GM check** : utilise `wc_gm_onchain_date` (PAS `wc_gm_date`) pour v├®rifier si un on-chain GM a ├®t├® fait aujourd'hui sur cette cha├«ne. `wc_gm_date` est commun ├á off-chain et on-chain ÔåÆ l'utiliser pour le check per-chain fausserait le r├®sultat apr├¿s un off-chain (le on-chain d'hier serait cru d'aujourd'hui).
- **GmContract.creatorAddress** : stocke l'adresse wallet du d├®ployeur (lowercase). Ajout├® en v0.1.13 pour le matching robuste dans my-contracts (ne d├®pend plus du CUID). Toujours renseigner `creatorAddress` ├á la cr├®ation/upsert.
- **my-contracts matching** : utilise `OR: [{ creatorAddress }, { owner: { address } }]` pour trouver les contrats par adresse wallet, pas par CUID. Le background sync a un cooldown de 5 min par user.
- **POST /api/gm/contracts/deploy** : v├®rification on-chain obligatoire (txHash ÔåÆ receipt ÔåÆ factory ÔåÆ ContractDeployed event ÔåÆ status 0x1). L'adresse du contrat d├®ploy├® est extraite depuis `topics[1]` de l'event `ContractDeployed`, PAS depuis `receipt.contractAddress` (qui est null pour les CREATE internes ├á la factory). Le frontend lit aussi l'adresse depuis le receipt log (pas `contractCount()` pour ├®viter les race conditions).
- **GM creator balance check (post-v0.1.19)** : `/api/gm/contracts/:id/balance` utilisait `contract.ownerId === req.user.id` (ID interne) pour d├®terminer si l'utilisateur est le cr├®ateur. Ce check ├®choue si l'utilisateur se reconnecte avec la m├¬me adresse wallet mais un ID session diff├®rent. Fix : utiliser `contract.creatorAddress?.toLowerCase() === userAddress` (adresse wallet), qui correspond ├á la r├®alit├® on-chain.
- **POST /api/gm/onchain** : transaction atomique via `prisma.$transaction`. Anti-replay (`[chainKey, txHash]` unique), score, streak, badges dans la m├¬me transaction. Le tip amount est d├®cod├® depuis l'event log (uint256 data) et persist├® dans `OnchainGm.tipWei`. `OnchainGm.createdAt` doit ├¬tre le timestamp de l'event `GmCheckedIn`, pas `now()`, sinon un retry/backfill tardif fabrique un GM "aujourd'hui". `rebuildChainStreakFromOnchain()` ins├¿re aussi `createdAt` depuis l'event et ne baisse pas un `UserChainGm.gmStreak` existant plus ├®lev├®.
- **Header On-chain vs ChainCard** : le Header s├®lectionne un contrat al├®atoire multi-cha├«ne ÔåÆ se bloque apr├¿s le 1er on-chain GM r├®ussi (`alreadyOnChain`). Les GMs additionnels sur d'autres cha├«nes passent par les boutons **ChainCard** (bonus per-chain).
- **syncOnChainContracts overwrite protection** : avant d'upsert un contrat, v├®rifie si un `ownerId` existe d├®j├á et appartient ├á un autre user ÔåÆ skip. N'├®crase jamais un owner existant.
- **Off-chain double-click guard** : ├®tat `offchainSending` emp├¬che les clics multiples pendant le POST off-chain.

## Gotchas WCORE Web - Security follow-up post-v0.1.19

- **CEX Binance/Bitpanda web (2026-06-13)** : le portage web des modules GAS `35_BITPANDA_SYNC.gs` / `36_BINANCE_SYNC.gs` garde les holdings CEX dans des tables separees (`cex_accounts`, `cex_holdings`) et les affiche via Profile > CEX. Ne jamais les injecter comme fausses chaines dans le scan on-chain. Contrairement a `wcore-gsheet`, le web NE fusionne PAS `USDC/TUSD -> USDT` ni `EUR/EURI -> EURC` : chaque symbole reste distinct et seul `trim().toUpperCase()` est applique. **Binance multi-user** : chaque utilisateur saisit SES PROPRES cles API read-only (`apiKey` + `apiSecret`) dans Profile. L'API WCORE les chiffre (AES-GCM) et les envoie au service `binance-relay` (Railway, IP non bloquee par Binance HTTP 451) qui SIGNE la requete HMAC avec le secret du user. Le secret ne touche jamais le navigateur. Bitpanda appelle l'API officielle server-side avec l'`apiKey` du user. Vars API requises : `RELAY_TOKEN` + `BINANCE_RELAY_URL` (ou `RAILWAY_SERVICE_BINANCE_RELAY_URL`) + `CEX_SECRET`.
- **binance-relay dual-endpoint (2026-06-13)** : le service `binance-relay` vit dans `wcore-gsheet/railway-relay/server.js` (source unique, deploye depuis la). DEUX endpoints : (1) `GET /binance?token=...` LEGACY pour `wcore-gsheet`/Apps Script ÔÇö cles serveur fixes `BINANCE_API_KEY`/`BINANCE_API_SECRET`, symboles FUSIONNES (USDC/TUSD->USDT, EUR/EURI->EURC). NE PAS MODIFIER cet endpoint, `wcore-gsheet` en depend. (2) `POST /binance/account` MULTI-USER pour WCORE web ÔÇö body `{token, apiKey, apiSecret}`, cles du user signees ici, symboles NON fusionnes (`pushRowExact`/`fetchSpotExact`/`fetchEarnExact`). Les deux partagent `RELAY_TOKEN`. Apres edit du relay, redeployer le service `binance-relay` depuis `wcore-gsheet`.
- **CEX dans `/home` + `/wallet` (2026-06-13)** : quand l'utilisateur est connecte, `useCexHoldings.ts` charge `/api/cex/accounts` et convertit chaque compte CEX en `ScanResult` synthetique (`address: cex:{provider}:{id}`, `isCex: true`, `chainKey: CEX_BINANCE`/`CEX_BITPANDA`). Le bouton `Scan` de `/home` synchronise d'abord les CEX (`POST /api/cex/accounts/:id/sync`), puis `/wallet` lit les holdings caches. Les CEX apparaissent dans le selecteur `/wallet` et peuvent etre deselectionnes comme des wallets, mais ne declenchent JAMAIS de scan on-chain ni GM. UI canonique : dans les listes `/home` et `/profile?tab=wallets`, pas de logo ni badge CEX, juste `Binance`/`Bitpanda` + `Exchange account` + `Remove` pour rester harmonieux avec les wallets. Dans `/wallet`, le header externe garde `Bitpanda [CEX]` / `Binance [CEX]` sans icone redondante ; la carte interne affiche l'icone plateforme a la place de l'icone chaine et le badge `CEX` a la place du badge VM. Les actifs CEX (`SYMBOL:bucket`) sont exclus du scam detector, des reports et des liens explorer.
- **Pricing CEX (2026-06-13)** : provider-first. Binance : `binance-relay` multi-user renvoie une map `prices` via `/api/v3/ticker/price` en batch (evite timeouts et HTTP 451), l'API WCORE utilise `quoteEur` avant fallback DefiLlama/stable/fiat. Bitpanda : ticker public `https://api.bitpanda.com/v1/ticker` pour crypto/commodities ; les stocks US (`AVGO`, `AAPL`, etc.) ne sont PAS dans ce ticker, fallback Yahoo Finance chart (`regularMarketPrice`, conversion USD->EUR via `getEurUsdRate`). Relay gotcha : deployer depuis `wcore-gsheet/railway-relay`, pas la racine ; `index.js` charge `server.js` car Railway peut demarrer `node /app/index.js`.
- **CEX Bitfinex web (2026-06-14, v0.2.44)** : 3e provider CEX, portage de `37_BITFINEX_SYNC.gs` (wcore-gsheet). **Routage API DIRECTE server-side, PAS de relais** : contrairement a Binance (IP datacenter bloquee HTTP 451), Bitfinex ne bloque PAS les IP datacenter ÔÇö l'API WCORE signe en **HMAC-SHA384** (pas SHA256) + nonce (`Date.now()*1000`) sur `/api/<path><nonce><body>` et POST `https://api.bitfinex.com/v2/auth/r/wallets` directement (`bitfinexAuthPost` dans `apps/api/src/plugins/cex.ts`). Aucune modif de `wcore-gsheet/railway-relay`. Credentials = `apiKey` + `apiSecret` (modele Binance, PAS Bitpanda key-only) : dans les ternaires de `cex.ts`, Bitfinex suit la branche Binance (`provider === "binance" || provider === "bitfinex"`). **Seul le wallet `exchange` (spot)** est synchronise (filtre `w[0] === "exchange"` sur `[WALLET_TYPE, CURRENCY, BALANCE, ...]`). **Contrairement aux autres CEX web, Bitfinex FUSIONNE** les symboles via `bitfinexCanonicalSymbol` (`normalizers.ts`) : alias courts (`ATO`ÔåÆ`ATOM`, `IOT`/`MIOTA`ÔåÆ`IOTA`, `DOG`ÔåÆ`DOGE`, `DAT`ÔåÆ`DATA`, `QTM`ÔåÆ`QTUM`, `BTCF0`ÔåÆ`BTC`) PUIS consolidation stables/fiat (`USD`/`USDC`/`UDC`/`TUSD`/`USTF0`ÔåÆ`USDT`, `EUR`/`EURT`/`EUT`/`EURS`/`EUS`/`EURI`/`EUTF0`ÔåÆ`EURC`) ÔÇö c'est conforme au module GAS (Bitfinex utilise des codes courts historiques qu'il FAUT normaliser, sinon `ATO`/`IOT`/`UDC` ne sont pas prices). **Logo** : CMC officiel `s2.coinmarketcap.com/static/img/exchanges/128x128/37.png` (simpleicons `bitfinex` et `bitfinex.com/favicon.ico` renvoient 404). `CEX_BITFINEX` ajoute a `chain-icon-manifest.json` + `CONTAIN_LOGOS` (`ChainIcon.tsx`) + `ChainCard.tsx` (branche cexProvider) + `cex-display.ts` regex `(binance|bitpanda|bitfinex)`. Aucune var env nouvelle (`CEX_SECRET` partage).
- **CEX Bybit web (2026-06-15, v0.2.45)** : 4e provider CEX, portage de `38_BYBIT_SYNC.gs` mais **routage obligatoire via `cex-relay` Railway EU** (pas d'appel direct API WCORE, `api.bybit.eu` geo/IP-block selon l'environnement). Le relais source vit dans `wcore-gsheet/railway-relay/server.js`, endpoint web multi-user `POST /bybit/account` protege par `RELAY_TOKEN`; body `{token, apiKey, apiSecret}`; le navigateur ne voit jamais le secret. L'API web chiffre les credentials (`CEX_SECRET`) puis appelle le relais via `BYBIT_RELAY_URL` ou `CEX_RELAY_URL`, avec fallback `BINANCE_RELAY_URL`/`RAILWAY_SERVICE_CEX_RELAY_URL` car l'ancien env Binance peut pointer vers le meme `cex-relay`. Bybit v5 signe en HMAC-SHA256 sur `timestamp + apiKey + recvWindow + queryString`, endpoint `https://api.bybit.eu`, headers `X-BAPI-*` + `x-referer: Cg000971`. Sync = `UNIFIED` (`/v5/account/wallet-balance`) + `FUND` (`/v5/asset/transfer/query-account-coins-balance`, best-effort). **Contrairement au GAS legacy `/bybit`, le web NE fusionne PAS les symboles** : `USDC`, `USDT`, `USD`, `EUR`, `EURI`, `EURT` restent distincts, coh├®rent avec Binance web. `CEX_BYBIT` ajoute provider meta dans `cex-display.ts`, parsing `cex:bybit:<id>`, `useCexHoldings`, `ChainCard`, `ChainIcon.CONTAIN_LOGOS`, et formulaire Profile > CEX. Logo verifie : CMC exchange `https://s2.coinmarketcap.com/static/img/exchanges/64x64/521.png` (SimpleIcons Bybit retourne 404).
- **Coinbase + OKX = PROCHAINE SESSION (2026-06-16)** : 5e/6e providers CEX, deja faits dans `wcore-gsheet` (`39_COINBASE_SYNC.gs`, `40_OKX_SYNC.gs`) ET dans le relais `wcore-gsheet/railway-relay/server.js` (endpoints legacy `GET /coinbase` cles serveur fixes + `GET /okx`). Le relais a deja `coinbaseJwt()` (ES256 CDP `COINBASE_API_KEY_NAME` + `COINBASE_PRIVATE_KEY` EC PEM, `derToJose()`, `/api/v3/brokerage/accounts`) et `okxAuthGet()` (HMAC-SHA256 + passphrase, `/api/v5/account/balance` + `/api/v5/asset/balances`). **├Ç ajouter pour le web multi-user (pattern Bybit)** : endpoints relais `POST /coinbase/account` + `POST /okx/account` qui recoivent les creds du user au lieu des cles serveur fixes, NE fusionnent PAS les symboles. C├┤t├® API : `normalizeCoinbaseBuckets`/`normalizeOkxBuckets`, `fetchCoinbaseRows`/`fetchOkxRows`, routage credentials (`CexProviderSchema`). C├┤t├® front : `cex-display.ts` meta+logos, `useCexHoldings`, `ChainCard`, `ChainIcon.CONTAIN_LOGOS`, Profile > CEX (Coinbase = key name + EC private key ; OKX = key + secret + passphrase). Voir ROADMAP.md section "PROCHAINE SESSION".
- **CEX Bitpanda stocks pricing via relais (2026-06-15, v0.2.46)** : Yahoo Finance refuse l'IP datacenter de l'API WCORE (`fetch failed` dans les logs, PAS un 429 ÔÇö meme blocage que Binance HTTP 451). Le pricing direct Yahoo (v0.2.43) echouait donc en prod. **Fix** : endpoint `POST /stock/prices` sur `cex-relay` (`wcore-gsheet/railway-relay/server.js`), body `{token, symbols}`, retourne `{SYMBOL:{priceEur,source}}`. L'API appelle `fetchStockPricesViaRelay` (`apps/api/src/cex/stock-relay.ts`) via `getStockRelayUrl()` (memes env que Bybit) + `RELAY_TOKEN`, cache Redis 6h `stockprice:{SYMBOL}`, fallback quote provider Bitpanda. **Multi-devises** : le relais convertit USD/GBP/GBp(pence, devise Yahoo `GBp` exactement)/CHF/KRW ÔåÆ EUR via taux `EUR{CCY}=X`. **Symboles** : mapping curat├® `STOCK_YAHOO_SYMBOLS` dans le relais ÔÇö coreens `HYXS`ÔåÆ`000660.KS` (SK Hynix), `SSU`/`SMSN`ÔåÆ`005930.KS` (Samsung), `NESN`ÔåÆ`NESN.SW` (CHF). Receipt Samsung `SSU`/`SMSN` divise par 25 (`STOCK_RECEIPT_DIVISOR`, aligne facteur ├ù25 wcore-gsheet). **Candidats minimaux** : un ticker US simple = 1 seul appel Yahoo (le balayage sp├®culatif des 8 suffixes EU pour TOUS les symboles saturait Yahoo en 429 ÔåÆ tous les stocks tombaient a `ÔÇö`). Aliases Bitpanda dans `stock-pricing.ts` (`AMD-US`ÔåÆ`AMD`, `BRKB`ÔåÆ`BRK-B`, `FB`ÔåÆ`META`, `MRKUS`ÔåÆ`MRK`, `RDSA`ÔåÆ`SHEL`, `TSFA`ÔåÆ`TSLA`, `BROA`ÔåÆ`AVGO`). **Collision crypto/stock** : `priceCexRowsForTest` tente le pricing stock AVANT le quote provider Bitpanda pour les lignes `bucket==="stocks"`, sinon un ticker crypto homonyme (ACN, AMZN, MC, WMT) price l'action avec un prix crypto. Apr├¿s edit du relais, redeployer `railway up --service cex-relay --ci` depuis `wcore-gsheet/railway-relay`.

- **FX cascade EUR/USD (v0.2.47, 2026-06-17)** : le code historique avait un `1.08` hardcod├® dans 4 engines (`engines/evm-scan.ts:117`, `engines/evm-batch.ts:487`, `engines/svm.ts:90`, `engines/ton.ts:80`, `engines/cosmos.ts:117`) qui corrompait silencieusement les prix quand la source FX (Frankfurter seul) echouait. **Cause racine** : pas de fallback, mais le 1.08 etait un `DEFAULT_USD_EUR` masque en `cascadethrow` qui devenait silencieux. **Fix** : cascade 4 sources (Frankfurter, open.er-api.com, Coinbase exchange-rates, DefiLlama EURC) avec consensus median (`packages/core/src/fx.ts`). Frankfurter et DefiLlama EURC retournent `1 EUR = X USD` nativement ÔåÆ invers├®s dans le parseur pour obtenir `1 USD = X EUR` (convention `priceEur = priceUsd * fxRate`). 4 sources free, sans cle, sans auth. **Consensus** : ÔëÑ3 sources ÔåÆ mediane (rejette outliers), 2 sources ÔåÆ moyenne si delta < 5% sinon throw, 1 source ÔåÆ retour, 0 source ÔåÆ **throw** explicite. **Plus aucun fallback fixe** (suppression de tout `1.08`/`0.92`/`0.86` historique). Si toutes les sources tombent, le scan throw `FX cascade: only 0 source(s) succeeded, need 1` ÔÇö pas de corruption silencieuse. **Cache** : `cacheKey("fxEurUsd", {})` = `fx:eur:usd` (TTL 1h, memoire + Redis optionnel). **Repro du bug originel** (sans cascade) : web ETH = 1623.96 EUR (FX 0.918 stale), web WBTC = 154.05 EUR, drift intra-runtime 5.5%, **sans aucun signe visible UI**. La cascade elimine cette classe de bug. **Cross-runtime telemetry** : web self-publie sur `/api/price/fx` ÔåÆ `fx_telemetry:web`. Gsheet POST apr├¿s chaque cascade reussie ÔåÆ `/api/gsheet/fx-telemetry` (auth `x-gsheet-token`, `WCORE_WEB_API_URL` et `GSHEET_API_TOKEN` set par onOpen dans `_SETUP_WCORE.gs`). **Endpoint `GET /api/diag/fx-parity`** : retourne `{web, gsheet, drift, ok}` avec tolerances 2% (warn, exit 1) et 5% (alert, exit 2). **CSRF** : `requiresCsrfOriginCheck` dans `server-helpers.ts` exclut `/api/gsheet/*` (auth par token, pas cookie) ÔÇö sans ce fix le POST telemetry etait bloqu├® en 403. **Tests** : 26 unit tests (`packages/core/src/fx.test.ts`) + 9 cross-runtime spec (`wcore-gsheet/scripts/fx-cascade-spec.test.cjs`) + 1 live (`wcore-web/scripts/test-fx-parity.cjs`). **Symptome post-deploy** : les scan results caches en `scan:result:*` Redis peuvent encore utiliser l'ancien FX jusqu'a expiration du TTL. ETH et WBTC scannes a des moments differents montrent des prix incoherents. Fix : `forceRefresh=true` sur les chaines concernees. Doc focalisee : `wcore-web/docs/fx-cascade.md`.
- **Micro-cryptos Bitpanda non valorisables (2026-06-15)** : `APP`, `DCK`, `DOGA`, `GODL`, `KIP`, `LAI` restent a `ÔÇö`. Le ticker Bitpanda officiel (`api.bitpanda.com/v1/ticker`) les cote lui-meme a `EUR=0.0000` et CoinGecko search renvoie plusieurs tokens ambigus par symbole. NE PAS deviner un CoinGecko ID (risque de gonfler le total comme le bug ETHG). `ÔÇö` est le comportement honnete.
- **Logos stocks CEX via DuckDuckGo (2026-06-15)** : `apps/web/lib/cex-stock-logos.ts` mappe chaque ticker stock/ETF Bitpanda ÔåÆ domaine de marque ÔåÆ `https://icons.duckduckgo.com/ip3/<domain>.ico`. **Clearbit (`logo.clearbit.com`) est DEPRECIE ÔÇö son DNS ne resout plus**, ne pas l'utiliser. DuckDuckGo est gratuit, sans cle, fiable (verifie GET 200, pas HEAD qui echoue). `useCexHoldings.holdingToToken` attache `logoUrl` UNIQUEMENT quand `bucket === "stocks"`. Inclut les aliases (`FB`, `*-US`, `BRKB`, `TSFA`) et receipts (`SSU`/`SMSN`ÔåÆsamsung.com).
- **TokenIcon chevauchement fallback (2026-06-15)** : le cercle colore de fallback (lettre du symbole) etait TOUJOURS rendu derriere l'image. Pour les cryptos (logos opaques CDN) invisible, mais pour les favicons stocks a fond transparent le cercle+lettre transparaissaient ÔåÆ effet dedouble/decale. **Fix** : etat `imageLoaded`, fallback masque (`opacity-0`) des que l'image charge avec succes, fond neutre `bg-bg` pendant le chargement, et `object-cover`ÔåÆ`object-contain` pour ne pas recadrer/zoomer les favicons carres. Test de garde : `token-icons.test.ts` "logo image uses object-contain".
- **Refresh All sync les CEX (2026-06-15)** : sur `/wallet`, `handleRefreshAll` (`WalletContent.tsx`) ne faisait qu'un re-scan on-chain (`triggerForceRefresh`). Il POST maintenant `/api/cex/accounts/:id/sync` pour chaque compte CEX (id parse depuis `cex:<provider>:<id>` via `parseCexWalletAddress`) puis `reloadCex()`, en plus du re-scan. `apiFetch` et `parseCexWalletAddress` deja importes dans le fichier.


- **Connect Wallet multi-extension (2026-06-03)** : MetaMask + Zerion peuvent casser `window.ethereum` (`Cannot redefine property: ethereum`, `Cannot set property ethereum ... only a getter`) et le chunk wagmi `injected` peut throw `ReferenceError: injected is not defined`. Ne pas enregistrer `injected()` dans le config wagmi statique. Utiliser WalletConnect QR en fallback permanent + EIP-6963 (`eip6963:announceProvider`) pour connecter les providers d├®tect├®s en direct (`eth_requestAccounts` + `personal_sign`). Le bouton `Connect Wallet` doit toujours ouvrir le picker, m├¬me si `useConnectors()` est vide.
- **WalletConnect Project ID vide (2026-06-03)** : `.env.local` peut contenir `NEXT_PUBLIC_WC_PROJECT_ID=` et ├®craser le default Dockerfile. Garder un fallback code `3090760ada2bf4a459a27506fcdc16ec` dans `wagmi.ts` pour que WalletConnect/Coinbase restent disponibles en prod/dev.
- **SIWE origin matching wcore.xyz (2026-06-03)** : `/api/auth/nonce` doit choisir le `CORS_ORIGIN` dont le hostname matche `req.headers.origin`. Sinon le message SIWE peut afficher localhost ou un autre domaine, et certains wallets refusent la signature. V├®rifier en prod : `wcore.xyz wants you to sign in...`.
- **GM page status unknown (2026-06-03)** : ne jamais convertir un statut GM global absent/vide en `{ deployed:false }` pour toutes les cha├«nes. `deployed:null` = inconnu et doit d├®clencher seulement un `has-deployed` cibl├®. Sinon `/gm` affiche `Deploy GM Contract` partout apr├¿s un fetch status rat├® ou vide.
- **Header GM on-chain optimistic lock (2026-06-03)** : `useOnChainGm.sendGm()` appelle `/api/gm/onchain` en fire-and-forget. Le backend peut lagger apr├¿s la tx. Le Header doit rester disabled localement via `wc_gm_onchain_chains` et l'event `wcore-gm-done`, m├¬me si `/api/auth/me` r├®pond encore `gmOnChainToday=false`. Un GM on-chain valide aussi le GM quotidien global : Off-chain doit afficher `Done today` et ├¬tre disabled quand `alreadyOnChain=true`.
- **Post X site-back (2026-06-03)** : post publi├® `https://x.com/WCORExyz/status/2062228120476725406`. Assets : `apps/web/public/wcore-post-site-back.svg` et `.png`. DA : garder le format 1200x675, fond sombre WCORE, badges s├®par├®s en haut droit, pas de texte qui d├®passe des cards. PNG ignor├® par d├®faut sauf exception `.gitignore` explicite.
- **R├®conciliation `wcore-gsheet` ÔåÆ `wcore-web` (2026-06-03)** : ne jamais merge automatiquement les `.gs` vers les `.ts`. D'abord classer chaque changement : Sheets-only (J1 latch, triggers OAuth, dashboards), portable conceptuel (cold-start RPC probe, OutputSnapshot ├®quivalent Redis), d├®j├á couvert (Multicall3, Blockscout discovery), ou cha├«ne ├á v├®rifier. Source d├®taill├®e : `docs/wcore-gsheet-to-web-reconciliation-2026-06-03.md`. Ne pas supprimer une cha├«ne web (`MIND`, `ZERO`, `REDSTONE`, etc.) sans v├®rifier RPC vivant, usage UI/GM et registry.
- **RPC harmonisation 11 couches (2026-06-03)** : `static ÔåÆ chainlist dynamic ÔåÆ health per-endpoint ÔåÆ consensus strict ÔåÆ timeout ÔåÆ per-chain timeout ÔåÆ cold-start probe ÔåÆ snapshot fallback ÔåÆ kill-switch ÔåÆ single-RPC warning ÔåÆ UI`. 8 cha├«nes d├®sactiv├®es via `FLAGS.DISABLE_CHAIN=true` (CROSS_MAINNET, ETHO_PROTOCOL, HAVEN1, MOCA_CHAIN, POLYNOMIAL, RIVALZ, STACK, SURFLAYER). `scripts/audit-rpcs.mjs` probe l'├®tat vivant des RPCs statiques en 4s timeout. Le scan API filtre ces cha├«nes sauf `WALLET_INCLUDE_DISABLED=1`. Note d├®taill├®e : `docs/rpc-harmonization-2026-06-03.md`.

- **GM balance legacy fallback** : `/api/gm/contracts/:id/balance` doit accepter `creatorAddress === userAddress` **OU** `ownerId === req.user.id`. `creatorAddress` est la source robuste apres reconnexion wallet, mais `ownerId` reste necessaire pour les anciennes rows sans `creatorAddress`; sinon elles retournent 403 au createur.
- **Linked wallet SVM/Cosmos signature binding** : ne jamais verifier seulement que la signature correspond au `publicKey` fourni. Il faut aussi deriver l'adresse depuis cette cle et comparer a `body.address` avant `SIGNED`. SVM : `bs58(publicKey) === address`. Cosmos : `toBech32(prefix, ripemd160(sha256(compressedSecp256k1Key))) === address`.
- **Notification SSE token hygiene** : ne pas passer le JWT dans l'URL EventSource. Le frontend doit appeler `POST /api/notifications/stream-token` avec `Authorization`, puis ouvrir `/api/notifications/stream?token=<opaque>`. Token opaque single-use, TTL 60s.
- **GmOnChain CreatorWithdrew event** : capturer `uint256 amount = creatorBalance` avant reset. Emettre `CreatorWithdrew(creator, amount)`, sinon l'event loggue toujours 0 apres retrait.
- **pnpm security overrides** : `solc@0.8.35` depend encore de `tmp@0.0.33` et `next@16.2.4` ramenait `postcss@8.4.31`. Garder `pnpm.overrides` pour forcer `tmp>=0.2.4` et `postcss>=8.5.10`; verifier avec `pnpm audit --json`.
- **Remote Git wcore-web (2026-05-08)** : ce clone n'avait aucun `origin` configure (`git remote -v` vide). Le commit `c3f3adc` reste local tant qu'un remote n'est pas ajoute via `git remote add origin <url>` puis `git push -u origin master`.
- **Staging local ports (2026-05-08)** : `.env.staging` met l'API sur `4001`, le Web sur `3001`; le default dev/prod reste `4000`/`3000`. La confusion initiale (PORT=3002 non honore) venait d'une app tierce (Manifest) sur le port 3000. Le standalone Next lit correctement `process.env.PORT`. `deploy-staging.ps1 -AutoStart` detecte les conflits de port avant lancement. `smoke-test.ps1` verifie que la reponse contient "WCORE" pour ne pas se tromper d'app.
- **Scripts staging PowerShell (2026-05-08)** : cause racine du parse failure = UTF-8 sans BOM + typographie (`ÔÇö`, coches, s├®parateurs) relue par Windows PowerShell 5.1 comme guillemets/texte casse. Fix applique : scripts ASCII-only. `smoke-test.ps1 -ApiPort 4001 -WebPort 3001` passe 15/15. `deploy-staging.ps1 -SkipBuild` parse, mais sa verification complete necessite Docker hors sandbox si l'acces a `~/.docker/config.json` / `npipe docker_engine` est refuse.
- **Manifest app on port 3000 (2026-05-08)** : une app Manifest (non-WCORE) tournait sur le port 3000, ce qui a cree la confusion quand le standalone Next avec PORT=3001 repondait correctement mais le smoke test visait 3000 et voyait HTML (pas WCORE). Tuer tout `node.exe` inconnu sur les ports 3000/3001 avant de lancer le staging. Manifest a ete supprime completement (`~\.manifest`, `~\manifest`, `wcore-web\.manifest`).
- **CORS multi-origine dev (2026-05-09)** : `CORS_ORIGIN` accepte les virgules pour autoriser `localhost` ET `127.0.0.1` simultanement. Sans ca, `fetch()` depuis `localhost:3000` vers l'API sur `127.0.0.1:4000` echoue en "Failed to fetch" (origines differentes, CORS bloque).
- **SIWE domain vs URI mismatch (2026-05-10)** : le `domain` du message SIWE vient de `req.headers.host` (ex: `127.0.0.1`), mais le `URI` etait toujours le 1er `CORS_ORIGIN` (ex: `http://localhost:3000`) ÔåÆ mismatch ÔåÆ `siwe_uri_mismatch`. **Fix** : `getNonce` choisit le `CORS_ORIGIN` dont le hostname match le domaine de la requete. Si aucune correspondance, fallback au 1er entry. `CORS_ORIGIN` doit inclure `localhost` ET `127.0.0.1`.
- **SIWE domain = web origin, not API host (2026-05-10)** : en prod, l'API et le Web sont sur des domaines differents. Le SIWE domain doit etre le domaine WEB (CORS_ORIGIN), pas le host API. Login validation verifie contre `_siweAllowedDomains` (CORS_ORIGIN) plutot que `req.headers.host`.
- **Docker + Railway (2026-05-10)** : `railway up` deploye depuis le Dockerfile local sans GHCR. `railway.json` controle quel Dockerfile est utilise (`apps/api` vs `apps/web`). `NEXT_PUBLIC_API_URL` doit etre passe en ARG Docker (build-time) et en variable Railway. `prisma db push` lance au startup du conteneur API (CMD dans Dockerfile).
- **Web port 8080 sur Railway** : Next.js standalone utilise 8080 par defaut sur Railway (override du PORT=3000 du Dockerfile). Custom domain ÔåÆ port 8080 pour le web, 4000 pour l'API.
- **Docker Desktop + sandbox (2026-05-09)** : le service `com.docker.service` peut s'arreter spontanement ÔåÆ `docker ps` echoue "pipe not found" ÔåÆ Postgres/Redis down ÔåÆ auth echoue `auth_failed` au `prisma.user.upsert`. Relancer Docker Desktop (icone tray ÔåÆ Restart) pour retablir les conteneurs. Le sandbox n'a pas les droits admin pour `Start-Service`.
- **Audit complet (2026-05-11)** : 32 fichiers de test, 0 TODO/FIXME, 110+ catch silencieux ÔåÆ 33 `console.error` ajout├®s. `prisma db push` maintenu (pas d'historique migrations en DB). `@wcore/ui` supprim├® (package vide). ROADMAP mis ├á jour v0.2.0-phase2. `useUnknownInCatchVariables: false` + `noImplicitAny: false` dans tsconfig.base.json pour le catch logging. BSC/Avalanche GM FACTORIES pr├¬tes, ChainCard off jusqu'au d├®ploiement on-chain.
- **reload Railway (2026-05-11)** : toujours v├®rifier `railway.json` et `railway status` (linked service) avant `railway up`. Le Dockerfile d├®ploy├® = celui dans railway.json. Si mal li├® ÔåÆ API Dockerfile sur web ou vice-versa ÔåÆ crash.
- **Token tolerant auth (2026-05-11)** : le token JWT ne doit ├¬tre effac├® QUE sur un 401 explicite (expir├®/invalide). Sur erreur r├®seau ou 500 API ÔåÆ garder le token et faire confiance au JWT expiry. Sans ├ºa, la moindre requ├¬te qui ├®choue d├®connecte l'utilisateur.
- **ETHG scam token non filtre (2026-05-09)** : ETHG (Ethereum Games) = 2M tokens ├ù 0.25EUR = 497kEUR gonfle le total. Cause : le token inconnu avec prix > 0 et valeur absurde n'etait pas detecte. Fix : regle #9 ajoutee dans `detectScam` core (`packages/core/src/tokens/scam-detector.ts`) ÔÇö token inconnu + valeur > 1000EUR + supply > 100k ÔåÆ scam poids 3. + regle #6 game token. `SCAM_RULES_VERSION` bump a 5. `detectScam` applique dans le backend (`apps/api/src/server.ts`) avant la notification de scan pour que la notif ne montre pas les montants gonfles.
- **TokenIcon spothq CDN cass├® (2026-05-09)** : `raw.githubusercontent.com/spothq/cryptocurrency-icons` retourne des SVGs qui affichent naturalWidth=0 pour de nombreux tokens (xdai, celo, arb, cro, frax, imx, etc.). Fix : overrides TrustWallet dans `TOKEN_LOGO_OVERRIDES` (`apps/web/components/TokenIcon.tsx`). TrustWallet utilise `xdai` pour Gnosis (pas `gnosis`).
- **llamao.fi chain icons ÔÇö slags renomm├®s (2026-05-10)** : `icons.llamao.fi/icons/chains/rsz_smartchain.jpg` retournait 404 ÔÇö DefiLlama a renomm├® BSC de `smartchain` ├á `bsc`. Toujours v├®rifier le slug dans `CHAIN_KEY_MAP` (icons.tsx) en curlant l'URL. Ajouter un fallback TrustWallet dans `FALLBACK_ICONS` pour les cha├«nes fragiles (pattern : OPTIMISM, ZERO, BSC).
- **Audit complet icons 404 (2026-05-10)** : 24/113 URL llamao.fi en 404. 10 slugs corrig├®s (BSC, AVALANCHE, ZERO, RARI, IMMUTABLE, FLOW, STABLE, TAC, ZKLINKNOVA, XRPLEVM, POLYGON_ZKEVM). 3 ajout├®s ├á FALLBACK_ICONS (BSC, ZKSYNC_ERA, GEB). 14 cha├«nes sans aucun logo ÔåÆ fallback emoji Ôøô´©Å. Ajout `onLoad` + `naturalWidth===0` detection dans ChainIcon et TokenIcon pour les images qui chargent mais restent vides (spothq/llamao).
- **ETHG scam / BTCB false positive (2026-05-10)** : BTCB (Bitcoin BEP2, BSC) ├®tait class├® scam par la r├¿gle #6 (nom contient "coin" + valeur > 10Ôé¼). Ajout├® ├á `KNOWN_TOKENS` + `SCAM_RULES_VERSION` bump ├á 6.

## Fonctions diagnostic utiles

- `WCORE_HEALTH()` ÔÇö Dashboard syst├¿me (Version, CacheVersion, Modules count, Cache Guard, Quota, Dynamic RPC)
- `WCORE_VERSION_CHECK()` ÔÇö V├®rification versions
- `GET_HTTP_BUDGET_STATS()` ÔÇö ├ëtat quota HTTP
- `GET_CACHE_GUARD_STATUS()` ÔÇö Protection cache
- `DegradedMode.getStatus()` ÔÇö Circuit breaker
- `GET_STORAGE_STATS()` ÔÇö Usage stockage 500KB
- `PURGE_CHAIN_PRICES(contracts, chainSlug, gtNetwork)` ÔÇö Purge prix d'un token de tous les caches
- `DIAG_*_NATIVE_PRICE()` ÔÇö Test pricing par cha├«ne
- `DIAG_*_RPC_STATUS()` ÔÇö Test RPC par cha├«ne
- `UPDATE_DYNAMIC_RPCS()` ÔÇö MAJ RPCs dynamiques (chainlist + Cosmos registry)
- `SHOW_DYNAMIC_RPCS()` ÔÇö Afficher RPCs dynamiques stock├®s
- `DYNAMIC_RPC_STATUS()` ÔÇö Statut du store (├óge, taille, compteurs)
- `CLEAR_DYNAMIC_RPCS()` ÔÇö Vider le store de RPCs dynamiques

## Gotchas WCORE Web (v0.1.18+)

- **Deploy RPC timeout** : toujours ajouter `AbortSignal.timeout()` sur les appels `eth_getTransactionReceipt` et `eth_getLogs`. Sans ├ºa, un RPC qui hang bloque le endpoint.
- **SIWE chainId bypass** : le check `siweChainId && storedChainId && siweChainId !== storedChainId` ├®tait bypass├® quand `storedChainId=0`. Utiliser `> 0` au lieu de truthy check.
- **JWT malformed = valid** : un token sans champ `exp` ├®tait trait├® comme valide. Toujours v├®rifier `!payload.exp || expired`.
- **JWT_SECRET change after API restart (2026-05-10)** : le `JWT_SECRET` g├®n├®r├® al├®atoirement par `deploy-staging.ps1` change ├á chaque restart ÔåÆ tous les tokens existants deviennent invalides ÔåÆ `not_authenticated`. **Fix** : persister un `JWT_SECRET` fixe dans `.env.staging`. `start-api.ps1 -Staging` et `deploy-staging.ps1` lisent le secret depuis `.env.staging` avant de fallback vers un random. Le frontend v├®rifie le token via `/api/auth/me` au montage et clear le token si invalide.
- **GM API before localStorage** : `useOnChainGm.sendGm` doit appeler l'API backend AVANT de set localStorage et dispatch l'event. Sinon GM perdus si API fail.
- **Admin NODE_ENV** : `isAdminAuthorized()` retournait `true` si `ADMIN_TOKEN=""` sans v├®rifier `NODE_ENV`. En prod, toujours exiger un token.
- **ETH price hardcoded** : ne jamais hardcoder un prix dans `/api/price/eth`. Toujours utiliser une cascade (DefiLlama ÔåÆ CoinGecko) et retourner `null` si tout ├®choue.
- **Circuit breaker events** : le passage HALF_OPENÔåÆCLOSED ne d├®clenchait pas d'event. V├®rifier `state !== "CLOSED"` pas juste `state === "OPEN"`.
- **Dispatcher unhealthy fallback** : ne jamais fallback vers des endpoints unhealthy. Le consensus strict avec `opts.total` g├¿re d├®j├á le cas o├╣ peu d'endpoints sont dispos.
- **ChainCard FACTORIES** : 3 copies doivent rester synchro ÔÇö `packages/shared/src/factories.ts` (source backend), `apps/web/hooks/useOnChainGm.ts` (chainId lookup), `apps/web/components/ChainCard.tsx` (gate affichage bouton). Si le backend n'a que `base`, le frontend ne doit pas afficher `arbitrum_one`, `optimism`, `polygon`. Actuellement: `base` + `arbitrum_one`.
- **D├®ployer GM sur nouvelle cha├«ne (2026-05-10, mise ├á jour 2026-06-04)** : 1) D├®ployer `GmOnChain` (implementation) puis `GmFactory(address _platformOwner, address _implementation)` via `/dev/deploy` ou `scripts/deploy-gm-contract.mjs`. 2) Ajouter l'adresse factory dans `packages/shared/src/factories.ts` uniquement. 3) V├®rifier que les RPCs viennent des configs `@wcore/core` via `getRpcEndpoints()` / `getPrimaryRpcEndpoint()` ; ne pas ajouter de map RPC GM locale. Contrats d├®j├á d├®ploy├®s: Base (factory `0xe7cfd4...6ecb`, contract `0xea39...49ea`), Arbitrum One (factory `0x8de14dd8...80c2`, contract `0xe828...b955`).
- **Docker HEALTHCHECK** : utiliser `node -e "require('http').get(...)"` plut├┤t que `wget` (pas dispo dans Alpine par d├®faut).
- **CSV injection** : neutraliser aussi le pipe `|` en plus de `=`, `+`, `-`, `@`.
- **Scam-detector perf** : `Array.from(KNOWN_DOMAINS)` est O(n). Utiliser une boucle `for-of` directe sur le Set.
- **Scam-detector dust at fake price (v0.1.19)** : regle #9 ÔÇö token non-connu avec prix>0 mais valeur<0.01Ôé¼ ÔåÆ fake. Poids 3, niveau scam. D├®tecte les honeypots avec pool liquide bidon sur DexScreener. `Array.from(KNOWN_DOMAINS)` est O(n). Utiliser une boucle `for-of` directe sur le Set.
- **Amino key parsing** : les cl├®s Amino Cosmos ont un pr├®fixe de longueur variable (protobuf). Prendre les 33 derniers bytes, pas un offset fixe.
- **Memory cache negative TTL** : `ttlMs` n├®gatif cr├®e une entr├®e imm├®diatement expir├®e. Clamper ├á `> 0`.
- **@wcore/shared + Turbopack (v0.1.19)** : `package.json` pointe vers `./src/index.ts` (source), les imports utilisent `.js` (convention ESM). Turbopack ne r├®sout PAS `.js` ÔåÆ `.ts` dans les workspace packages m├¬me avec `transpilePackages`. **Ne pas importer `@wcore/shared` en runtime depuis le frontend web** ÔÇö utiliser des `import type` uniquement, ou dupliquer la constante inline. L'API (tsx) et Docker (dist patch├®) g├¿rent correctement la r├®solution.
- **PDF Export visible ├á l'├®cran (v0.1.19)** : `PdfExport` et `MultiWalletPdfExport` n'avaient PAS de `hidden`/`display:none` hors print ÔåÆ contenu PDF visible en bas de page. Ajouter `hidden print:block` sur le div racine.
- **ZERO Network RPCs (v0.1.19)** : `thirdweb.com/543210` bloque (`-32603`), `calderachain.xyz/http` down. RPCs fonctionnels : `rpc.zerion.io/v1/zero` et `zero.drpc.org`. Avec seulement 2 RPCs qui marchent sur 4, le consensus exclut les nulls (2/2 = OK).
- **Notifications read-all (v0.1.19)** : endpoint `PUT /api/notifications/read-all`. Dans le dropdown, le badge "XX unread" est un bouton cliquable ÔåÆ `markAllRead()`. Pas de bouton "Mark all read" s├®par├®.
- **Scan timeout (v0.1.19)** : 300 polls ├ù 3s frontend / 15min backend job TTL. Scan async >20 cha├«nes.
- **Async scan auth polling (post-v0.1.19)** : un job cr├®├® avec `Authorization` doit ├¬tre poll├® avec le m├¬me header sur `GET /api/scan/async/:jobId`. Sans ├ºa, le backend owner-check retourne 404 et le frontend peut boucler jusqu'au timeout avec une erreur `network` cach├®e. Apr├¿s restart API, les jobs en m├®moire sont perdus (`job_not_found`) : forcer un reload web avant de relancer.
- **Deep scan frontend (post-v0.1.19)** : `deepScan=true` doit toujours utiliser `/api/scan/async`, m├¬me pour une seule cha├«ne, car le sync fetch garde un timeout frontend 60s. Les cha├«nes sont batch├®es par groupes de 5 pour donner du progr├¿s progressif sans exploser le nombre de jobs.
- **Deep scan progress UI (post-v0.1.19)** : la progression globale doit ├¬tre pond├®r├®e en `chain checks` = somme des cha├«nes compatibles par wallet apr├¿s filtrage VM (EVM/SVM/Cosmos). Ne PAS utiliser `currentChainDone/currentChainTotal` comme pourcentage global : `6/110` sur le wallet courant peut n'├¬tre que `6/990` si 9 wallets EVM sont activ├®s. Encart compact recommand├® : `N/M chain checks`, `X/Y wallets`, `Current: Label ┬À VM ┬À A/B chains`.
- **Deep scan progress min 1% (post-v0.1.19)** : quand `overallChainDone > 0`, afficher au moins `1%` m├¬me si `Math.round(done/total*100)` donne 0. Sinon l'UI affiche `1/348 chain checks` avec `0%`, ce qui semble bloqu├®.
- **Header GM on-chain sync (post-v0.1.19)** : apr├¿s un GM valid├® via `ChainCard`, le Header doit griser `On-chain +25 pts` sans refresh. Ne pas d├®pendre uniquement de l'├®v├®nement `wcore-gm-done` (volatile pendant re-render) : relire `wc_gm_onchain_date` depuis localStorage au montage, sur event, au focus et ├á l'ouverture du menu Header. Le libell├® Header doit rester `On-chain +25 pts`, sans mention `gas fee`.
- **Unknown chain drift (post-v0.1.19)** : `validateChains()` doit catch `getChain()` et skip les cha├«nes inconnues si au moins une cha├«ne valide reste. Un drift frontend/core (ex: `CELESTIA`) ne doit pas faire ├®chouer tout le scan avec `invalid_chains`.
- **EVM discovery performance (post-v0.1.19)** : discovery deep garde `500_000` blocs mais chunk `eth_getLogs` par 10k et ex├®cute 5 chunks en parall├¿le. La d├®couverte de logs utilise le premier RPC fonctionnel plut├┤t qu'un consensus strict, car `eth_getLogs` est trop fragile/lent en multi-RPC.
- **EVM Multicall3 balances (post-v0.1.19)** : lire les balances ERC-20 d├®couvertes via Multicall3 (`0xcA11bde05977b3631167028862bE2a173976CA11`) avant fallback per-token. Exporter le helper depuis `packages/core/src/rpc/index.ts` pour ├®viter les imports profonds.
- **ZERO WBTC registry (post-v0.1.19)** : ZERO Network WBTC `0xf1f9e08a0818594fde4713ae0db1e46672ca960e` doit rester registry-backed avec `decimals=8` et prix `coingecko:wrapped-bitcoin`, car discovery RPC/log peut le manquer sur ZERO.
- **Cache navigateur portfolio d├®sactiv├® (post-v0.2.28, namespace v0.3.1)** : ne plus bumper `WALLET_SCAN_CACHE_VERSION` pour les r├®sultats portfolio. Le frontend ne persiste plus les scans wallet en `localStorage`; diagnostiquer d'abord l'API puis Redis `scan:result:*` et les caches engine.
- **Optimism icon fallback (post-v0.1.19)** : `icons.llamao.fi/icons/chains/rsz_optimism.jpg` r├®pond en `image/webp` et peut rendre une image cass├®e. Utiliser le fallback PNG TrustWallet `blockchains/optimism/info/logo.png`.
- **OP token icon fallback (post-v0.1.19)** : ne pas confondre l'ic├┤ne de cha├«ne `OPTIMISM` et le token `OP`. Playwright a montr├® que `OPTIMISM` chargeait bien (`naturalWidth=256`) mais que `OP` utilisait `spothq/.../op.svg` avec `naturalWidth=0`. Utiliser l'override PNG TrustWallet `blockchains/optimism/assets/0x4200000000000000000000000000000000000042/logo.png`.
- **Scam ETHG / game token inflated supply (post-v0.1.19)** : les tokens inconnus avec th├¿me game, supply massive et valeur ├®lev├®e doivent ├¬tre class├®s scam. Bump `SCAM_RULES_VERSION` quand une r├¿gle change pour invalider les r├®sultats cach├®s c├┤t├® client.
- **Solana token discovery (v0.1.19)** : `mainnet-beta.solana.com` supporte `getTokenAccountsByOwner`, `publicnode.com` le bloque. Mettre `api.mainnet-beta.solana.com` en premier dans les RPCs SVM. Ajouter `solana.drpc.org` en backup.
- **Rate limit + async polling (v0.1.19)** : `GET /api/scan/async/:jobId` exclu du rate limit scan. Seuls les POST `/api/scan` et `POST /api/scan/async` (cr├®ation de job) sont limit├®s.
- **LeaderboardPreview + Scans tab (v0.1.19)** : le leaderboard ne doit PAS appara├«tre dans l'onglet Scans du profil. Il est sur sa propre page `/leaderboard`. Le `LeaderboardPreview` n'est rendu que dans l'overview ou pas du tout. : endpoint `PUT /api/notifications/read-all`. Dans le dropdown, le badge "XX unread" est un bouton cliquable ÔåÆ `markAllRead()`. Pas de bouton "Mark all read" s├®par├®.
- **edit_file + CRLF (v0.1.19)** : sur Windows, les fichiers ont des `\r\n`. `edit_file` ├®choue silencieusement si le `oldValue` utilise `\n` au lieu de `\r\n`. Utiliser un script `.cjs` Node.js avec `fs.readFileSync` + `fs.writeFileSync` pour les fichiers avec CRLF.
- **Chrome CDP profile (post-v0.1.19)** : `scripts/chrome-cdp.js` doit utiliser le profil persistant `%USERPROFILE%\chrome-debug-profile`, pas `%TEMP%\chrome-debug-profile`, sinon Google/session/wallet semblent d├®connect├®s. Le CLI `node scripts/chrome-cdp.js start <url>` doit passer l'URL ├á `startChrome(url)`.
- **EVM balance fallbacks (post-v0.1.19)** : garder le chemin rapide Multicall3, mais ne jamais relire les fallbacks `balanceOf` en s├®quence. Quand Multicall3 miss/retourne `0x`, traiter les fallbacks en groupes parall├¿les born├®s (`BALANCE_FALLBACK_CONCURRENCY=10`). Mesure 2026-05-08 : `ARBITRUM_ONE` cached scan ~10.5s ÔåÆ ~6.5s, `balancesMs` ~9.1s ÔåÆ ~3.6s. Test de garde : `packages/core/src/engines/evm.test.ts` / `getEvmWalletAssets reads per-token balance fallbacks concurrently when multicall misses`.
- **View-only linked wallets (post-v0.1.19)** : `LinkedWallet.verificationStatus` vaut `UNSIGNED` ou `SIGNED`. `POST /api/wallets` avec `mode:"view_only"` cr├®e un wallet server-synced sans signature ; un POST ult├®rieur avec signature valide upgrade `UNSIGNED` ÔåÆ `SIGNED`. Si le site demande encore une signature apr├¿s ce changement, v├®rifier d'abord que la branche feature est merg├®e dans `master`, que Prisma client/DB sont synchronis├®s, et que les serveurs web/API ont ├®t├® red├®marr├®s.
- **Prisma generate EPERM Windows (post-v0.1.19)** : `prisma generate` peut ├®chouer avec `EPERM rename ... query_engine-windows.dll.node` si un process Node API/web tient le DLL Prisma. Stopper les listeners locaux (`3000`, `4000`) puis relancer `pnpm --filter @wcore/db db:generate`. Le message `Update available 6.19.3 -> 7.8.0` est seulement un warning de major upgrade ; ne pas upgrader Prisma dans un fix fonctionnel sans plan/tests d├®di├®s.
- **GT bulk pre-fetch + parallel cascade (post-v0.1.19)** : `GeckoTerminalPriceSource.batchTokenPrices` fetches all token prices in 1 HTTP call (comma-separated contracts). Results are pre-loaded into `sharedPriceCache` before the per-token cascade loop. `priceToken` uses `known.contract` (no chain prefix) as cache key ÔÇö the bulk pre-fetch must use the same format or the cache won't hit. In the cascade, `dex || llama-coins` run in parallel after llama-map miss. Fallback: if GT batch fails, per-token GT runs as before.
- **Discovery range + registry (post-v0.1.19)** : `eth_getLogs` discovery limit├® ├á `DEEP_LOG_SCAN_BLOCKS` (50k) blocs. Sur BSC (3s/bloc), ca couvre ~42h. Tokens acquis avant sont invisibles sans registry. Toujours ajouter tokens connus dans `TOKEN_REGISTRY`. Status `DEGRADED` + `explorer HTTP 404 for BSC` normal pour chaines sans Blockscout.
- **RealT spam filter (post-v0.1.19)** : `isLikelySpam` dans explorer-discovery.ts filtre `symbol.length > 20`. Les tokens RealT (Gnosis) ont des symboles de 40-60 caract├¿res et ├®taient syst├®matiquement filtr├®s. Exception `REALTOKEN-` ajout├®e. Les prix RealT viennent de `api.realtoken.community` (ni CoinGecko, ni DefiLlama, ni DexScreener ne les listent).
- **RealT vs GT cache (post-v0.1.19)** : Sur Gnosis, le GT bulk pre-fetch doit ├¬tre skip (`skipBulkGt = chain.key === "GNOSIS"`) car GT retourne des prix de pools illiquides qui ├®crasent les vrais prix RealT dans le cache. La source `realt` est prioritaire dans la cascade (juste apr├¿s stablecoins).
- **Incremental discovery cursor (post-v0.1.19, fix├® `0ec54fe`)** : Redis stocke `disc:{wallet}:{chain}:block` pour le `lastScannedBlock`. Au rescan, seul le delta (`lastScannedBlock+1` ÔåÆ bloc courant) est scann├® via `eth_getLogs`, puis merge avec les tokens cach├®s. **Bug initial** (`a2151e9`) : `cache.delete(cacheKey)` + `cache.delete(:block)` ├®tait fait AVANT discovery ÔåÆ si une ├®tape RPC ├®chouait, curseur perdu. Le bypass cache discovery ├®tait conditionn├® ├á `usedIncremental` au lieu de `hasCachedDiscovery`, et un second `dispatcher.run(blockNumber)` ├®tait fait ├á la fin pour r├®├®crire le curseur. R├®sultat : warm rescan **plus lent** que cold (172s vs 125s, ratio 1.38├ù pour `0x17d51ÔÇªD080` 110 cha├«nes). **Fix harmonis├®** : ne plus rien delete pr├®emptivement, ├®crire atomiquement ├á la fin avec le `toBlock` d├®j├á fetch├® par `getRecentLogRange`, bypass cache d├¿s qu'on a des tokens cach├®s (incr├®mental ou non), merge cache toujours. Bench post-fix : warm 74s vs cold 93s (gain ÔêÆ21%). ZERO 8.5s ÔåÆ 1.7s sur rescan (ÔêÆ80%).
- **Frontend scan cache stale (historique)** : l'ancien cache `wcore_scan_v3:*` n'est plus utilis├® pour les r├®sultats portfolio. Si un r├®sultat stale persiste, v├®rifier Redis/API plut├┤t que localStorage.
- **Scan concurrency v0.2.10 (commit `1cc0e75` ÔåÆ v0.2.10)** : deux param├¿tres ind├®pendants gouvernent la concurrence ÔÇö `SCAN_CONCURRENCY` c├┤t├® API (`apps/api/src/plugins/scan.ts`, d├®faut **50**) et `BATCH_SIZE` c├┤t├® frontend (`apps/web/components/WalletContent.tsx`, **50** = 1 job async par batch de 50 cha├«nes). Bench historique : c=20 cold 34s/warm 28s, c=50 cold 20s/warm 17s (CV ~24 %, ~60 errChains stable), c=120 cold 14s/warm 12s (60ÔÇô108 errChains, variable), Ôê× cold 18.5s/warm 13.8s (**101ÔÇô110 errChains** ÔÇö quasi toutes les cha├«nes en `[DEGRADED]`). **Trade-off explicite** : 50 = bon ├®quilibre vitesse/qualit├®. >50 sature les RPCs free-tier ÔåÆ consensus failures. Override prod via `SCAN_CONCURRENCY` (ou autre entier). Endroits ├á modifier ensemble : `scan.ts`, `.env.example`, `.env.staging`, `scripts/deploy-staging.ps1`, `apps/web/components/WalletContent.tsx`.
- **RSC page split pattern (v0.1.20)** : Next.js 16+ exige la s├®paration server/client components. Les pages avec hooks (useState, useEffect, useWallet, etc.) **ne peuvent pas** rester en `"use client"` au niveau page si le layout est d├®j├á client-side-heavy. Pattern : `page.tsx` = server component (importe layout/header/footer + `XxxClient`), `XxxClient.tsx` = `"use client"` avec toute la logique interactive. Appliqu├® ├á : home (`HomePageClient`), history (`ScansClient`), pricing (`PricingClient`), profile (`ProfileClient`), stats, leaderboard. Le `ConnectButton` et les autres widgets client (GmButton, NotificationsBell, SettingsBar) restent dans le server component ÔÇö ils g├¿rent leur propre hydratation via `!mounted ? null`.
- **RSC conversion complete (v0.1.20, commits `6c8aee5` ÔåÆ `0971cc6`)** : toutes les pages top-level converties au pattern server-shell + `XxxClient`. Liste compl├¿te : home, history, pricing, profile, stats, leaderboard, share/[token], dev/deploy, creator, scans/[id], support, admin. Pages avec params dynamiques (`scans/[id]`, `share/[token]`) re├ºoivent les params dans le server component (`async function ... { const { id } = await params }`) et les passent en prop au client. Build : ~826 lignes retir├®es du payload server total.
- **CircuitBreaker decay (post-v0.1.20, commit `c16230f`)** : `CircuitBreaker` gagne un 4e arg constructeur `decayMs` (d├®faut **600_000 = 10 min**). `failureCount` se reset ├á 0 si aucun nouvel ├®chec depuis `decayMs`. Triggered par `allowRequest()`, `onFailure()`, `getStatus()` via `maybeDecay()`. Sans ce decay, les cha├«nes long-tail accumulaient `failureCount` ind├®finiment quand elles scannaient des wallets vides avec RPC flaky (`hasError && !hasValue` ÔåÆ `onFailure`, mais jamais `!hasError` ÔåÆ pas de `onSuccess`). Threshold prod = 20, donc 13/20 sur ~30 cha├«nes apr├¿s 7h n'├®tait pas un bug mais une accumulation pure. Pour activer le decay, restart l'API.
- **Chain configs `.ts` source-of-truth (post-v0.1.20)** : `packages/core/src/chains/*.ts` ont un header `Auto-generated from src/*.gs by tools/migrate/extract-chains.mjs / Do not edit by hand`. **Sur ce repo (`wcore-web`), les `.gs` n'existent pas** ÔÇö ils vivent en amont dans `wcore-gsheet` (Apps Script). Les `.ts` sont donc ├®ditables ici directement ; toute correction doit aussi ├¬tre port├®e upstream pour ne pas ├¬tre ├®cras├®e ├á la prochaine extraction. Ne PAS lancer `tools/migrate/extract-chains.mjs` depuis `wcore-web` sans avoir d'abord les `.gs`.
- **SOMNIA chainId typo (post-v0.1.20, commit `e6f4811`)** : `CHAIN_ID: 50311` ├®tait faux. L'on-chain `eth_chainId` renvoie `0x13a7 = 5031` sur tous les RPCs (publicnode, infra officielle, mirror thirdweb `5031.rpc.thirdweb.com`). Sympt├┤me silencieux : chaque scan Somnia ratait le consensus chainId. V├®rifier les chainIds via `mcp__rpc-mcp__rpc_validate_endpoint` quand on ajoute/├®dite une cha├«ne, surtout si le RPC mirror est nomm├® `<chainId>.rpc.thirdweb.com` (le nom doit matcher la config).
- **Dead RPCs POLYNOMIAL/ZIRCUIT (post-v0.1.20, commit `299711e`)** : `rpc.polynomial.fi` et `rpc-proxy.polynomial.fi` sont morts (`no backends available` / 503). Switch vers `polynomial.rpc.thirdweb.com` + `8008.rpc.thirdweb.com`. Sur ZIRCUIT, `zircuit1-mainnet.p2pify.com` fetch fail et `zircuit.drpc.org` gate `eth_chainId` derri├¿re paid tier ÔÇö garder uniquement `mainnet.zircuit.com`. Audit large via `mcp__rpc-mcp__rpc_validate_endpoint` recommand├® apr├¿s plusieurs jours d'uptime quand `failureCount` monte sur des cha├«nes long-tail.
- **API tsconfig NodeNext vs bundler (2026-05-09, commit `0eec387`)** : `apps/api/tsconfig.json` utilisait `module: "NodeNext"` + `moduleResolution: "NodeNext"` alors que `@wcore/shared` utilise des bare specifiers (`./address` sans `.js`). R├®sultat : 20 erreurs TS2835/TS2305 sur toutes les imports depuis `@wcore/shared`. **Fix** : h├®riter `moduleResolution: "bundler"` du `tsconfig.base.json` (supprimer les overrides `module` et `moduleResolution`). L'API tourne avec `tsx` en runtime, donc `bundler` est correct. Garder `verbatimModuleSyntax: false`.
- **calderachain.xyz RPCs all dead (2026-05-09)** : les RPCs calderachain.xyz sont morts sur TOUTES les cha├«nes. Retir├®s de ZERO, INTUITION, RARI. INTUITION passe de 2ÔåÆ1 RPC (quasi single-RPC). Tout nouveau RPC `calderachain.xyz` doit ├¬tre v├®rifi├® avant inclusion. Pattern : `*.calderachain.xyz/http` = suspect.
- **INTUITION single-RPC (2026-05-09)** : apr├¿s retrait du calderachain.xyz mort, INTUITION n'a plus qu'1 RPC (`rpc.intuition.systems`). 10 autres cha├«nes sont aussi single-RPC : ANCIENT8, B3, CITREA, FOGO, MITOSIS, OPENLEDGER, STABLE, TAC, TEMPO, VANA, ZIRCUIT. Surveiller la sant├® de leur RPC unique ÔÇö si down, la cha├«ne est enti├¿rement cass├®e.
- **ESLint 10 monorepo flat config (2026-05-09, commit `0eec387`)** : config dans `eslint.config.mjs` ├á la racine. Utilise `tseslint.config()`, `globals` (Node + browser), `eslint-plugin-react-hooks` v7. Fichiers `.mcp/**` exclus. Scripts Node.js (`scripts/`, `apps/api/set-test-env.js`) avec `globals.node`. `eslint-plugin-react-hooks` appliqu├® uniquement sur `apps/web/**`. R├¿gle `react-hooks/set-state-in-effect` d├®sactiv├®e (trop stricte, flag des patterns l├®gitimes de fetch/init).
- **`_` prefix convention + typescript-eslint (2026-05-09)** : la r├¿gle `@typescript-eslint/no-unused-vars` est configur├®e avec `varsIgnorePattern: "^_"`, `argsIgnorePattern: "^_"`, `caughtErrorsIgnorePattern: "^_"`. Convention : pr├®fixer les variables non utilis├®es avec `_`. Si d├®j├á pr├®fix├® et toujours flagg├® ÔåÆ double pr├®fixe `__` (rare, ex: `_serialize ÔåÆ __serialize`).
- **react-hooks/refs WalletContent (2026-05-09)** : `prevResultsRef.current` est acc├®d├® pendant le render pour merger les r├®sultats de scan. Le plugin `react-hooks/refs` le flagge. Solution : bloc `/* eslint-disable react-hooks/refs */ ... /* eslint-enable react-hooks/refs */` autour du merge.
- **`.env.staging` retir├® de git (2026-05-09)** : `.env.staging` ├®tait commit├® dans l'historique via une exception `!.env.staging` dans `.gitignore`. Contenait `DB_PASSWORD` et `JWT_SECRET` (valeurs placeholder, mais exposition inutile). **Fix** : retirer l'exception du `.gitignore`, `git rm --cached .env.staging`. Le fichier reste localement mais n'est plus track├®.
- **pnpm audit ÔÇö 0 vuln (2026-05-09)** : `pnpm audit --json` confirme 0 vuln├®rabilit├® (404 d├®pendances). Les overrides `postcss`, `tmp`, `fast-uri` dans `package.json` couvrent les CVE connues.
- **API crash after session crash (2026-05-10)** : l'API tourne en process Node.js fils de la session. Quand la session crash/timeout, le process API meurt avec ÔåÆ erreur "network" dans le frontend. Relancer avec `.\scripts\start-api.ps1 -Staging -Detached` (port 4001, fond) ou `.\scripts\start-api.ps1` (port 4000, foreground). Le script kill l'ancien process sur le port cible avant de relancer. V├®rifier aussi le web Next.js (port 3000 dev / 3001 staging) qui peut ├¬tre mort.
- **Custom tokens scoped par user (2026-05-13)** : bouton "+ Token" dans chaque ChainCard. `POST /api/custom-tokens` persist├® en DB par `userId`. Les scans sync et async utilisent `resolveCustomTokens(userId, body.customTokens)` dans `apps/api/src/server.ts` : merge tokens requ├¬te + tokens DB du m├¬me user, `MAX_CUSTOM_TOKENS=100` appliqu├® apr├¿s merge. Ne pas revenir ├á un `findMany()` global sans filtre userId, sinon DoS RPC/pricing multi-user.
- **Admin scam report temps r├®el (2026-05-10)** : bouton ­ƒÜ®/Ô£à par asset dans TokenTable. Si admin (adresse = `PLATFORM_OWNER`) ÔåÆ override imm├®diat (pas de ticket). Sinon ÔåÆ ticket support automatique. Persistance 3 niveaux : localStorage (`wcore_scam_approved/blocked` ÔåÆ survit refresh), DB (`scam_overrides` table ÔåÆ survit restart API), m├®moire (`addAdminApproved/Blocked` ÔåÆ effet instantan├®). `scam-detector.ts` : `isKnownToken()` wrapper avec `_adminApproved`/`_adminBlocked` Sets + fallback `_KNOWN_TOKENS`. R├®activit├® cross-component via event `wcore-scam-override` ÔåÆ ChainCard recalcule `cleanTotal`, WalletContent recalcule `displayResultsAdjusted` ÔåÆ totaux par cha├«ne et globaux mis ├á jour instantan├®ment.
- **ConnectButton wagmi auto-connect NOP (v0.2.12, fix 2026-05-18)** : ligne 67 `if (authStep === "idle") setAuthStep("idle")` ÔÇö une NOP qui emp├¬chait wagmi auto-connect (MetaMask eager-connect) de passer `authStep` ├á `"authenticated"`. R├®sultat : wallet connect├® mais UI affichait "Connect Wallet". **Fix** : nouvel ├®tat `"ready"` ÔÇö affiche l'adresse wallet avec un bouton "Sign In", mais NE traite PAS comme authentifi├® (pas de JWT). Pages GM/Profile v├®rifient `authStep === "authenticated"`, pas juste `address`. + 3 bugs de rehydration : erreurs API non-200/non-401 trait├®es comme authentifi├®, erreur r├®seau trait├®e comme authentifi├®, adresse localStorage utilis├®e au lieu de l'adresse v├®rifi├®e par le serveur JWT. Fichiers : `ConnectButton.tsx`, `GmPageClient.tsx`, `ProfileClient.tsx`.
- **Scam override contract propagation (v0.2.12, fix 2026-05-18)** : 3 failles faisaient que le blocage admin par contrat ne survivait pas au refresh. **(1)** Le bouton admin TokenTable n'envoyait pas `contract` ├á `/api/admin/scam-override` ÔåÆ le serveur recevait `addAdminBlocked(symbol, undefined)` ÔåÆ aucun contrat dans `_adminBlockedContracts`. **(2)** Le `upsert` DB ├®crasait `contract` avec `null` quand absent de la requ├¬te ÔåÆ m├¬me la migration d'insertion avec contrat ├®tait annul├®e. **(3)** Aucune synchro APIÔåÆfrontend : le frontend ne lisait que `localStorage`, jamais les overrides serveur. **Fix** : bouton envoie `contract`, upsert ne touche pas `contract` si `null`, endpoint `GET /api/admin/scam-overrides` + hook `useScamOverrideSync` qui synchro localStorage au montage. Fichiers : `TokenTable.tsx`, `admin.ts`, `scam-detector.ts`, `WalletContent.tsx`.
- **Scan TTL tuning (2026-05-10)** : `DISCOVERY_CACHE_TTL_MS` pass├® de 15min ├á **24h** pour que le curseur incr├®mental (`disc:{addr}:{chain}:block` dans Redis) survive entre les visites. Frontend `CACHE_TTL_MS` pass├® de 30min ├á **2h**. Sans ces TTL longs, chaque visite refait un scan complet ÔåÆ lent.
- **Page heading server-component pattern (2026-05-12)** : toujours mettre le `<h1>` dans le `page.tsx` server component (comme About et Leaderboard). Le `XxxClient.tsx` ne rend que le contenu interactif. Ne JAMAIS wrapper un client component dans `flex flex-wrap` ÔÇö ├ºa casse le layout vertical. Exemple corrig├® : History page qui avait son `<h1>` dans ScansClient + wrapper flex.
- **SidebarLayout pattern (2026-05-12)** : quand la sidebar et le contenu principal partagent un ├®tat (ex: collapsed), cr├®er un `SidebarLayout.tsx` client component qui wrappe les deux. Il g├¿re le state + localStorage + la marge dynamique (`ml-[56px]` vs `ml-[200px]`). Le `layout.tsx` server component reste clean. La sidebar est r├®ductible via bouton `ÔùÇ` en bas + raccourci `Ctrl+[`.
- **Mobile UI Phase 1 (2026-05-18)** : pr├®server le desktop via `sm:`. Sur mobile, `SidebarLayout` utilise `ml-0` + drawer overlay; desktop garde `sm:ml-[56px]` / `sm:ml-[200px]`. `TopBar` mobile ne montre que hamburger + titre/WCORE + menu `...`; le `ConnectButton` reste dans le menu pour ├®viter l'overflow. Tap targets mobiles minimum `h-11 w-11`. `TokenTable` masque uniquement le texte secondaire (`asset.name` + contract) en `hidden sm:block`, jamais les colonnes valeur/balance/prix.
- **@WCORExyz display case (2026-05-12)** : le texte affich├® utilise `@WCORExyz` (majuscules), les URLs X/Twitter (`x.com/wcorexyz`, `screen_name=wcorexyz`) restent en minuscules. Coh├®rence sur toutes les pages (layout footer, about, leaderboard, profile, WelcomeModal, referral prompts).
- **Em-dashes ban dans le contenu (2026-05-12)** : ne pas utiliser `ÔÇö` (em-dash) comme s├®parateur dans le texte visible. Remplacer par un point suivi d'une majuscule. Ex: `"On-chain GM ÔÇö Say GM..."` ÔåÆ `"On-chain GM. Say GM..."`. Les `"ÔÇö"` en tant qu'indicateur "no data" dans les tableaux sont accept├®s.
- **Layout responsive fluide data pages (2026-05-12)** : les pages data (WalletContent, History, Leaderboard, Profile, Stats, Scans/[id]) utilisent `w-full` sans max-width pour remplir l'├®cran. Les pages lecture/form (About, Support, Pricing, Share, Home, GM, Admin, Dev, Creator) gardent `max-w-5xl` ou `max-w-4xl` pour la lisibilit├®. Ne PAS remettre de `max-w-5xl` sur les pages data sans v├®rifier avec l'utilisateur.
- **Layout full-width desktop-first (2026-05-12, v2)** : TOUTES les pages sont pass├®es en `w-full` (desktop-first, mobile plus tard). Plus aucun `max-w-*` sur les `<main>`. Les pages lecture (About) utilisent des grilles 2-col pour occuper la largeur. Le home scan form est compact avec options avanc├®es repliables. Le ChainSelector est une liste unique fusionn├®e (plus de split "defaults / other"). Les boutons "Select default / Select all" sont des pills. L'ordre du form home : adresse + Scan ÔåÆ chains ÔåÆ wallets ÔåÆ deep scan.
- **ValueDistribution currency (2026-05-12)** : le composant `ValueDistribution.tsx` utilisait un `formatCompact` maison qui hardcodait `Ôé¼`. Remplac├® par `formatValue` de `usePreferences` qui respecte la devise s├®lectionn├®e.
- **Gnosis RealT pricing (2026-05-12)** : la source RealT (`api.realtoken.community/v1/token/`) est prioritaire dans la cascade pour Gnosis. Mais le cache `sharedPriceCache` servait des prix GT/DexScreener p├®rim├®s pendant 1h sans rappeler RealT. Fix : `skipCache: true` pour Gnosis dans `packages/core/src/engines/evm.ts` ÔåÆ priceTokenCascade. V├®rifier les prix via `curl api.realtoken.community/v1/token/<contract>`.
- **ChainSelector unifi├® (2026-05-12)** : plus de section "All other chains (101)" s├®par├®e. Une seule liste avec recherche globale. Si tu cherches "Ethereum" il appara├«t m├¬me s'il est dans les defaults. Grille responsive `grid-cols-2` ÔåÆ `xl:grid-cols-6`.
- **Scan toast sans prix (2026-05-12)** : la notif "Scan complete" n'affiche plus le total EUR car il incluait les tokens scam (­ƒÜ®) non filtr├®s, cr├®ant un ├®cart avec le total affich├®. Affiche seulement `N chains ┬À M tokens`.
- **Profile/Support unauthenticated (2026-05-12)** : les ├®crans non-connect├®s de Profile et Support ne sont plus centr├®s (`text-center`, `items-center justify-center`) mais align├®s ├á gauche comme le reste du site.
- **X brand assets (2026-05-12)** : `wcore-avatar.svg/.png` doit utiliser le logo exact de `apps/web/components/Logo.tsx` (hexagone + n┼ôuds + connexions). `wcore-banner.svg/.png` (1500x500) a le logo ├á droite (x=1156), texte ├á gauche (x=350), footer ├á droite (x=1300). Ne jamais utiliser d'approximation du logo. Les posts X (wcore-post-1/2/3) sont en 1200x675 avec badge WCORE utilisant l'avatar.
- **X banner avatar overlay (2026-05-14)** : le banner X doit rester lisible avec l'avatar de profil qui recouvre le bas gauche. Garder la zone inf├®rieure gauche sans texte/logo critique. Banner final : base proche de `banner-old` avec encart logo WCORE mis en avant ├á droite, logos de cha├«nes v├®rifi├®s en arri├¿re-plan, sans cercle noir externe autour des logos. Ne pas ajouter de logos dont l'URL retourne 404. Ne pas dupliquer les logos de cha├«nes. Ne pas cacher de logos derri├¿re le texte, les pills, l'encart WCORE ou `wcore.xyz`. `wcore.xyz` et `@WCORExyz` restent centr├®s sous l'encart logo WCORE de droite.
- **X product screenshot posts (2026-05-13)** : pour un screenshot produit r├®el (ex: `apps/web/public/pour X.png`), conserver l'original intact et g├®n├®rer une copie post-ready (`pour-X-post.png`). La DA doit rester proche des `wcore-post-*` : badge WCORE compact, avatar officiel `wcore-avatar.png` (jamais logo redessin├®), `wcore.xyz` en texte vert WCORE discret pr├¿s du bas, pas de gros encarts/watermarks qui masquent le rendu produit. Objectif du post publi├® : montrer un aper├ºu multi-wallet/multi-chain; le d├®tail par cha├«ne existe dans le produit mais n'est pas n├®cessairement montr├® dans l'image.
- **Railway deploy (2026-05-12)** : `railway.json` contr├┤le quel Dockerfile est utilis├®. **Ne jamais faire** `edit railway.json` + `railway up` manuellement ÔÇö utilise `scripts/deploy.ps1 -Service api` ou `-Service web`. Ce script change le dockerfilePath temporairement, lance le deploy, et restaure le fichier. Cause fr├®quente de l'erreur "API deploy = web Dockerfile". **Ne jamais lancer deux `deploy.ps1` en parall├¿le** (ex: API + Web simultan├®ment) ÔÇö ils se battent sur `railway.json`, le dernier qui ├®crit ├®crase l'autre ÔåÆ un des deux services d├®marre avec le mauvais Dockerfile (v4.15.36). **Incident 2026-05-19** : API down (servait du HTML Next.js). Cause = race condition. Fix = `railway up --service api`. V├®rifier `Content-Type: application/json` apr├¿s chaque d├®ploiement API.
- **API Dockerfile chown timeout (2026-06-04)** : ne pas utiliser `RUN chown -R node:node /app` dans le runner stage. Quand le cache Docker est absent, cela parcourt tout `/app` incluant `node_modules` et peut timeout Railway (~15 min). Utiliser `COPY --chown=node:node ...` sur chaque `COPY` du runner stage ├á la place.
- **API Dockerfile import rewrite `$1` (2026-06-19)** : dans `apps/api/Dockerfile.railway`, le rewrite post-build ESM utilise un `RUN node -e "..."` ex├®cut├® par `/bin/sh`. Le remplacement doit ├¬tre `from "./\$1.js"`, pas `from "./$1.js"`. Sinon le shell expand `$1` en vide pendant le build Docker et `packages/shared/dist/index.js` contient `export * from "./.js"`, crash runtime Railway `ERR_MODULE_NOT_FOUND: /app/packages/shared/dist/.js`. V├®rifier avec `docker build --target builder -f wcore-web/apps/api/Dockerfile.railway ...` puis inspecter `/app/packages/shared/dist/index.js`.
- **GitHub autodeploy Railway multi-service (2026-06-19)** : `web` peut ├¬tre connect├® ├á `Icesenator/WCORE@master` car le `railway.json` committ├® pointe vers le Dockerfile web. Ne pas connecter `api` ├á GitHub tant que Railway n'a pas une config service-level ou config-file s├®par├®e pour `wcore-web/apps/api/Dockerfile.railway`; sinon un push peut builder le web pour le service API.
- **GM Moonbeam/Moonriver status-onchain (2026-06-04)** : les deux cha├«nes ont `RPC.MAX_LOG_RANGE=1024`. `/api/gm/status-onchain` doit chunker `eth_getLogs` avec `getChainMaxLogRange()` comme `fetchOnChainContracts`; sinon lÔÇÖendpoint officiel Moonriver retourne `-32603 block range is too wide` sur 10k blocs et lÔÇÖUI reste `Say GM` malgr├® lÔÇÖevent `GmCheckedIn` confirm├®. Tests de garde : `apps/api/src/gamification/gm-onchain-status.test.ts`.
- **Audit courant** : `docs/AUDIT.md`, vérifié le 2026-07-10. L'audit transversal Web + GSheet est `../docs/AUDIT.md`. Ne pas piloter le backlog depuis les anciens rapports datés ou les sections historiques de la roadmap.
- **GM status-onchain s├®curit├® (audit 2026-06-05)** : endpoint utile pour la r├®conciliation, mais ├á durcir : exiger auth, valider `EvmAddress`, limiter ├á `address === req.user.address` sauf admin, cache court `(chain,address,date)`. Sans ├ºa, un anonyme peut amplifier les appels RPC via contract enumeration + `eth_getLogs`.
- **deploy.ps1 exit code (audit 2026-06-05)** : apr├¿s `railway up --service`, capturer `$LASTEXITCODE` avant le `finally`, restaurer `railway.json`, puis propager le code. Sinon un ├®chec Railway peut ├¬tre masqu├® par la restauration r├®ussie.
- **TokenIcon overrides (2026-05-12)** : les logos spothq (SVG) ont `naturalWidth=0` pour de nombreux tokens (glmr, movr, mnt, etc.). Ajouter les tokens natifs de cha├«nes dans `TOKEN_LOGO_OVERRIDES` (TokenIcon.tsx) avec les URLs TrustWallet `blockchains/<slug>/info/logo.png`. V├®rifier la dispo avec un `HEAD` HTTP avant d'ajouter.
- **TokenIcon overrides + fallback visible (2026-05-13)** : plusieurs SVG spothq restent cass├®s (`naturalWidth=0`) pour MITO, HYPE, OKB, OPEN, BTCN, METIS, FLR, ASTR, etc. Solution canonique : d'abord ajouter des overrides v├®rifi├®s (`HEAD 200`) vers CoinGecko/CoinMarketCap/llamao quand le logo existe, puis garder un fallback texte/color├® derri├¿re l'image pour les vrais long-tail sans logo. Tests de garde : `apps/web/__tests__/token-icons.test.ts` v├®rifie les overrides MITO/HYPE/OKB/METIS/FLR/ASTR/OPEN/BTCN/PLUME/KAIA/BERA/VANA/GHO/G et le fallback `ZZZZ`.
- **@WCORExyz convention (2026-05-12)** : toujours utiliser `@WCORExyz` en majuscules dans le texte affich├®. Les URLs restent en minuscules (`x.com/wcorexyz`). Cette convention s'applique aux posts X, au CM strategy, et ├á toutes les pages.
- **X daily update post layout (2026-05-14/15)** : pour `wcore-post-daily-update-*`, conserver le format 1200x675 et la DA du post original. Titre compact en haut ├á droite pour lib├®rer l'espace. Composition valid├®e : grande carte gauche, carte GM en haut ├á droite, carte polish/reliability en bas ├á droite. Pour une update GM, la carte gauche doit rester user-facing (`GM streaks simplified`, `Off-chain`, `On-chain`) et ├®viter les formules trop denses. Ne pas mentionner platform fees dans les visuels publics. Pour `New GM chains`, traitement final valid├® : pas de pills, groupe logo + label centr├® dans l'encart. Reprendre le pattern des anciens SVG pour les logos (cercle blanc + image clipp├®e + label), sans transform/scale exotique. **Daily update v7** : `wcore-post-daily-update-7.svg/.png` met en avant `180+ tracked chains` (130 ÔåÆ 180+) avec anneau de 8 logos locaux dans la grande carte gauche. Ne pas utiliser de wording public `API restored` ni `Safer admin`; pr├®f├®rer `More chains. Cleaner scans. Better pricing.`.
- **Daily update v9 (2026-05-23)** : `https://x.com/WCORExyz/status/2058219512185434210`. Image : `apps/web/public/wcore-post-daily-update-9.svg` + `.png` (`1200x675`). Angle user-facing : `Cleaner scans. Better coverage.` Mettre en avant RealT/Gnosis, Solana+Cosmos fallback, 180+ chain mapping, read-only/no wallet connect. Grande carte gauche valid├®e : mini scan summary, cercle/check recentr├®, badges `RealT`, `SVM`, `Cosmos`, headline `Cleaner scans`. Ne pas mettre `Safer X ops`, `API restored`, incident, ni wording interne.
- **Daily update v10 (2026-06-04)** : `https://x.com/WCORExyz/status/2062634948259885209`. Image : `apps/web/public/wcore-post-daily-update-10.svg` + `.png` (`1200x675`). Angle user-facing : Moonbeam + Moonriver GM live, Cleaner GM status, Read-only map. Utiliser `170+ live` (174 enabled / 182 registry), pas `180+ chains`, pour ├®viter une promesse sup├®rieure aux cha├«nes actives.
- **Tower post v13 (2026-06-06)** : `https://x.com/WCORExyz/status/2063348147825397977`. PAS un "Today's WCORE update" ÔÇö post standalone, format structur├® avec sauts de ligne. Texte publi├® : `The tower. / 6 layers. / 182 chains. / 1 truth. / Read first. Act later. / wcore.xyz`. Image : `apps/web/public/wcore-post-tower.svg` + `.png` (`1200x675`). DA v12 (gradient diagonal + 2 splines + 2 glows lime + grid). Layout 2 colonnes : tour pyramidale gauche (6 couches avec label + sous-titre + accent color├®), 6 cards num├®rot├®es droite (01-06 CHAINSÔåÆTRUTH dans le m├¬me ordre que la tour). Badge WCORE ajout├® en haut ├á droite (ajout 2026-06-06 post-publication, pr├®sent d├®sormais sur tous les posts concept). Pattern r├®utilisable pour les posts "concept" WCORE qui ne sont pas des daily updates.
- **GM footprint post v14 (2026-06-06)** : `https://x.com/WCORExyz/status/2063357122423779695`. Post engagement (PAS un daily update). Texte publi├® : `Your GM footprint is growing. / Personal GM contracts. / Creator fees. / Cross-chain streaks. / More chains are rolling in. / Which chain do you want next? / wcore.xyz`. Image : `apps/web/public/wcore-post-gm-footprint.svg` + `.png` (`1200x675`), g├®n├®r├®e par `scripts/build-post-gm-footprint.cjs`. DA v12. Grille 7├ù8 (56 cases) : 55 cha├«nes GM live + 1 case `More soon` (cercle pointill├® lime + 3 points) au dernier emplacement pour signifier "ce n'est que le d├®but". **S├®lection des cha├«nes** : le script lit `GM_FACTORIES` (`packages/shared/src/factories.ts`) + configs core (`packages/core/src/chains/*.ts`), exclut `FLAGS.DISABLE_CHAIN=true` ET une liste `MARKETING_EXCLUDED_DEAD` (cha├«nes dont aucun RPC ne r├®pond ├á `eth_blockNumber` en 5s). Redstone exclue (2 RPCs morts : `rpc.redstonechain.com` fetch fail, `690.rpc.thirdweb.com` r├®pond `eth_chainId` mais pas `eth_blockNumber`). **Gotcha** : ne pas figer le compte ("56" final) ÔÇö toujours dire `N live now` + case `More soon`, car les cha├«nes GM arrivent progressivement. Logos en double cercle (noir externe + blanc fin), 3 pills harmonis├®es `PERSONAL CONTRACTS`/`CREATOR FEES`/`CHAIN STREAKS`, badge WCORE haut droite, question engagement `Which chain do you want next?`. Caract├¿re `┬À┬À┬À` (U+00B7) cass├® en `├é┬À├é┬À├é┬À` ├á l'encodage ÔåÆ utiliser 3 `<circle>` SVG ├á la place.
- **Scam flags post v16 (2026-06-12)** : `https://x.com/WCORExyz/status/2065305153444405747`. Post concept (PAS un daily update). Texte publi├® : `Your total is lying. / Airdropped scam tokens show up with fake prices and absurd supplies. / Most trackers count them. Your portfolio looks rich. It is not. / WCORE flags them and keeps them out of your clean total. / wcore.xyz`. Image : `apps/web/public/wcore-post-scam-flags.svg` + `.png` (`1200x675`), g├®n├®r├®e par `scripts/build-post-scam-flags.cjs`. DA v12 + badge WCORE haut droite. Layout 2 colonnes : carte portfolio mock gauche (total gonfl├® `$498,231` barr├® + pill `INFLATED` rouge, clean total `$1,204` lime + pill `CLEAN TOTAL`, 3 tokens l├®git avec **vrais logos TrustWallet** ETH/USDC/OP embarqu├®s en base64 + clipPath cercle, token scam `ETHG` en row rouge avec pastille anonyme + pill `SCAM`), 4 cards signaux droite (`FAKE PRICE`, `ABSURD SUPPLY`, `AIRDROPPED JUNK`, `FLAGGED, NOT COUNTED`). **Convention logos** : les tokens l├®git ont leur vrai logo (cache local `scripts/x-cycle/assets/*.png`, source TrustWallet), le scam garde une pastille texte rouge anonyme ÔÇö renforce le contraste l├®git/scam. **Workflow draft-only** : `scripts/x-cycle/prepare-post.cjs` pr├®pare le draft complet dans Chrome CDP 9224 (texte tap├® via keyboard + Shift+Enter pour les sauts de ligne, image attach├®e via `input[data-testid="fileInput"]`, retrait des attachments stale en cas de re-run) SANS publier ÔÇö l'utilisateur review et clique Post lui-m├¬me. R├®utilisable pour tous les futurs posts.
- **CEX wallets post (2026-06-13)** : `https://x.com/WCORExyz/status/2065912993515233508`. Daily update (PAS un concept post). Texte publi├® : `Today's WCORE update. / CEX wallets now live. / Binance and Bitpanda sit next to your on-chain wallets. Same totals. Same export. Same risk rules. / Provider-first pricing. Read only. / Bitfinex, Bybit, Coinbase and OKX are next. / Read first. Act later. / wcore.xyz`. Image : `apps/web/public/wcore-post-cex-wallets.svg` + `.png` (`1200x675`), g├®n├®r├®e par `scripts/build-post-cex-wallets.cjs`. DA v12 + badge WCORE haut gauche. Layout 2 colonnes : colonne gauche (titre `Wallets, on-chain and off.` + body 2 lignes + section `ONE PORTFOLIO` lime + 2 chips CEX empil├®es Binance/Bitpanda + grille 2├ù2 pills `180+ live chains`/`80 GM chains`/`Provider-first pricing`/`Read only` + footer `Read first. Act later.` + `wcore.xyz`), colonne droite (capture `/wallet` CEX scrolled avec ratio 0.7 pr├®serv├®, contour vert 3px align├® avec clipPath + drop shadow 26/0.6). **Convention logo Bitpanda** : le brand asset officiel est un JPEG carr├® `apps/web/public/cex/bitpanda-official.jpeg` avec fond vert `#27D17F` + B noir centr├®. Le `<image>` SVG doit utiliser `data:image/jpeg;base64` (PAS png) + `preserveAspectRatio="xMidYMid slice"` + `clipPath` cercle pour matcher le style Binance. **Fichiers temporaires** : NE PAS garder d'it├®rations multiples (bitpanda-circle.png, bitpanda-diamond.png, bitpanda-original.png, bitpanda.svg, bitpanda-clean.svg/png, Bitpanda_id-qGvjcIp_0.svg). Le JPEG officiel `apps/web/public/cex/bitpanda-official.jpeg` est la source unique.
- **Bitfinex post (2026-06-14)** : `https://x.com/WCORExyz/status/2066215478549180488`. Daily update (PAS un concept post). Texte publi├® : `Today's WCORE update. / Bitfinex is now live. / Link your own read-only key. Your spot wallet sits next to Binance, Bitpanda and your on-chain wallets. Same totals, same export, read only. / Direct API, no relay. Provider-first pricing. / Bybit, Coinbase and OKX are next. / wcore.xyz`. Image : `apps/web/public/wcore-post-bitfinex.svg` + `.png` (`1200x675`), g├®n├®r├®e par `scripts/build-post-bitfinex.cjs`. DA v12 + badge WCORE haut gauche. Layout final valid├® : colonne gauche compacte (`Bitfinex is now live.` + body 2 lignes + tags `Read-only`/`Direct API`/`Provider-first pricing` + carte `ONE PORTFOLIO`), colonne droite avec Binance/Bitpanda en cartes secondaires et Bitfinex en grande carte hero `NEW`. Le script g├®n├¿re SVG + PNG via Playwright pour ├®viter un PNG stale.
- **Bybit post (2026-06-15)** : `https://x.com/WCORExyz/status/2066586833908080732`. Daily update (PAS un concept post). Texte publi├® : `Today's WCORE update. / Bybit is now included. / Your Bybit balances now sit next to Binance, Bitpanda, Bitfinex and your on-chain wallets. / 178 chains checked. 4 CEX sources. One value distribution. / Read-only keys. No wallet connection required. / Read first. Act later. / wcore.xyz`. Image : `apps/web/public/wcore-post-one-portfolio.svg` + `.png` (`1200x675`), g├®n├®r├®e par `scripts/build-post-one-portfolio.cjs`. DA v12 + badge WCORE haut gauche, fond sombre, screenshot produit int├®gr├® ├á droite, headline `Bybit is now included.`, pills `Bybit live`/`4 CEX`/`Read-only`, stats `PORTFOLIO $5,647.99` + `ASSETS 1,988`. Draft pr├®par├® via `scripts/x-cycle/prepare-post-one-portfolio.cjs`; publication manuelle par l'utilisateur.
- **Swellchain shutdown post (2026-06-18)** : `https://x.com/WCORExyz/status/2067654188692111746`. Post alerte utilitaire (PAS daily update) suite ├á l'annonce Swell : bridge avant le 23 juin, ne pas d├®pendre d'un seul tracker, WCORE retire Swell Chain de la couverture active apr├¿s le 23 juin. Image `apps/web/public/wcore-post-swell-shutdown.svg/.png` (`1200x675`), g├®n├®r├®e par `scripts/build-post-swell-shutdown.cjs`. DA v12, ton sobre, carte `June 23 deadline`, stack `Chain shutdown` / `Tracker drift` / `Coverage update`. Draft pr├®par├® via `scripts/x-cycle/prepare-post-swell-shutdown.cjs`, publication manuelle par l'utilisateur.
- **Kraken post + badges MiCA (2026-07-05)** : `https://x.com/WCORExyz/status/2073777736384950364`. Daily update (7e CEX). Texte publi├® : `Today's WCORE update. / @krakenfx is now live. / Your Kraken spot balances now sit next to Binance, Bitpanda, Bitfinex, Bybit, Coinbase, OKX and your on-chain wallets. / 7 CEX sources. 5 of them MiCA-licensed. 182 chains. One clean total. / Read-only keys. Direct API, no relay. / Read first. Act later. / wcore.xyz`. Image : `apps/web/public/wcore-post-kraken.svg` + `.png` (`1200x675`), g├®n├®r├®e par `scripts/build-post-kraken.cjs`. DA v12 : colonne gauche (titre + pills + carte ONE PORTFOLIO), colonne droite 6 mini-chips `LIVE` (2 rangs de 3) + carte hero Kraken `NEW`. **Badges MiCA** : marqueur bleu `#60a5fa` `LIVE ┬À MiCA` sur les chips licenci├®s (Bitpanda BaFin, Bybit Autriche, Coinbase Luxembourg, OKX Malte) + pill `MiCA LICENSED` sur le hero Kraken (Central Bank of Ireland). Binance et Bitfinex = `LIVE` seul (pas de licence MiCA). Le bleu distingue le statut r├®glementaire du lime `LIVE` sans casser la DA. **Convention tag** : taguer UNIQUEMENT le nouveau CEX (`@krakenfx`), jamais les 7 ÔÇö tag-begging r├®duit le reach. **Logo Bitfinex** : CMC 37 en full-bleed slice (le PNG local ├®cras├® en 28px sur cercle blanc rendait mal) ; le mode `fullbleed`/couleur hex du helper `miniCexChip` g├¿re les logos circulaires pleins. Logo Kraken : CMC exchange 24.
- **Read first / sign later post (2026-07-07)** : `https://x.com/WCORExyz/status/2074361865904283858`. Post concept (PAS daily update) issu d'un cycle X read-only : replies verifiees sur approvals/read-only, public API sans private key, fake portfolio balance scam. Texte publie : `Read first. / Sign later. / A wallet balance is public data. / A token showing $5,000 is not always liquid value. / An approval is risk, not just metadata. / Good crypto tools should tell you what is happening before asking you to do anything. / No seed phrase. / No forced wallet connect. / No fake total. / Read first. Act later. / wcore.xyz`. Image : `apps/web/public/wcore-post-read-first-sign-later.svg` + `.png` (**3200x1800**), generee par `scripts/build-post-read-first-sign-later.cjs`. DA v12 concept haute resolution : 2 colonnes equilibrees, titre gauche compact, carte `WHAT CAN BE READ SAFELY`, panneau droit `VISIBILITY LAYER`, raw value barree vs clean value, footer discret. **Gotcha composition** : verifier visuellement les exports haute resolution avant publication ; premiere version etait desequilibree (panneau droit trop dominant, debordement carte gauche), corrigee avant publication.
- **Market Cap directories post (2026-07-17)** : `https://x.com/WCORExyz/status/2078069673707348415`. Image `apps/web/public/wcore-post-market-cap.svg` + `.png` (1200x675), generee par `scripts/build-post-market-cap.cjs`. Layout final valide : quatre rangs reels dans chaque capture, contour lime Crypto et bleu Stock trace au-dessus des images, rayons du contour et du clip harmonises, aucun badge `CRYPTO RANKING`/`STOCK RANKING`. Le builder attend les quatre logos avant capture et masque les rangs suivants pour un rendu deterministe. Cycle X termine avec trois replies publiees et verifiees sur market cap/quality, equity vs token market cap, et unlock dilution. Aucun worktree ne doit etre utilise pour les futurs cycles de ce projet.
- **Compound V3 call target + pricing identity (2026-07-17)** : `collateralBalanceOf(user, asset)` appartient au Comet, jamais au contrat ERC-20 collatéral. Dans `DiscoveredToken`, `contract` reste la cible RPC Comet pendant la lecture et `pricingContract` porte l'actif pour pricing/logo/sortie. Les décimales viennent de `AssetInfo.scale`, pas d'un default 18. Les positions Compound persistées dans le discovery cache sont ignorées puis reconstruites dynamiquement; ne pas réintroduire les anciennes entrées statiques dans `tokens/registry.ts`. Les modes `mirror_native`/`mirror_underlying` sont différés vers `applyStakedPriceMirrors` sans générer de faux `NO_PRICE`, sinon le scan final devient `degraded=true` malgré un résultat correctement réparé.
- **llamao tiny WebP + naturalWidth=0 (2026-05-14)** : llamao peut retourner 200 avec un WebP microscopique (ex: `rsz_ripple.jpg` = 962 bytes) qui passe le `naturalWidth` check en `size="md"` (28px) mais ├®choue en `size="sm"` (20px). Sympt├┤me : l'ic├┤ne appara├«t dans ChainCard mais pas dans ValueDistribution (qui utilise `size="sm"`). **Fix (v2, 2026-05-14)** : cascade simplifi├®e ÔÇö plus de TrustWallet auto-d├®riv├® (`twSlug`). La cascade est : FALLBACK_ICONS (TrustWallet fiable) ÔåÆ llamao. `naturalWidth===0` v├®rifi├® UNIQUEMENT pour les CDN fragiles (llamao, spothq, coingecko) ET seulement si la cha├«ne n'a PAS de FALLBACK_ICONS. `hasFallbackIcon()` (export├® depuis `icons.tsx`) v├®rifie l'existence dans FALLBACK_ICONS. R├®sultat : logo identique ├á toutes les tailles, pas de TW 404.
- **ChainIcon cascade (2026-05-14, v2)** : le `twSlug` auto-d├®riv├® (`chainKey.toLowerCase().replace(/_/g,"")`) produisait des URLs TrustWallet fausses pour la plupart des cha├«nes non-FALLBACK_ICONS ÔåÆ 404 ÔåÆ emoji. Supprim├®. La cascade est maintenant : primary (FALLBACK_ICONS ou llamao) ÔåÆ emoji. `naturalWidth===0` ne d├®clenche que pour les CDN fragiles sans fallback TW explicite. Les cha├«nes dans FALLBACK_ICONS utilisent TrustWallet (fiable ├á toutes les tailles), les autres utilisent llamao (comportement identique sm/md).
- **Chain icons ÔÇö 3 root causes (2026-05-16, v3)** : (1) `.gitignore` bloquait `*.png`/`*.jpg` ÔåÆ 35 fichiers critiques non track├®s (Ethereum, Base, BSC, etc.). Fix : `!apps/web/public/chains/**` dans `.gitignore`. (2) `next/image` avec `unoptimized` ne servait pas les chemins locaux en Docker standalone ÔåÆ remplac├® par `<img>` natif dans `ChainIcon.tsx`. (3) Logos obsol├¿tes (HEMI = CMC token, VANA/SOMNIA = anciens llamao) ÔåÆ remplac├®s depuis **rubyscore CDN** : `https://rubyscore.fra1.digitaloceanspaces.com/chain_icons/<chain>.{png,jpg,svg}`. Pattern : `HEMI.svg`, `VANA.png`, `SOMNIA.jpg`. Toujours v├®rifier les logos avec l'utilisateur apr├¿s deploy.
- **No testnets in WCORE (2026-05-14)** : ne jamais ajouter de testnet (Monad Testnet, Sepolia, Goerli, etc.) dans `/gm`, `/dev/deploy` ou toute autre UI WCORE. Mainnet uniquement.
- **ERC20 balance cache fallback (2026-05-14)** : `readErc20Balance` (`evm.ts`) expose maintenant `consensusFailed` et le caller utilise un cache fallback, comme `readNativeBalance` le faisait d├®j├á. Quand le consensus RPC ├®choue et que la balance lue est 0, le cache est consult├®. Si le cache a une balance >0, elle est pr├®serv├®e avec `[DEGRADED]`. Cela ├®vite la perte silencieuse de tokens sur les cha├«nes ├á RPC fragiles (ex: ZERO Network, 2 RPCs seulement).
- **Ajout de cha├«nes ÔÇö checklist (2026-05-14)** : quand on ajoute une cha├«ne, 3 ├®tapes sont n├®cessaires : 1) Cr├®er le fichier `packages/core/src/chains/CHAIN.ts` (config RPC, CHAIN_ID, DEX_SLUG, etc.), 2) L'ajouter dans `packages/core/src/chains/index.ts` (import + entr├®e dans l'objet `chains`), 3) L'ajouter dans `apps/web/components/icons.tsx` ÔåÆ `CHAIN_KEY_MAP` pour la r├®solution d'ic├┤ne (llamao). Optionnel : `FALLBACK_ICONS` si le slug TrustWallet diff├¿re du chainKey, `TOKEN_REGISTRY` si des tokens connus existent. D├®ployer API + Web apr├¿s ajout. Actuellement 130 cha├«nes (v4.15.36).
- **Activation GM factory ÔÇö checklist (2026-05-14, mise ├á jour 2026-06-04)** : source unique dans `packages/shared/src/factories.ts` (adresse factory + chainId). Les RPCs sont r├®solus centralement depuis `@wcore/core` (`getRpcEndpoints()` / configs chain), pas via `CHAIN_RPCS` local. V├®rifier le chainId on-chain via `eth_chainId` avant d'ajouter. Apr├¿s ajout, v├®rifier `/gm`, `/dev/deploy` et les hooks qui consomment `getFactory()` / `getActiveFactoryChains()` ; ne pas indexer `GM_FACTORIES[chainKey]` directement.
- **GM native pricing (2026-05-14)** : `/api/price/native` utilise maintenant `NATIVE_LLAMA_ID` et `NATIVE_GECKO_ID` des configs cha├«ne (`@wcore/core`), pas de mapping hardcod├®. Le frontend fallback utilise CoinGecko avec le m├¬me ID. Ajouter une cha├«ne GM = le prix natif fonctionne automatiquement si la config cha├«ne a `NATIVE_LLAMA_ID` correct.
- **llamao slug Ôëá chainKey (2026-05-14)** : le slug llamao peut diff├®rer du chainKey. Ex: `WORLDCHAIN` ÔåÆ llamao `world-chain` (pas `worldchain`). V├®rifier avec un `HEAD` HTTP avant d'ajouter dans `CHAIN_KEY_MAP`. Si le slug TrustWallet existe (200), pr├®f├®rer `FALLBACK_ICONS`.
- **FALLBACK_ICONS 404 (2026-05-14)** : ne pas mettre une URL TrustWallet dans `FALLBACK_ICONS` sans v├®rifier `HEAD 200`. Une URL 404 dans FALLBACK_ICONS bloque le fallback llamao ÔåÆ l'emoji s'affiche sans image. Pattern : d'abord v├®rifier llamao, puis ajouter FALLBACK_ICONS uniquement si TrustWallet fonctionne ET le slug diff├¿re.
- **edit_file + function wrapper deletion (2026-05-14)** : l'outil d'├®dition peut accidentellement supprimer la d├®claration `function ChainCardInner(...)` et tous les hooks associ├®s quand on retire un bloc de code dans le useEffect. Laisser le `useEffect(() => {` intact est critique ÔÇö le Turbopack verra un `return` au scope module et refusera le build. Toujours v├®rifier que la fonction englobante est pr├®serv├®e apr├¿s une ├®dition.
- **DeployClient chainId hex vs on-chain (2026-05-14)** : le `chainId` dans `CHAINS` de DeployClient doit matcher le return de `eth_chainId` on-chain. Ink: `0xdef1` (57073), pas `0xdef0` (57072). Abstract: `0xab5` (2741), pas `0x1095a` (67882). V├®rifier via `eth_chainId` avant d'ajouter.
- **Railway backboard instability (2026-05-14)** : `backboard.railway.com` peut retourner 500 ou timeout ind├®finiment. Les d├®ploiements ├®chouent en masse. Solution : r├®essayer plus tard, le code est d├®j├á push├® sur GitHub. Ne pas boucler sur les retries.
- **Audit fixes s├®curit├®/pricing (2026-05-13)** : stablecoin peg pricing n├®cessite `isStable === true` (ne jamais pricer par symbole seul), les cl├®s de cache prix EVM sont `${chain}:${contract}`, les custom tokens EVM doivent ├¬tre ajout├®s avant les appels balance, le fallback cache native EVM/SVM ne s'active que si consensus RPC ├®choue, pas sur un vrai z├®ro. SSE notifications accepte uniquement les tokens opaques single-use, jamais un JWT en query string. API Docker doit rester sur `prisma migrate deploy`, jamais `db push --accept-data-loss` en prod.
- **API tests Railway-only (2026-05-13)** : ne plus documenter Postgres/Redis locaux (`127.0.0.1:5433`, `6380`) comme pr├®requis. Les tests API DB utilisent `TEST_DATABASE_URL` et `TEST_REDIS_URL`, id├®alement Railway test/staging. Ne JAMAIS pointer ces variables vers la prod : plusieurs suites font des `deleteMany()` Prisma. `apps/api/set-test-env.js` mappe `TEST_DATABASE_URL` ÔåÆ `DATABASE_URL` et `TEST_REDIS_URL` ÔåÆ `REDIS_URL`, et refuse explicitement `localhost`, `127.0.0.1`, `::1`.
- **RealToken scam exception (2026-05-13)** : les tokens RealToken (Gnosis) ont des symboles longs (`REALTOKEN-S-...`) 40-60 chars et des noms contenant "RealToken". Sans exception dans `scam-detector.ts`, les regles #5 (longueur symbole > 20, weight 1) + #8 (nom generique + prix null, weight 2) declenchent ­ƒÜ®. Fix : exception `REALTOKEN-` prefix + `RealToken ` name prefix dans `detectScam()`. `explorer-discovery.ts` avait deja cette exception mais `scam-detector.ts` non. `SCAM_RULES_VERSION` bump 6 ÔåÆ 7. Source de prix : `api.realtoken.community/v1/token/{contract}`, prioritaire dans la cascade (avant DefiLlama/DexScreener/GT).
- **Blockscout Pro API (2026-05-18)** : `BLOCKSCOUT_API_KEY` sert uniquement ├á la d├®couverte de contrats ERC-20 dans `explorer-discovery.ts`, jamais aux balances affich├®es/calcul├®es. Les balances restent lues via RPC/Multicall. En cas de `402/403/429/5xx` ou timeout, cooldown m├®moire par cha├«ne (`BLOCKSCOUT_COOLDOWN_MS`, d├®faut 10min) puis fallback `eth_getLogs`. Kill-switch prod : `BLOCKSCOUT_DISABLE=1`. Timeout d├®faut : `BLOCKSCOUT_TIMEOUT_MS=2500`. Ne jamais cacher un ├®chec Blockscout comme r├®ponse vide "clean" sinon `trustExplorerWhenClean` skipperait les logs. **Doc Pro API** : la nouvelle Pro API universelle utilise `https://api.blockscout.com/v2/api?chain_id=<id>&module=...&action=...&apikey=...`. Les anciennes cl├®s MyAccount et anciennes routes ne fonctionnent pas avec la Pro API. Ne pas supposer que `X-API-Key` sur les URLs explorer individuelles `/api/v2/...` active le quota Pro; v├®rifier/migrer vers l'endpoint universel avant de compter dessus. **API call costs (credits)** : default=20, `token-transfers`=30, `logs`=30, `internal-transactions`=40, `coin-balance-history`=50, `raw-trace`=50, `summary`=50, `tokens`=30, `search/quick`=25, `verification/config`=40, `advanced-filters`=50, `metadata`=120, `multichain/*`=100-120. Notre endpoint `module=account&action=tokenlist` = default 20 credits/call. 52 cha├«nes actives ├ù 20 credits = 1040 credits/run si toutes scann├®es. **52 cha├«nes support├®es** : ETHEREUM, OPTIMISM, ROOTSTOCK, GNOSIS, SHIBARIUM, FUSE, UNICHAIN, POLYGON, HASHKEY, TAC, FVM, ZKSYNC_ERA, SHAPE, ASTAR, FLOW, REDSTONE, MATCHAIN, ZETACHAIN, LISK, STORY, REYA, SONEIUM, WORLDCHAIN, MEGAETH, MODE, INK, BASE, ARBITRUM_ONE, ARBITRUM_NOVA, CELO, ETHERLINK, IMMUTABLE, SCROLL, ETHEREUM_CLASSIC, ZILLIQA_EVM, SHIMMER_EVM, BXN, EDEN, WORLD_MOBILE, PLAYNANCE_PLAYBLOCK, LIGHTLINK, MOCA_CHAIN, KITEAI, AWAJI, NUMINE, IOTA_EVM, EDU_CHAIN, ICB_NETWORK, CREDITCOIN, CROSS_MAINNET, GENSYN, NEON. **Filtre UNKNOWN** : tokens avec symbol < 2 chars ou "Unknown Token" sont filtr├®s. **eth_getLogs TOUJOURS activ├®** ÔÇö Blockscout est un hint en plus, pas un remplacement.
- **RealT Cache Permanent Redis (2026-05-19)** : `RealTPriceSource` utilise maintenant un cache Redis permanent (`realt:price:{contract}`) sans TTL. Les prix ne sont mis ├á jour QUE quand l'API r├®pond 200 avec un prix valide. Quand l'API bloque Railway (403/401/timeout), le prix cach├® Redis est servi ind├®finiment. Circuit breaker pr├®serv├® (5 ├®checs ÔåÆ skip API 30min). Hydratation m├®moire au premier acc├¿s. `evm.ts` passe `opts.cache` (Redis) au constructeur `RealTPriceSource(cache)`.

### Gotchas WCORE Web ÔÇö EVM Consensus + Nexus + Polling + GM Gnosis + BASE (v0.2.23, 2026-05-20)

- **EVM Consensus ÔÇö z├®ro ne bat plus cache positif (v0.2.23)** : quand le consensus RPC retourne un balance z├®ro partiel ou non-consensus, il ne doit PLUS ├®craser un cache positif r├®cent. Seuls les z├®ros **confirm├®s par consensus** (votes * 2 > total) peuvent ├®craser le cache. Fichiers : `packages/core/src/engines/evm.ts`, `packages/core/src/balances/consensus.ts`. Commit `575151f`.
- **Nexus Mainnet (v0.2.23)** : cha├«ne EVM ajout├®e ÔÇö chainId `3946`, RPC `https://mainnet.rpc.nexus.xyz/`, natif `NEX`. Config : `packages/core/src/chains/NEXUS.ts`. V├®rifi├® en prod via `/api/chains`.
- **Polling progressif UI (v0.2.23)** : le frontend merge maintenant `pollData.chains` pendant le polling async ÔÇö les r├®sultats apparaissent progressivement sans attendre la fin du lot. Protection cache : pas de cache si r├®sultat partiel avec erreur. Fichiers : `apps/web/components/WalletContent.tsx`, `apps/web/components/scan-results.ts`.
- **GM Gnosis fix (v0.2.23)** : `/api/gm/status-onchain` doit ├¬tre appel├® avec `apiFetch` (JWT), PAS `fetch` brut. Le bypass r├®conciliation quand `deployed=true/gmDone=false` doit aussi utiliser `apiFetch`. Reset ├®tat sur changement cha├«ne/adresse. Fichier : `apps/web/hooks/useGmChain.ts`.
- **BASE timeout ÔÇö Blockscout tokenlist massive (v0.2.23)** : Blockscout retourne une tokenlist spam-heavy massive sur BASE ÔåÆ traitement synchrone timeout (`chain_timeout: BASE exceeded 60000ms`). Fix : `BLOCKSCOUT_MAX_TOKENLIST` cap ├á 500 items + `prefetchTokenLogo` fire-and-forget. Fichier : `packages/core/src/tokens/explorer-discovery.ts`. Commit `78840ba`. **Non d├®ploy├®** si Railway bloque.
- **Railway deploy paused (2026-05-20)** : `railway up` peut retourner `Deploys have been paused temporarily`. C'est un blocage c├┤t├® Railway, pas un probl├¿me de code. Les services restent online. R├®essayer plus tard. Ne pas boucler.
- **NotificationsBell unread count revient (2026-05-20)** : apr├¿s un clic sur "mark all read", le count revient car le SSE `snapshot` et le polling 60s (`fetchNotifications`) ├®crasent l'├®tat local optimiste (`setUnreadCount(0)`). Le serveur retourne l'ancien count ou le snapshot arrive apr├¿s l'update locale. **Fix** : ignorer les updates SSE/polling pendant 5s apr├¿s une action utilisateur via un `lastActionAt` ref. Fichier : `apps/web/app/profile/components/NotificationsBell.tsx`.

### Gotchas WCORE Web ÔÇö Scam fix + Batch scan + BASE timeout (v0.2.24-v0.2.25, 2026-05-21)

- **_BLOCKED_CONTRACTS ne bloquait pas (v0.2.24)** : le check `_BLOCKED_CONTRACTS` ├®tait dans `isKnownToken()` qui retournait `false` (= "pas connu"), rendant les tokens bloqu├®s **plus susceptibles** de passer les heuristiques au lieu d'├¬tre bloqu├®s. **Fix** : d├®plac├® en haut de `detectScam()` comme `_adminBlockedContracts`, retourne `isSuspicious: true, level: "scam"` imm├®diatement. Fichier : `packages/shared/src/scam-detector.ts`. Tokens concern├®s : stkAVNT (`0xd546040f...`), Scroll scam (`0x94b5bd0c...`), BASE scam (`0x27777ec2...`, `0x59828f30...`).
- **Multi-wallet batch scan (v0.2.24)** : nouveau endpoint `POST /api/scan/batch` accepte un array d'adresses (max 20). `getEvmWalletsAssets()` dans `evm.ts` scanne plusieurs wallets sur la m├¬me cha├«ne en une passe : **UN seul appel Multicall3** pour TOUS les wallets ├ù TOUS les tokens au lieu de N appels s├®par├®s. Pricing partag├® entre wallets. Non-EVM fallback vers scans individuels. Fichiers : `apps/api/src/plugins/scan.ts`, `apps/api/src/schemas.ts`, `packages/core/src/engines/evm.ts`.
- **GLOBAL_CHAIN_CONCURRENCY harmonis├® avec SCAN_CONCURRENCY (2026-05-27)** : frontend et backend utilisent maintenant la m├¬me source ÔÇö `NEXT_PUBLIC_SCAN_CONCURRENCY` (frontend) et `SCAN_CONCURRENCY` (backend), d├®faut **50**. Le frontend lit `process.env.NEXT_PUBLIC_SCAN_CONCURRENCY` au lieu d'une constante hardcod├®e. Les deux doivent rester synchronis├®s pour ├®viter un bottleneck. Fichier : `apps/web/components/WalletContent.tsx`.
- **Batch frontend par VM/chain** : EVM, SVM et Cosmos passent tous par `/api/scan/batch` depuis `WalletContent.tsx`. EVM est optimis├® nativement via `getEvmWalletsAssets()` ; SVM/Cosmos utilisent le fallback serveur vers scans individuels, mais restent dans la m├¬me queue globale. Ne pas r├®introduire un `await` EVM s├®par├® avant les non-EVM.
- **UI activeScanChains** : les logos ├á droite de `Scanning portfolio` doivent ├¬tre aliment├®s par la queue globale, y compris le chemin batch. Si les logos disparaissent pendant un scan batch, v├®rifier que `setActiveScanChains()` est appel├® au d├®but/fin de chaque job.
- **Post X One Scan Flow (2026-05-21)** : `https://x.com/WCORExyz/status/2057542358670189016`. Image : `apps/web/public/wcore-post-global-scan-queue.svg` + `.png` (`1200x675`). DA valid├®e : fond sombre WCORE, titre `One Scan Flow.`, carte centrale `Shared Scan Engine`, ic├┤nes vectorielles EVM/Solana/Cosmos ├á gauche, live progress ├á droite. Ne pas remettre de logos PNG de cha├«nes dans les 3 cartes de gauche ni de gros halo rond vert au centre.
- **Post X Multichain Map (2026-05-23)** : `https://x.com/WCORExyz/status/2058205408188158434`. Image : `apps/web/public/wcore-post-multichain-map.svg` + `.png` (`1200x675`). DA valid├®e : fond sombre WCORE, titre `Your crypto is not on one chain.`, badge compact `READ-ONLY ┬À 180+ CHAINS`, cartes EVM/Solana/Cosmos sur pastilles sombres, label `ONE CLEAN MAP`, CTA `wcore.xyz`. Ne pas remettre `EVM ┬À SVM ┬À IBC` dans le badge haut droit (d├®bordait) et ne pas reprendre le wording `Stop jumping between explorers.` trop proche de l'angle AALADIN.
- **Scripts X / Playwright CDP (2026-05-23)** : apr├¿s l'incident AALADIN masqu├®, toute automatisation X doit ├¬tre **lecture seule par d├®faut**. S├®parer scan et ex├®cution. Ne jamais publier/liker/follow/DM dans le m├¬me script qui scanne. Toujours proposer l'action + texte exact et attendre validation explicite utilisateur. Pour ex├®cuter : cibler par `status_id`, v├®rifier auteur + texte, timeouts explicites, abort si `data-testid="mask"`, modale inattendue, `R├®afficher`, `Masquer`, `Bloquer`, ou menu global. V├®rifier le draft r├®el avant publication et recharger le thread apr├¿s. Si l'utilisateur dit que c'est publi├®, stopper et ne pas retenter.
- **BASE scan timeout r├®solu (v0.2.25)** : BASE a `DISABLE_BLOCKSCOUT: true` + `MAX_LOG_RANGE: 2000` ÔåÆ repose uniquement sur `eth_getLogs` + pricing cascade sur ~100+ tokens. Le goulot est le **pricing cascade** (DefiLlama ÔåÆ DexScreener ÔåÆ GT ÔåÆ CoinGecko) ├ù 100+ tokens. Pas de timeout de cha├«ne actif (supprim├® pr├®c├®demment). **├Ç investiguer** : ajouter un per-chain timeout ou limiter le nombre de tokens ├á pricer sur BASE.

### Gotchas WCORE Web ÔÇö Refactor splits + GM rate-limit (2026-05-29)

- **INCENTIV phantom chain retir├®** : INCENTIV n'existe que comme **testnet** (Incentiv Devnet, chainId 16350, banni par "No testnets in WCORE"). Le chainId 24101 r├®f├®renc├® dans wagmi/DeployClient ├®tait un fant├┤me : `rpc.incentiv.network` DNS fail, `24101.rpc.thirdweb.com` ÔåÆ "Invalid chain", absent de chainid.network. Aucun core config (jamais dans les 181 cha├«nes). Le logo apparaissait en fallback Ôøô´©Å car la cha├«ne n'├®tait pas scannable. **Fix** : retir├® de `wagmi.ts` (chain def + transport + array), `DeployClient.tsx` (CHAINS array + lookup), `GmPageClient.tsx` (SOON_CHAINS), `chain-icon-manifest.json`, `chain-native-symbols.json`, + supprim├® `public/chains/INCENTIV.svg`. **Le├ºon** : avant d'ajouter un logo manifest, v├®rifier que la cha├«ne a un core config valide ET un RPC mainnet vivant (`eth_chainId`). INTUITION (1155) et DBK_CHAIN sont valides et gard├®s.
- **GM header native price `chain=undefined`** : le `GmButton` du header cr├®e `useOnChainGm({ walletAddress, streak })` SANS `chainKey` (la cha├«ne vient de `getRandomContract()` au moment du clic). `fetchNativePrice` lisait `config.chainKey` (undefined) ÔåÆ `GET /api/price/native?chain=undefined` ÔåÆ `{"price":null}` ÔåÆ throw `"Native price zero for undefined"`. **Fix** : `fetchNativePrice(chainKey)` prend la cha├«ne en param├¿tre ÔÇö `sendGm` passe le `chainKey` r├®solu par `getRandomContract()`, `deployContract` passe `config.chainKey`. Ne jamais d├®river la cha├«ne du `config` statique du hook dans le flow header (random multi-cha├«ne).
- **GM native price transient null (KAIA/fraxtal)** : `/api/price/native` d├®pend de DefiLlama/CoinGecko qui hiccupent transitoirement ÔåÆ `{"price":null}` au moment du clic ÔåÆ throw `"Native price unavailable for X"`. Le retry frontend (3├ù, ~2.4s) ne suffit pas toujours. **Fix d├®fense en profondeur** : (1) timeout `AbortSignal.timeout(5000)` sur chaque fetch upstream, (2) cache Redis `native_price:{chainKey}` (TTL 24h) ├®crit ├á chaque succ├¿s, servi en fallback (`source: "...-cached"`) quand les deux upstreams ├®chouent, (3) retry frontend 3├ù dans `fetchNativePrice`. Warmer le cache apr├¿s deploy en hittant `/api/price/native?chain=X` pour chaque cha├«ne GM. Le tip GM n'a besoin que d'un prix approximatif ÔåÆ 24h de staleness est acceptable.
- **`as any` factoris├® `useSafeSwitchChain`** : le pattern `switchChainAsync({ chainId: x as any })` (wagmi inf├¿re un union litt├®ral de chainIds, les flows GM r├®solvent des IDs dynamiques valides au runtime) ├®tait dupliqu├® dans `useOnChainGm.ts` ET `useGmContracts.ts`. Factoris├® dans `hooks/useSafeSwitchChain.ts` ÔÇö un seul cast contenu `as Parameters<typeof switchChainAsync>[0]`, helper stable `switchChain(chainId: number)`. `window.ethereum` a d├®j├á un type global dans `lib/window.d.ts` ÔåÆ les `(window as any).ethereum` ├®taient redondants.

- **`next build` Ôëá `pnpm typecheck`** : la commande `pnpm typecheck` (tsc projet) peut passer alors que `next build` (Turbopack TS check) ├®choue sur les m├¬mes fichiers. Railway lance `next build`. **Toujours** lancer `pnpm --filter @wcore/web build` localement avant de d├®ployer le web apr├¿s un refactor de composants, sinon le build Railway ├®choue (deploy failed au runtime/healthcheck). Trois erreurs introduites par un split n'ont ├®t├® vues que par `next build` : prop `chains: unknown[]` au lieu de `ChainScan[]`, `secondaryLabel?: string` au lieu du type canonique `ScanProgressDisplay` (`string | null`), et la collision `TokenTable`.
- **Collision de composant lors d'un split** : un agent qui extrait un sous-composant peut **├®craser un fichier existant** du m├¬me nom. `WalletContent.tsx` (onglet tokens) a g├®n├®r├® un nouveau `TokenTable.tsx` qui a d├®truit le `TokenTable` riche (scam detection + tri + expand) utilis├® par `ChainCard.tsx`. Sympt├┤me : `next build` ├®choue `Property 'native' does not exist`. Fix : restaurer l'original via `git checkout <sha> -- <file>` et renommer le nouveau (`AllTokensTable.tsx`). V├®rifier les noms de fichiers existants avant qu'un agent cr├®e un composant.
- **GM rate_limited depuis le header (fan-out per-card)** : la page `/gm` rend ~30 `GmChainCard`. Chaque carte via `useGmChain` refaisait `GET /api/gm/has-deployed` + `GET /api/gm/status` (le **m├¬me** endpoint global) + parfois `status-onchain` au montage ÔåÆ 2-3├ù30 = 60-90 appels `gm_read` simultan├®s ÔåÆ 429. Le header `GET /api/gm/random` (m├¬me bucket `gm_read`, limite 90) tombait alors en `rate_limited`. **Fix** : `gmStatusFetchPlan(initialStatus)` dans `gm-status-reconcile.ts` ÔÇö quand la page passe `initialStatus` (elle a d├®j├á fetch├® le status global une fois), la carte ne refait JAMAIS le fetch global, seulement le reconcile `status-onchain` cibl├® si `deployed && !gmDone`. + bump `RATE_LIMIT_GM_READ` (d├®faut 300, env override) comme defense-in-depth. Repro : ├¬tre sur `/gm` puis cliquer le GM on-chain random du header.
- **evm.ts split├® en 5 modules (barrel)** : `packages/core/src/engines/evm.ts` (1498 l.) ÔåÆ `evm-types.ts`, `evm-balances.ts`, `evm-pricing.ts`, `evm-scan.ts`, `evm-batch.ts` + barrel `evm.ts` (~25 l.) qui re-exporte tout. Z├®ro changement conscommateur (`engines/index.ts`, `dispatch.ts`, `evm.test.ts`, `scan.ts`). Singletons module-level (`sharedPriceCache`, `defaultSources`, `_blockCache`) partag├®s car scan+batch importent du m├¬me module.
- **WalletContent.tsx split├®** : 1231 ÔåÆ 487 l. Extrait `useScanOrchestrator` (hook scan complet), `useWalletLabels`, `lib/scan-api.ts` (`fetchBatchScan`/`fetchScan`/`makeErrorChainScan`), et composants `PostScanBanner`/`ScanProgressBanner`/`PortfolioSummaryCard`/`WalletSelector`/`AllTokensTable`.

### Gotchas WCORE Web ÔÇö Connect + GM fixes v0.2.34 (2026-05-28)

- **Connect flow ÔÇö erreur invisible en "ready"** : l'erreur de login etait set via setError() mais jamais affichee dans la vue "Sign In" (l.220-234). Ajouter l'element {error && ...} dans le bloc "ready". Sans ca, toute erreur de login (rate limit, chainId mismatch, serveur down) est silencieuse ÔåÆ l'utilisateur reclique en boucle.
- **Connect flow ÔÇö chainId hardcode a 1** : quand isConnected=true, chainId restait 0 ÔåÆ fallback chainId||1. Lire wagmiChainId depuis useAccount() pour que le SIWE message claim la bonne chaine.
- **Connect flow ÔÇö flickering wagmi sync** : l'effet [isConnected, wagmiAddress, authStep] appelle setAddress(addr) a chaque changement d'authStep meme si l'adresse est identique. Guard prevAddressRef et prevAuthStepRef pour eviter les re-renders inutiles.
- **GM contracts deployes mais absents de la DB** : has-deployed (GET) utilisait seulement prisma.gmContract.count(). Si le vieux deploy fire-and-forget avait echoue (RPCs lents), le contrat on-chain existait mais pas en DB ÔåÆ bouton "Deploy" au lieu de "Say GM". Fix : scan on-chain (eth_getLogs sur la factory) quand count===0.
- **GM onchain receipt verification trop courte** : 3 retries ~6s insuffisant pour B3 et autres chaines a RPCs lents. Passe a 10 retries avec backoff 5s+2s ÔåÆ ~2 min max. Le frontend fait maintenant un fire-and-forget avec 3 retries background au lieu d'attendre.
- **GM frontend marquait succes meme si backend echouait** : lsSetGmDone + wcore-gm-done etaient appeles meme quand /api/gm/onchain retournait 400. Desormais le backend est appele en fire-and-forget, l'UI marque Ô£à GM des confirmation MetaMask (la tx est on-chain), et le backend retry en arriere-plan.

### Gotchas WCORE Web ÔÇö Audit complet (2026-05-26)

- **Pas de compression HTTP sur l'API** : `@fastify/compress` n'est pas enregistr├®. Les r├®ponses JSON (scan results 50-200KB) ne sont pas compress├®es. Ajouter `await app.register(compress, { global: true })` dans `server.ts`.
- **Indexes DB manquants** : `onchain_gms(userId, createdAt)`, `onchain_gms(contractId)`, `gm_contracts(ownerId)`, `gm_contracts(creatorAddress)`, `notifications(userId, createdAt)`, `users(score)` n'ont pas d'index. Migration Prisma ├á cr├®er.
- **N+1 query dans `/api/gm/random`** : boucle `for (const c of contracts)` avec `findFirst` individuel ÔåÆ 30+ queries s├®quentielles. Remplacer par un batch `findMany` + Set.
- **`recordOpsEvent` fait DELETE ├á chaque write** : `prisma.opsEvent.deleteMany` ex├®cut├® ├á chaque event. D├®placer dans le `setInterval` 5min existant.
- **`clearCookie` ne passe pas `COOKIE_OPTS`** : `clearAuthCookies` dans `auth.ts:88-91` ne passe que `{ path: "/" }` au lieu de `COOKIE_OPTS` complet. En prod avec `secure: true`, certains navigateurs peuvent ├®chouer le clear.
- **`support.ts` admin check timing-unsafe** : `isAdmin()` utilise `===` au lieu de `timingSafeEqual`. Refactorer pour utiliser `isAdminAuthorized` de `server.ts`.
- **Access tokens non r├®voqu├®s** : seul le refresh token a un check de r├®vocation (`isTokenRevoked`). Un access token compromis reste valide 24h. Option : r├®duire TTL ├á 15min + refresh flow, ou ajouter `jti` aux access tokens.
- **`strict: false` sur web tsconfig** : `apps/web/tsconfig.json:14` d├®sactive tous les strict checks. Contradictoire avec `tsconfig.base.json` qui a `strict: true`.
- **`evm.ts` = 1474 lignes** : contient scan single-wallet, multi-wallet batch, balances, pricing, discovery, metadata, cache. Split en `evm-scan.ts`, `evm-balances.ts`, `evm-pricing.ts`.
- **`WalletContent.tsx` = 1124 lignes, 22 useState** : contient orchestration scan, progress, merge r├®sultats, UI 3 tabs, toasts, circuit breaker, CSV export, scam adjustments. Split en sous-composants.
- **`IntraScanCache` type dupliqu├® dans 7 fichiers** : `Map<string, Promise<any>>` r├®p├®t├® dans evm/svm/cosmos/dispatch/scan. Extraire dans `packages/core/src/types.ts` comme `Map<string, Promise<PricingResult>>`.
- **Pas de `next/dynamic`** : 0 usage de code splitting. PdfExport, WelcomeModal, ValueDistribution, GmWithdrawNotification devraient ├¬tre lazy-loaded.
- **Pas de `next/image`** : tous les `<img>` sont natifs. Known tradeoff (Docker standalone), mais les images distantes pourraient b├®n├®ficier de l'optimisation Next.js.
- **`@tanstack/react-query` non utilis├®** : import├® en dependency mais aucun `useQuery`/`useMutation` dans le code. Dead dependency ~40KB.
- **`React.memo` sous-utilis├®** : seul `ChainCard` est memo. `TokenIcon`, `ChainIcon`, `TokenTable` rendus dans des listes de centaines d'items sans memo.
- **Pas de `error.tsx` par route** : 1 seul ErrorBoundary global. Un crash dans WalletContent ou ProfileClient fait tomber toute l'app.
- **Bearer header auth activ├® par d├®faut** : `AUTH_ALLOW_BEARER !== "false"` accepte les Bearer tokens. Augmente la surface XSS. D├®sactiver une fois les cookies fully adopted.
- **`x-admin-token` absent de CORS `allowedHeaders`** : les requ├¬tes cross-origin avec ce header ├®chouent le preflight. Ajouter dans `server.ts:227`.

### Gotchas WCORE Web ÔÇö Audit global 2026-05-27

- **Secrets dans docs = rotation obligatoire** : m├¬me apr├¿s remplacement par placeholders, toute cl├® r├®elle qui a ├®t├® commit├®e ou charg├®e en contexte doit ├¬tre rotat├®e c├┤t├® fournisseur. Ne jamais mettre `GOCSPX-`, `proapi_`, tokens Railway/DB/JWT dans `AGENTS.md`, `ROADMAP.md`, `README.md`, `DEPLOY.md` ou audits.
- **CI API integration gated** : le step GitHub Actions doit lire `TEST_DATABASE_URL` et `TEST_REDIS_URL` au niveau `job.env`, pas seulement dans le `env` du step, sinon le `if` peut skipper les tests avant injection des secrets.
- **Open findings audits** : la source d'├®tat courant est `docs/AUDIT.md` (fichier unique). Les anciens rapports dat├®s (2026-05-21 ÔåÆ 2026-06-07) sont consolid├®s dedans et conserv├®s uniquement dans l'historique git. Ne plus cr├®er de nouveau fichier d'audit dat├® : mettre ├á jour `docs/AUDIT.md`.
- **GM factory lookup web** : c├┤t├® frontend, ne pas indexer `GM_FACTORIES[chainKey]` directement. Les cl├®s factory sont lowercase et les r├®ponses API/DB peuvent ├¬tre UPPERCASE canoniques. Utiliser `getFactory(chainKey)` / `getFactoryAddress(chainKey)`.
- **CSRF state-changing routes** : toute nouvelle route `POST/PUT/PATCH/DELETE /api/*` avec cookies doit ├¬tre couverte par CSRF. Pr├®f├®rer une strat├®gie deny-by-default plut├┤t qu'une allowlist partielle par pr├®fixes.
- **GM public reads rate-limit** : `/api/gm/random`, `/api/gm/contracts`, `/api/gm/status` ne doivent pas bypasser ├á la fois le rate-limit GM et le catch-all. `/api/gm/random` peut faire DB + RPC + pricing.
- **`TokenIcon` render purity** : ne jamais appeler `setState` dans le corps du render. Les fallbacks d'ic├┤nes doivent ├¬tre d├®termin├®s sans mutation state, puis avanc├®s uniquement via `onError`/`onLoad` ou calcul pur.
- **forceRefresh vs `empty:*` EVM** : `forceRefresh` doit bypasser les short-circuits d'optimisation (`empty:*`, ├®ventuellement `bal_cache:*`) mais peut garder les caches de fallback balance/prix pour mode d├®grad├®. Sinon un wallet nouvellement fund├® peut rester `[CACHED_EMPTY]` jusqu'au TTL.
- **RPC resolver dynamique pas encore universel** : `getRpcEndpoints()` centralise GM/scripts, mais les engines scan peuvent encore lire `chain.RPC.ENDPOINTS` directement. Ne pas documenter ÔÇ£scan utilise toujours les RPC dynamiquesÔÇØ tant que les engines ne passent pas par le resolver.
- **SVM/Cosmos raw amounts** : ├®viter `Number(rawAmount)` sur les balances. Les montants REST/SPL peuvent d├®passer `Number.MAX_SAFE_INTEGER`; utiliser string/bigint + format d├®cimal.
- **Cosmos IBC decimals** : ne pas default aveugl├®ment `ibc/*` inconnu ├á 18 d├®cimales. R├®soudre via assetlist/denom trace ou marquer `decimals_unknown` pour ├®viter une sous-├®valuation massive.
- **Docs version/test counts** : `README.md` ne doit plus hardcoder version produit ni nombre de tests. Source ├®tat courant = `ROADMAP.md`; r├®sultats tests = CI / commandes de validation fra├«ches.

### Gotchas WCORE Web ÔÇö TON support (v0.2.39, 2026-06-03)

- **TON = 4e VM** : `VmType` z.enum ├®tendu de `["EVM","SVM","COSMOS"]` ├á `["EVM","SVM","COSMOS","TON"]`. Fichier : `packages/shared/src/vm.ts`. Toute union `VmType` doit ├¬tre mise ├á jour.
- **TON address format** : user-friendly base64 (`EQxxx...` / `UQxxx...` / `Efxxx...` / `Ufxxx...`, 48 chars total) OU raw `-1:hex64` (workchain + hash). Regex permissive `^(EQ|UQ|Ef|Uf)[A-Za-z0-9_-]{40,60}$` + raw `^-?\d+:[a-f0-9]{64}$`. Sch├®ma Zod `TonAddress` dans `packages/shared/src/address.ts`. Inclus dans `AnyAddress` union.
- **TON RPC** : TonAPI (`https://tonapi.io/v2/accounts/:addr`) primary, Toncenter (`https://toncenter.com/api/v2/getAddressBalance?address=...`) fallback. Pas de consensus multi-endpoint (TON a peu d'API publiques stables, multi-source = fragilit├®). Engine `getTonWalletAssets` dans `packages/core/src/engines/ton.ts`.
- **TON decimals** : native 9 (nano Toncoin). `1 TON = 1_000_000_000 nano`. Jettons d├®clarent leurs propres `decimals` (1-18 typiquement).
- **TON jettons pricing** : cascade DefiLlama (mapping `coingecko:`) ÔåÆ DexScreener ÔåÆ GeckoTerminal ÔåÆ CoinGecko ÔåÆ Jupiter (SVM). 10-concurrence max via `Promise.allSettled` chunked. Cache 3600s via `RedisPricingCache` partag├®.
- **TON n├®gatif cache** : 2min TTL, mais liveness check : si le n├®gatif cache hit est lu, on retente quand m├¬me (vs SVM/Cosmos 10min). Pourquoi : TON jettons bougent vite (mint/burn via contracts dynamiques).
- **TON chain config** : `packages/core/src/chains/TON.ts`, `CACHE_VERSION: 1`, `FLAGS.DISABLE_NATIVE_BALANCE: false` (native OK sur TON), `NATIVE_LLAMA_ID: "coingecko:the-open-network"`. Pas de `CHAIN_ID` (TON a workchain, pas chainId EVM).
- **TON UI** : EMOJI ­ƒîè dans `ChainIcon.tsx`, VmBadge cyan (`bg-cyan-900/30`) dans `ChainCard.tsx`, `TOKEN_COLORS.TON: "#0098EA"` dans `icons.tsx`, ic├┤ne locale `apps/web/public/chains/TON.png` (TrustWallet 11836 bytes), entr├®e dans `chain-icon-manifest.json` et `chain-native-symbols.json`.
- **TON default chains** : ajout├® ├á `apps/web/lib/defaults.ts` (16e default). Appara├«t coch├® dans le `ChainSelector` au chargement initial.
- **TON HomePageClient** : `detectVmType` reconna├«t TON, `chainType` cast ├®tendu ├á `"EVM" | "SVM" | "COSMOS" | "TON"`, placeholder `TON (EQ...)` ajout├®.
- **TON n'a pas de GM factory** : pas d'entr├®e dans `GM_FACTORIES`. La page `/gm` ne liste pas TON. `apps/api/src/gamification/` n'est pas touch├® pour TON.
- **TON 5 tests** : `packages/core/src/engines/ton.test.ts` couvre empty address, decimal conversion, jetton pricing, fallback Toncenter, all-fail degraded. 200/200 core tests pass.
- **TON perf** : pas de consensus RPC = scan rapide. TonAPI retourne balance + jettons en 1 call. Total: ~1-3s par wallet TON (vs 5-10s pour EVM multi-RPC).

### Gotchas WCORE Web ÔÇö KCC pre-Shanghai + Paris EVM build + Deploy UX guard (2026-06-05)

- **KCC mainnet est pre-London + pre-Shanghai** : `eth_getBlockByNumber("latest", false)` ne retourne pas `baseFeePerGas` (KCC n'a jamais re├ºu EIP-1559) ET le bytecode deploy├® avec PUSH0 (`0x5f`) revert silencieusement ├á l'ex├®cution (opcode `PUSH0` ajout├® dans Shanghai = EIP-3855, KCC ne l'a jamais activ├®). Sympt├┤me : `eth_getCode` retourne `0x` (vide) sur les contrats d├®ploy├®s avec un build Shanghai. Solution : build Paris d├®di├®.
- **Build Paris EVM (solc 0.8.19 + `evmVersion: "paris"`)** : `apps/web/public/build.json` contient 4 entr├®es ÔÇö `GmOnChain` (Shanghai), `GmOnChainParis` (Paris, 0 PUSH0), `GmFactory` (Shanghai), `GmFactoryParis` (Paris, 0 PUSH0). ABI identiques entre les 2 builds (m├¬mes functions/events/errors), seul `bin` change. `build-selector.ts` exporte `pickBuild(build, chainKey, contract)` + `PARIS_BUILD_CHAINS = new Set(["KCC"])` + interface `BuildOutput` ├®tendue. Test statique `build-json-paris.test.ts` (2 tests) lit `build.json` au runtime et v├®rifie 0 PUSH0 dans constructor+runtime via scanner forward avec instruction-boundary tracking.
- **Source pragma `^0.8.20` ne compile PAS en 0.8.19** : workaround = `contracts/compile-v0.8.19.js` cr├®e un dossier `tmp-paris/` avec une copie relax├®e ├á `pragma solidity ^0.8.19` (depuis 0.8.20), compile via `require("solc")` (npm), nettoie. Aucune modification persistante des `.sol` originaux. Scripts `compile-v0.8.19.js` + `patch-build-json.js` track├®s en git, artefacts `*.v0819.json` + `tmp-paris/` gitignor├®s.
- **Detection PUSH0 robuste** : scanner forward avec instruction-boundary tracking, pas juste `bytes[i-1] not in 0x60-0x7f` (donne faux positifs dans les immediates PUSH2/PUSH32). Vraie r├¿gle : `if b === 0x5f { push0; i++; } else if b >= 0x60 && b <= 0x7f { i += 1 + (b - 0x5f); } else { i++; }`. Le test `build-json-paris.test.ts` impl├®mente ce scanner.
- **KCC RPCs en prod** : `rpc-mainnet.kcc.network`, `kcc.drpc.org`, `kcc-rpc.com`. Block time ~6s, gas price 3 Gwei. `eth_chainId` = `0x141` = 321. NATIVE KCS, 18 decimals. Prix DefiLlama 6.33 USD (mai 2026).
- **DeployClient UX guard (commit `35f0434`)** : useEffect lit `localStorage.gm_impl_${chainKey}`. Si l'adresse match `/^0x[a-fA-F0-9]{40}$/` ET `eth_getCode` retourne `0x` (contrat vide on-chain, signe d'un deploy Shanghai PUSH0 revert), auto-clear localStorage + reset `setStep("gm")` + setError("On-chain bytecode is empty ÔÇö clear the cached GmOnChain address to deploy a fresh one"). Emp├¬che la re-saisie accidentelle d'un contrat cass├®. Important : les Factory/Impl cass├®s restent on-chain comme d├®chets (`eth_getCode = 0x`) et faussent l'inspection explorer.
- **Activation KCC factory (commit `a18c112`)** : `GM_FACTORIES.kcc = { address: "0x76edb44d846b6378519aeed5c9ee2bcabcd2c15a", chainId: 321 }`. Block 52727377, tx `0x2a8a7ee9...`. GmOnChain impl = `0xd741c65517f883cd2b4c7cfbda3da110e8b41675`, block 52727373, tx `0x71d2ca39...`. Wagmi `kcc: { id: 321, name: "KCC Mainnet", ... }` + `kcc` dans `chains[]` array + `[kcc.id]: http()` transport. `GM_CHAIN_NAMES.kcc = "KCC Mainnet"`. V├®rif prod : `/api/chains` retourne KCC `{key:"KCC", chainId:321, disabled:false, nativeSymbol:"KCS", rpcCount:3, explorerUrl:"https://explorer.kcc.io/en"}`.
- **D├®chets on-chain KCC** (pour tra├ºabilit├®, ne PAS cleanup) : `0x83dde2e0a15417c3d206a9c74827ef5a15194632` (factory initiale Paris OK + impl cass├®e, block 52716810), `0xf25ebed98426a4dc01e30a1e04ead7d3639579bd` (GmOnChain initial Shanghai PUSH0 reverted, block 52716806), `0x9cc14b976a713d388636cd8736b291ff4be3a1c3` (factory abandonn├®e #1, block 52725852, implementation pointe vers l'ancien GmOnChain cass├®). Tous retournent `eth_getCode = 0x`. Le UX guard dans DeployClient ├®vite que l'utilisateur les resaisisse par accident.
- **Prochaines cha├«nes pre-Shanghai ├á supporter** : identifier via `eth_getBlockByNumber("latest", false)` retour sans `baseFeePerGas` OU par leur activation Shanghai manuelle. KCC est la premi├¿re. Le pattern `PARIS_BUILD_CHAINS = new Set(["KCC"])` est con├ºu pour ├¬tre ├®tendu. Ajouter le `chainKey` au Set + bump version build Paris + red├®ployer.
- **Pre-London Ôëá pre-Shanghai** : KCC est pre-London (pas d'EIP-1559) ET pre-Shanghai (pas d'EIP-3855). Une cha├«ne peut ├¬tre London mais pas Shanghai (EIP-3855 a ├®t├® activ├®e s├®par├®ment). Toujours tester PUSH0 sur une nouvelle cha├«ne avant de d├®ployer avec un build Shanghai.
- **Wallet connect quirks KCC** : la v3 du `chain-switch.ts` (commit `cb6df36`) r├®sout les 3 probl├¿mes rencontr├®s sur KCC : (1) `wallet_switchEthereumChain` 4902 = "Unrecognized chain ID 0x141" ÔåÆ fallback `wallet_addEthereumChain`, (2) certains wallets wrappent le code dans `data.originalError.code` (pattern wagmi) ÔåÆ helper `getErrorCode(e)` lit `e.code` (number ou string) + `e.data?.originalError?.code`, (3) codes inconnus (-32000, -32603) ÔåÆ fallback add en dehors de 4001 (user reject) et 4902. Tests : `chain-switch.test.ts` 9/9.

### Gotchas WCORE Web ÔÇö FX fetch + GM contracts cross-user publication (2026-06-06, commit `e2e72d1`)

- **PreferencesProvider `fetch("/api/price/fx")` cass├® en dev/staging (audit C.2)** : `fetch()` avec URL relative n'aboutit pas en dev (web port 3000) ou staging (web port 3001) parce que l'API tourne sur un port diff├®rent (4000/4001). CORS bloque l'appel avant qu'il n'atteigne l'API. En prod, ├ºa marche par hasard (proxy Next.js ou m├¬me origin). **Fix** : utiliser `apiFetch("/api/price/fx", { signal: ctrl.signal })` (de `apps/web/lib/api.ts`) qui g├¿re les origins + cookies. Ajouter AbortController pour ├®viter `setState` sur composant unmount, et un `r.ok` check pour ne pas ├®craser le default `1.08` avec une r├®ponse 5xx. Fichier : `apps/web/components/PreferencesProvider.tsx`. Source : audit 2026-06-05 C.2 (consolid├® dans `docs/AUDIT.md`).
- **useGmContracts publication cross-user (audit C.3)** : `publishContracts(key, contracts)` (module-level) filait les `contracts` ├á TOUS les listeners via `for (const listener of contractListeners) listener(contracts)`. Si un refresh in-flight pour user A compl├®tait APR├êS que user B a mont├®, B h├®ritait des contracts de A. **Fix** : passer `key` au listener, listener check `publishedKey === cacheKey` avant `setContracts(nextContracts)`. Le `cachedKey` module-level + `cachedContracts` restent (lecture sync pour le montage initial), mais la publication ne touche que les listeners qui matchent. Fichier : `apps/web/hooks/useGmContracts.ts:13-67, 102-109`. Type : `type ContractsListener = (key: string, contracts: GmContractWithBalance[]) => void`. Source : audit 2026-06-05 C.3 (consolid├® dans `docs/AUDIT.md`).
- **Listener cleanup useEffect (rappel)** : `contractListeners.delete(listener)` dans le cleanup useEffect (ligne 108) est crucial ÔÇö sinon chaque mount d'un composant `useGmContracts` ajoute un nouveau listener et le cleanup ne se fait jamais (fuite m├®moire + listener stale qui setState un composant d├®mont├®). Si on passe ├á un contexte (`GmContext`), c'est le contexte qui poss├¿de le listener, pas le hook.

### Gotchas WCORE Web ÔÇö TokenIcon broken-image overlay (2026-06-06, commit `e0bead8`)

- **Chevauchement logo bugg├® sur tokens absents de spothq** : `TokenIcon` rendait TOUJOURS le cercle color├® de fallback (texte sur fond color├®) DERRI├êRE le `<img>`. Quand l'URL retournait 404 (fr├®quent pour tout token absent de spothq/cryptocurrency-icons : SOMI, MON, RealToken symbols sur Gnosis, la plupart des ERC-20 non-majeurs sur Base, HH, etc.), le navigateur affichait son ic├┤ne "image cass├®e" PAR-DESSUS le cercle color├®. C'est le "chevauchement de plusieurs infos" rapport├® par l'utilisateur.
- **Fix `imageBroken` state** : nouveau state `imageBroken`. Quand l'img d├®clenche `onError` (404) ou se charge avec `naturalWidth === 0` (SVG vide), `imageBroken` passe ├á `true` et le `<img>` n'est plus rendu. Seul le cercle color├® avec texte est visible. `useEffect([src])` reset `imageBroken` quand le src change (cascade vers le fallback suivant). Cascade `logoUrl ÔåÆ spothq ÔåÆ CMC ÔåÆ null` inchang├®e.
- **Tokens affect├®s** : tout token dont l'ic├┤ne n'est PAS dans spothq/cryptocurrency-icons (r├®f├®rentiel qui n'est plus mis ├á jour pour les tokens r├®cents). Spothq sert des SVG vides (`naturalWidth=0`) pour ETH, CELO, ARB, CRO, FRAX, IMX (cf. gotcha ant├®rieur 2026-05-09) ; retourne 404 pour les tokens plus r├®cents.
- **Bonus overrides SOMI/MON** : ajout d'overrides llamao + CMC pour les natives r├®centes (Somnia, Monad). llamao `rsz_somnia.jpg` / `rsz_monad.jpg` 200 OK ; CMC UCID 37637 / 30495 200 OK. Ne PAS utiliser l'ic├┤ne de cha├«ne (llamao) pour des tokens natifs non-listes chez spothq ÔÇö c'est un fallback acceptable pour les tokens long-tail. Source : audit 2026-06-05 post-fix (consolid├® dans `docs/AUDIT.md`).
- **Test de garde** : `token-icons.test.ts` couvre les overrides SOMI/MON + les CMC fallback IDs. Tests de garde contre retour ├á l'ancien comportement : `apps/web/__tests__/token-icons.test.ts` 6 tests verts.

### Gotchas WCORE Web ÔÇö Notifications spam/revival + Auth resilience (v0.2.31, 2026-05-26)

- **Scan notifs supprim├®es** : `prisma.notification.create` pour `scan_done`/`scan_degraded` retir├®. Vieilles notifs filtr├®es de toutes les requ├¬tes + cleanup one-shot au boot API. Fichier : `apps/api/src/gamification/notifications.ts`.
- **NotificationsBell stale closure** : `fetchNotifications` ne doit pas utiliser `isAuthenticated` directement (capture la valeur au montage via `useCallback([], ...)`) ÔÇö utiliser `isAuthenticatedRef` (ref synchronis├®e ├á chaque render). Sinon un fetch lanc├® en "authenticated" peut ├®craser le reset fait apr├¿s d├®motion en "ready".
- **Mark-read optimiste** : `markAllRead`/`markAsRead` posent `lastActionAt` avant l'appel API et appliquent l'update locale imm├®diatement. Si l'API ├®choue ÔåÆ `lastActionAt = 0` + `fetchNotifications()` pour resynchro serveur.
- **SSE snapshot overwrite** : apr├¿s un clic "mark all read", le SSE snapshot (60s poll) peut r├®├®craser `unreadCount` avec l'ancienne valeur si le `lastActionAt` n'est pas pos├® **avant** l'appel API (pas apr├¿s). Le guard est `Date.now() - lastActionAt.current > 5000`.
- **Affichage conditionn├® ├á l'auth** : `NotificationsBell` et `GmWithdrawNotification` ne fetch/affichent rien si `authStep !== "authenticated"`. `GmWithdrawNotification` passe `address=null` ├á `useGmContracts` si pas authentifi├®.
- **Access token 24h** : pass├® de 15min ├á 24h (`apps/api/src/auth.ts`, `ACCESS_TOKEN_TTL` + `maxAge` cookies). Le refresh token reste ├á 7j.
- **expired ÔåÆ ready** : quand le JWT expire et que wagmi reconnect, `authStep === "expired"` est promu en `"ready"` (affiche "Sign In" au lieu de "Connect Wallet"). Fichier : `apps/web/components/ConnectButton.tsx:101`.
- **hasCachedValue native price guard** : `hasCachedValue()` rejette les entr├®es cache o├╣ `native.balance > 0` mais `native.priceEur == null` ÔåÆ l'API re-scanne. Fichier : `apps/api/src/plugins/scan.ts:40-47`.

### Gotchas WCORE Web ÔÇö Stale cache + merge case-sensitivity (v0.2.30, 2026-05-26)

- **mergeChainResults case-insensitive** : `mergeChainResults()` dans `apps/web/components/scan-results.ts` utilise maintenant `.toLowerCase()` pour les cl├®s de Map. Avant v0.2.30, un r├®sultat `"solana"` (lowercase, ancien) et `"SOLANA"` (uppercase, frais) devenaient deux entr├®es distinctes et l'ancien avec `priceEur=null` survivait au rendu. Test : `replaces stale chain results case-insensitively` dans `apps/web/__tests__/scan-results.test.ts`.
- **Cache serveur `scan:result` + native sans prix** : `shouldCacheAssets()` refuse maintenant le cache quand `native.balance > 0` mais `native.priceEur == null`. Avant v0.2.30, un scan partiel SVM/Cosmos avec le prix natif absent (0 erreurs) empoisonnait le cache r├®sultat. Test : `does not cache positive native balances without a native price` dans `apps/api/src/scan-cache-policy.test.ts`.

### Gotchas WCORE Web ÔÇö Async Scan Resilience + BASE timeout (v0.2.25, 2026-05-21)

- **BASE scan timeout r├®solu (v0.2.25)** : per-chain timeout 90s via `Promise.race` + `ChainTimeoutError`. `SCAN_CHAIN_TIMEOUT_MS` (d├®faut 90000). Le timeout s'applique au scan async ET au batch scan. Les timeouts sont enregistr├®s dans les m├®triques via `metrics.recordChainTimeout(chain)`. La cha├«ne reste en "error" mais le job continue (les autres cha├«nes ne sont pas bloqu├®es). Fichier : `apps/api/src/plugins/scan.ts`.
- **Job TTL 3-guard cleanup** : 3 gardes de nettoyage des jobs async. Guard 1 : 10min sans progr├¿s (toutes les cha├«nes "pending") ÔåÆ `job_timeout: no chain progress after 10min`. Guard 2 : cha├«nes bloqu├®es en "scanning" > 2├ù `CHAIN_TIMEOUT_MS` (180s) avec `allStuck` (aucune cha├«ne "done" ou "error") ÔåÆ `job_timeout: chains stuck in scanning after 180s`. Guard 3 : hard cap `JOB_TTL_RUNNING_MS` (30min) kill tout job running quel que soit son ├®tat. Les jobs running avec progr├¿s NE sont JAMAIS supprim├®s. Les jobs done/error sont gard├®s 30min (`JOB_TTL_DONE_MS`) puis supprim├®s. Fichier : `apps/api/src/plugins/scan.ts`.
- **Partial cache writes (async scan)** : apr├¿s chaque cha├«ne compl├®t├®e dans le scan async, le r├®sultat est ├®crit dans le cache r├®sultat courant `scan:result:{address}:{chain}` via la registry. M├¬me si le job expire avant que toutes les cha├«nes soient finies, les donn├®es des cha├«nes d├®j├á scann├®es survivent dans le cache. Cache ├®crit selon `shouldCacheAssets()` pour ├®viter les r├®sultats utiles mais incomplets. Fichier : `apps/api/src/plugins/scan.ts`.
- **Phase instrumentation** : `console.log` par cha├«ne dans le scan async avec `tokens/pricedCount`, `cleanValue`, `discovery/balances/pricing/scan ms`. Permet de diagnostiquer les cha├«nes lentes (ex: BASE avec pricing cascade massive). Format : `[scan] CHAIN: N/M tokens (with balance/priced), clean=X.XXEUR, discovery=Xms, balances=Xms, pricing=Xms, scan=Xms`. Fichier : `apps/api/src/plugins/scan.ts`.
- **Cache key fix historique (scan_result: ÔåÆ scan: ÔåÆ scan:result)** : la cl├® de cache r├®sultat de scan est pass├®e de `scan_result:{address}:{chain}` ├á `scan:{address}:{chain}`, puis ├á `scan:result:{address}:{chain}` en v0.3.1 via la registry. Fichier : `apps/api/src/plugins/scan.ts`.
- **clearTimeout cleanup** : `clearTimeout(timer!)` explicite apr├¿s chaque `Promise.race` avec timeout. ├ëvite les fuites de timer. Le `!` est safe car le callback Promise s'ex├®cute synchronement avant le `await`.
- **Batch scan per-chain timeout** : le endpoint `POST /api/scan/batch` applique aussi le per-chain timeout via `Promise.race` + `ChainTimeoutError` sur `getEvmWalletsAssets`. M├¬me `CHAIN_TIMEOUT_MS` (90s) que l'async scan.
- **Circuit_open entries (async scan)** : les entr├®es de cha├«nes en circuit_open dans le job async utilisent maintenant un `ChainScan` complet avec tous les champs requis (`vm`, `degraded`, `fxRate`, `scriptVersion`, etc.) plut├┤t qu'un objet partiel.
- **SVM/Cosmos forceRefresh ne doit pas d├®sactiver le cache engine (2026-05-23)** : `forceRefresh=true` doit bypass uniquement le cache r├®sultat haut niveau (`scan:*`), pas les caches fallback internes non-EVM. Bug prod : `/api/scan/batch` passait `effectiveCache=undefined` ├á `getWalletAssets` pour SVM/Cosmos, donc sous saturation Solana (`HTTP 429/400`, `token accounts: no data`) les caches `native:*` et `ta:*` ne pouvaient pas pr├®server les balances et l'UI affichait 0 al├®atoirement. Fix : `getEngineCacheForScan(forceRefresh, vm, sharedCache)` retourne `sharedCache` pour `SVM`/`COSMOS` m├¬me en refresh forc├®, mais `undefined` pour EVM. Test de garde : `apps/api/src/scan-cache-policy.test.ts`.
- **Scan result cache registry (2026-05-23, renomm├® v0.3.1)** : apr├¿s un fix pricing/cache, ne pas bumper le cache navigateur. Le cache serveur Redis peut continuer ├á servir d'anciens r├®sultats. La cl├® r├®sultat courante est `scan:result:{address}:{chain}` via `cacheKey("scanResult", ...)`, ce qui ignore les vieux `scan:{address}:{chain}` et `scan:v2:{address}:{chain}`. Exemple historique : Gnosis RealT restait ├á ~0.35Ôé¼ avec `REALT_PRICE_UNAVAILABLE` tant que l'ancien `scan:*` ├®tait servi.
- **Cache policy SVM/Cosmos critiques (2026-05-23)** : `shouldCacheAssets()` refuse de persister les scans avec `token accounts: no data`, `balances fetch:`, `balances HTTP`, ou `native balance failed on all`. Ces erreurs signifient scan partiel/RPC failure, pas wallet vide. Les d├®grad├®s avec fallback cache et donn├®es utiles restent cachables.

### Gotchas WCORE Web ÔÇö Error analysis routine (v0.2.22, 2026-05-20)

- **Routine d'analyse des erreurs** : apr├¿s chaque d├®ploiement ou scan massif, ex├®cuter `powershell -File scripts/analyze-errors.ps1` pour identifier les RPCs morts, limites de block range, et timeouts. Le script query `/api/metrics/errors/detail`, filtre les faux positifs (BAL_CACHE, NO_PRICE), et classe les erreurs par type.
- **Types d'erreurs RPC fixables** :
  - **Block range limit** : ajouter `MAX_LOG_RANGE: <limit>` dans la config RPC de la cha├«ne. Le code `evm.ts` respecte d├®j├á ce param├¿tre.
  - **publicnode -32701** : retirer les endpoints `*.publicnode.com` qui bloquent `eth_call`/`eth_getLogs` sur le tier gratuit.
  - **DNS fail** : retirer les endpoints dont le domaine ne r├®sout plus.
  - **Timeout** : augmenter `TIMEOUT_MS` ou retirer les endpoints lents.
  - **Consensus fail** : souvent caus├® par des endpoints morts ÔÇö les retirer suffit.
  - **Cosmos REST 400/404/500** : probl├¿me infrastructure externe, pas fixable c├┤t├® config (endpoints publics cass├®s).
- **BAL_CACHE n'est pas une erreur** : `[BAL_CACHE] No activity since last scan` = cache hit du balance shortcut, pas un probl├¿me. Exclu des m├®triques d'erreur.
- **Pricing NO_PRICE n'est pas une erreur** : tokens long-tail sans liquidit├® ÔÇö comportement normal.

### Gotchas WCORE Web ÔÇö Scan/Cache/Redis optimizations (v0.2.22, 2026-05-20)

- **CacheStore `mget` requis** : l'interface `CacheStore` expose maintenant `mget<T>(keys: string[]): Promise<(T | undefined)[]>`. Les impl├®mentations `MemoryCacheStore` et `RedisStore` doivent l'impl├®menter. Redis utilise `client.mget()` pour un seul round-trip.
- **`CacheStats` dans `WalletAssetsCommon`** : chaque scan retourne `cacheStats: { hits, misses, stale, skipped }` pour monitoring. Export├® via `@wcore/core`.
- **`RpcHealthTracker` singleton** : filtre les endpoints RPC unhealthy (60s TTL). `recordSuccess(chain, endpoint)` / `recordFailure(chain, endpoint)` / `getHealthyEndpoints(chain, allEndpoints)`. Fichier : `packages/core/src/rpc/rpc-health.ts`.
- **Non-ERC20 skip cache** : `meta:skip:{chain}:{contract}` (24h TTL) ├®crit quand tous les RPCs retournent `execution reverted`. Le token est skip au prochain scan sans appel RPC.
- **Scan result cache (v0.3.1)** : cl├® courante `scan:result:{address}:{chain}` via la cache-key registry. **Corrig├® 2026-05-22** : avant le fix, `forceRefresh=true` ne bypassait PAS ce cache ÔÇö il contournait `effectiveCache` (cache interne engine) mais pas la boucle de cache check haut niveau lignes 141-152 de `scan.ts`. R├®sultat : un premier scan avec pricing cass├® stockait un r├®sultat nul ÔåÆ tous les `forceRefresh` suivants resservaient le r├®sultat pourri en 25ms (cache hit) au lieu de rescanner. **Fix** : `if (!forceRefresh)` ajout├® autour de la boucle de cache check dans les handlers sync (l.141) et batch EVM (l.331). Le handler non-EVM avait d├®j├á la garde. Commit `2a544fd`. Fichier : `apps/api/src/plugins/scan.ts`.
- **localStorage scan cache ÔÇö historique (2026-05-22, obsolete)** : l'ancien frontend avait son propre cache localStorage versionn├® (`WALLET_SCAN_CACHE_VERSION` dans `scan-cache.ts`) et n├®cessitait parfois un bump. Depuis post-v0.2.28, le cache navigateur portfolio est d├®sactiv├®. Pour un r├®sultat stale, v├®rifier l'API puis Redis `scan:result:*` et les caches engine, pas localStorage.
- **Intra-scan price cache** : `Map<string, Promise<PricingResult>>` partag├® entre workers `SCAN_CONCURRENCY`. Le premier worker qui price un token stocke la Promise ; les autres l'attendent. Passe via `opts.intraScanCache` dans `getEvmWalletAssets` ÔåÆ `priceToken` ÔåÆ `priceTokenCascade`.
- **Balance no-TX shortcut (1h)** : cl├® `bal_cache:{chain}:{address}`. Si aucun nouveau token + cache valide (<1h) ÔåÆ retourne les balances cach├®es imm├®diatement. Ne fait PAS de `eth_getLogs` ÔÇö le TTL 1h agit comme fen├¬tre d'inactivit├®. ├ëcrit fire-and-forget en fin de scan.
- **Per-chain timeout (90s)** : `Promise.race` avec `setTimeout` reject. Env `SCAN_CHAIN_TIMEOUT_MS` (d├®faut 90000). Error message = `chain_timeout:{chain} exceeded {ms}ms`. Metrics : `metrics.recordChainTimeout(chain)`.
- **`GET /api/metrics/errors`** : nouveau endpoint. Retourne `byType`, `byChain`, `circuits` (open/halfOpen/closedCount), `cache` (backend, hits/misses), `scanConcurrency`, `uptime`, `startedAt`. Plugin : `apps/api/src/plugins/metrics-plugin.ts`.
- **`priceNative` signature chang├®e** : ajoute `intraScanCache?: Map<string, Promise<any>>` en dernier param├¿tre. Tous les callers doivent ├¬tre mis ├á jour.
- **`PriceTokenCascadeOptions`** : ajoute `intraScanCache?: Map<string, Promise<PricingResult>>`. La cascade v├®rifie ce cache AVANT de lancer le pricing interne.
- **`dispatch.ts` `DispatchOptions`** : ajoute `intraScanCache?: Map<string, Promise<any>>`. Pass├® ├á `getEvmWalletAssets`.
- **Toujours passer `intraScanCache` dans `scan.ts`** : cr├®├® avant le scan pool, pass├® ├á chaque `getWalletAssets` call.
- **`EvmWalletToken` requiert `decimals`** : le shortcut balance cache doit inclure `decimals` dans les tokens retourn├®s, sinon TS ├®choue.
- **`WalletAssetPrice` requiert `valueEur`** : le shortcut balance cache doit calculer `valueEur` pour le native, sinon TS ├®choue.
- **148/148 tests core passent** apr├¿s toutes les modifications.

### Gotchas WCORE Web ÔÇö Audit triple v0.2.20 (2026-05-19)

- **RealT bulk registry (v0.2.20, 2026-05-19)** : `RealTPriceSource` ne fait plus d'appels per-contract. Un seul fetch `https://api.realtoken.community/v1/token` retourne 829 tokens ÔåÆ registry Map en m├®moire index├®e par tous les contrats connus (uuid, xDai, gnosis, ethereum). Miroir Redis sous `realt:registry:v2` (TTL refresh 6h, stale fallback infini). Lookup O(1). Les anciennes cl├®s `realt:price:{contract}` du cache permanent sont obsol├¿tes mais inoffensives (jamais lues). Supersede la note "RealT Cache Permanent Redis (2026-05-19)" ci-dessus.
- **RealT Woo fallback officiel (2026-05-23)** : `api.realtoken.community/v1/token` peut retourner 404 en prod. Fallback fiable ajout├® via `https://realt.co/wp-json/wc/store/v1/products?search=...`, cache Redis `realt:woo:{contract}` TTL 6h, matching strict/fail-closed. Ne jamais fallback DEX/GT pour un contrat RealT reconnu : les pools illiquides donnaient des prix faux. Validation prod Gnosis : 9 RealToken pric├®s, valeur RealT ~1083Ôé¼, cha├«ne Gnosis ~1088Ôé¼.
- **RPC centralis├®s GM/scan/scripts (2026-05-23)** : ne plus ajouter de map `CHAIN_RPCS` locale dans `apps/api`, `apps/web` ou `scripts`. La source unique est `@wcore/core` via `getRpcEndpoints()` / `getPrimaryRpcEndpoint()` (`packages/core/src/rpc/endpoints.ts`), aliment├®e par les configs `packages/core/src/chains/*.ts` + warmup Chainlist valid├® (`eth_chainId`, HTTPS, pas de templates, fallback statique). Incident d├®clencheur : deploy GM Ethereum `tx_not_found` car la map GM divergeait des RPC core. Les scripts GM (`audit-gm-consistency`, `recover-gm-history`) doivent importer `getRpcEndpoints()` et utiliser `DATABASE_URL`, jamais un endpoint DB/RPC hardcod├®.
- **deploy-staging sans db push destructif (2026-05-24)** : `scripts/deploy-staging.ps1` ne fallback plus vers `prisma db push --accept-data-loss`. Apr├¿s backup, si `prisma migrate deploy` ├®choue, le script stoppe avec erreur et affiche le backup. Corriger les migrations explicitement; ne pas r├®introduire de fallback automatique.
- **API unit helpers sans DB (2026-05-24)** : les helpers purs de `server.ts` (`validateChains`, `buildChainScan`, `validateCustomToken`) vivent dans `apps/api/src/server-helpers.ts`. Les tests unitaires comme `scan.test.ts` doivent importer ce module, pas `server.ts`, sinon ils d├®marrent Fastify/plugins et touchent Prisma m├¬me sans DB locale.
- **RealT stale Redis fallback bug (2026-05-19)** : le code ne chargeait `realt:registry:v2` que si `ts < 6h`. Si Redis avait un registry stale et que l'API RealToken ├®tait bloqu├®e c├┤t├® Railway, le registry ├®tait ignor├® ÔåÆ `isKnownRealTContract=false` ÔåÆ fallback DEX/GT ÔåÆ prix RealT faux (`6-14Ôé¼` au lieu de `~50Ôé¼`). Fix : charger le registry Redis m├¬me stale, tenter refresh, et si refresh ├®choue garder le stale actif + cooldown via `registryLoadedAt=Date.now()`. Test de garde : `source-adapters.test.ts` / `RealT source serves stale Redis registry when API refresh fails`.
- **Cascade short-circuit RealT (v0.2.20)** : `cascade.ts` court-circuite le main price cache pour les contrats reconnus par `realt.isKnownRealTContract(token)` ÔÇö sinon un prix DEX/GT poison├® stock├® ant├®rieurement reste coll├® pendant `staleMs` (1h par d├®faut). Si registry hit mais pricing fail ÔåÆ `REALT_PRICE_UNAVAILABLE` retourn├®, jamais fallback DEX/GT (pools illiquides garbage). Nouvelle interface `RealTSource extends TokenPriceSource` avec m├®thode obligatoire `isKnownRealTContract`.
- **Logos hors hot path pricing (v0.2.20)** : `priceToken` (`evm.ts:626`) n'await plus `resolveTokenLogoAsync`. Utilise `resolveTokenLogoCachedOrFallback` (Redis-only + sync fallback TrustWallet/CMC, **jamais HTTP**) et d├®clenche `prefetchTokenLogo` fire-and-forget. Map module-level `_prefetchInflight` d├®dupe les fetches concurrents pour un m├¬me `(chain, contract)` ÔÇö 30 scans simultan├®s = 1 HTTP. Premier scan d'un nouveau token = logo fallback ; scan suivant = logo Blockscout/DexScreener. UX d├®grad├®e 1 scan, gain perf 100-200ms/token cold.
- **Stripe retir├® (v0.2.21)** : billing plugin, checkout, webhook, portal, et toutes les variables STRIPE_* supprim├®es. `getUserPlan()` ne v├®rifie plus `planExpiresAt` ÔÇö tout le monde est sur le plan gratuit. `invalidateUserPlan` reste pour le cache Redis mais n'est plus appel├® par des webhooks. `user.plan` et les colonnes Stripe (`stripeCustomerId`, `stripeSubscriptionId`, `subscriptionStatus`) retir├®es du sch├®ma Prisma.
- **Admin scam-override : ADMIN_TOKEN obligatoire (v0.2.20)** : `apps/api/src/plugins/admin.ts` route `scam-override` ne fait plus de fallback `req.user.address === platform_owner`. Un cookie vol├® du owner ne peut plus ├®crire dans la table scam. **`ADMIN_TOKEN` est l'unique porte d'entr├®e** ÔÇö v├®rifier qu'il est set en prod Railway.

### Gotchas WCORE Web ÔÇö Home page restructure + RealT cache (v0.2.17, 2026-05-19)

- **Home page ÔÇö 2 fichiers ├á mettre ├á jour** : `page.tsx` (server component) contient le hero + MiniCards. `HomePageClient.tsx` contient le formulaire + les 3 encarts feature en bas. Ne PAS ajouter d'encarts dans `page.tsx` ÔÇö ils sont dupliqu├®s. Pattern : `page.tsx` = hero uniquement, `HomePageClient.tsx` = formulaire + feature cards en bas.
- **Deep scan checkbox** : positionn├®e juste apr├¿s le `<ChainSelector>` dans `HomePageClient.tsx`, PAS en bas des options avanc├®es.
- **Donn├®es ├á synchroniser** : quand le nombre de cha├«nes/GM/rules change, mettre ├á jour : `page.tsx` (MiniCards + subtitle), `HomePageClient.tsx` (3 encarts), `about/page.tsx` (sous-titre + features), `SidebarLayout.tsx` (footer), `ROADMAP.md` (intro + ├®tat actuel).
- **RealT Cache Permanent Redis (2026-05-19)** : `RealTPriceSource` utilise maintenant un cache Redis permanent (`realt:price:{contract}`) sans TTL. Les prix ne sont mis ├á jour QUE quand l'API r├®pond 200 avec un prix valide. Quand l'API bloque Railway (403/401/timeout), le prix cach├® Redis est servi ind├®finiment. Circuit breaker pr├®serv├® (5 ├®checs ÔåÆ skip API 30min). Hydratation m├®moire au premier acc├¿s. `evm.ts` passe `opts.cache` (Redis) au constructeur `RealTPriceSource(cache)`.
- **Single-RPC chains enrichies (2026-05-19)** : 54/62 cha├«nes single-RPC ont re├ºu des RPCs additionnels depuis chainlist.org. 8 restent single-RPC (aucun RPC dispo sur chainlist) : CROSS_MAINNET, ETHO_PROTOCOL, FOGO, HORIZEN_EON, LAYERAI, MITOSIS, RIVALZ, STACK. CYSIC (rpc-evm.cysic.xyz), NUMINE (subnets.avax.network/numi/mainnet/rpc), MOCA_CHAIN (moca-mainnet.drpc.org) enrichis le 2026-05-27. Surveiller ces 8 ÔÇö si leur RPC unique est down, la cha├«ne est enti├¿rement cass├®e.
- **RealT Circuit Breaker (2026-05-18)** : `api.realtoken.community` bloque les IPs Railway (401/403/503). `RealTPriceSource` a maintenant un circuit breaker : apr├¿s 5 ├®checs cons├®cutifs, skip RealT pendant 30 min. Timeout r├®duit ├á 3s. Logs silencieux (`console.log`). Les tokens RealT valides re├ºoivent leurs prix (~$50), les REG/governance (401) sont skip├®s.
- **SCAN_CONCURRENCY r├®duit ├á 30 (2026-05-18)** : d├®faut pass├® de 50 ├á 30 dans `apps/api/src/plugins/scan.ts` pour r├®duire la saturation RPC free-tier. 62/180 cha├«nes ont un seul RPC endpoint ÔÇö les ├®checs sont in├®vitables pour ces cha├«nes quand le RPC est down.
- **Node 22 build types ÔÇö `RequestInfo` absent (2026-05-18)** : le Docker build API ex├®cute `pnpm --filter "@wcore/core" build` donc compile aussi les tests core. Dans Node 22, `RequestInfo` n'est pas expos├® par les types utilis├®s dans le container, m├¬me si `Request`, `Response`, `RequestInit` existent. Pour typer un `fetchImpl` de test, utiliser `string | URL | Request` au lieu de `RequestInfo | URL`. Repro : `explorer-discovery.test.ts` cassait le build Railway; fix valid├® par `pnpm --filter @wcore/core build` puis redeploy API.
- **Railway Prisma baseline (2026-05-13)** : l'API Railway d├®marre via `apps/api/start-production.sh`. Le script tente `prisma migrate deploy`; uniquement si Prisma retourne `P3005` (DB non vide sans historique), il marque les migrations existantes comme appliqu├®es avec `prisma migrate resolve --applied`, puis relance `migrate deploy`. Ne pas r├®introduire `db push --accept-data-loss` en prod.
- **Backlog UI produit inspir├® dashboards crypto (2026-05-13)** : ne pas impl├®menter maintenant, mais garder pour les ├®volutions : navigation interne des r├®sultats (`Overview`, `Wallets`, `Chains`, `Tokens`, `Activity/Scans`, `GM`), vue globale `All Tokens`, vue globale `Wallets`, topbar contextuelle wallet (`Updated`, currency, refresh, add wallet, export), sidebar avec ic├┤nes SVG au lieu d'emojis, `SettingsBar` en segmented control, tables tokens triables/filtrables, et `capture/demo mode` pour screenshots. Ne pas promettre une vraie transaction history avant cadrage multi-VM EVM/SVM/Cosmos.
- **Streak warning (2026-05-14)** : `ProfileClient.tsx` affiche un badge jaune `Streak Warning` en haut de l'onglet Overview quand `gmStreak > 0` mais aucun GM (off-chain ou on-chain) n'a ├®t├® fait aujourd'hui. D├®tection via `/api/auth/me` qui retourne `gmOffChainToday` et `gmOnChainToday`. Le badge affiche `"X-day streak at risk. Say GM off-chain or on any chain to keep it alive."`.
- **Server split en plugins (2026-05-15)** : `apps/api/src/server.ts` (1262 LOC ÔåÆ ~360 LOC) d├®coup├® en 4 plugins : `scan.ts`, `admin.ts`, `wallet.ts`, `chains.ts`. Chaque plugin re├ºoit ses d├®pendances via une interface typ├®e (`ScanPluginDeps`, `AdminPluginDeps`, etc.). Les helpers (`validateChains`, `buildChainScan`, etc.) restent dans `server.ts` et sont r├®-export├®s pour les tests.
- **SCAN_CONCURRENCY env (2026-05-15)** : `apps/api/src/plugins/scan.ts` utilise `Math.max(1, Math.floor(Number(process.env.SCAN_CONCURRENCY) || 50))`. Ne jamais laisser `SCAN_CONCURRENCY=0` ou n├®gatif ÔåÆ boucle infinie.
- **useEffect import manquant = build fail (2026-05-15)** : Next.js 16 build production v├®rifie les imports m├¬me avec `use client`. Si un hook est utilis├® sans ├¬tre import├® de `react`, le build ├®choue avec `ReferenceError: useEffect is not defined`. Le dev local (Turbopack) ne le d├®tecte pas toujours. **Toujours v├®rifier** : `grep -r "useEffect" --include="*.tsx" apps/web | grep -v "from.*react"` apr├¿s un edit.
- **"use client" requis pour tout composant avec hooks (2026-05-15)** : `TokenIcon.tsx` et `ValueDistribution.tsx` utilisaient `useState` sans `"use client"` ÔåÆ crash au prerender `/_not-found`. **R├¿gle** : tout fichier `.tsx` qui utilise un hook React DOIT avoir `"use client";` en premi├¿re ligne.
- **SidebarLayout useState misuse (2026-05-15)** : `useState(() => { ... })` utilis├® comme un effet (sans setter) ÔåÆ crash au prerender. Remplac├® par `useEffect(() => { ... }, [])`.
- **next.config ignoreBuildErrors (2026-05-15)** : `typescript: { ignoreBuildErrors: true }` masquait les erreurs TS au build production. Retir├® pour d├®tecter les probl├¿mes t├┤t.
- **Reown Project ID par d├®faut (2026-05-15)** : `apps/web/Dockerfile` d├®finit `NEXT_PUBLIC_WC_PROJECT_ID=3090760ada2bf4a459a27506fcdc16ec` par d├®faut. Sans ├ºa, WalletConnect/Coinbase Wallet sont d├®sactiv├®s avec un warning au build.
- **ESLint no-unused-vars sur les hooks (2026-05-15)** : les variables de boucle `for (const chain of openCircuits)` non utilis├®es dans `scan.ts` d├®clenchent `@typescript-eslint/no-unused-vars`. Fix : `for (let i = 0; i < openCircuits.length; i++)` ou pr├®fixer `_chain`.
- **Docker cache bust (2026-05-15)** : Railway cache les layers Docker. Pour forcer un rebuild, changer un commentaire dans le Dockerfile (`# CACHE_BUST=...`) ou modifier `pnpm-workspace.yaml`.
- **railway.json deploy pattern (2026-05-15)** : `scripts/deploy.ps1` modifie temporairement `railway.json` pour pointer vers le bon Dockerfile, puis le restaure. Ne jamais lancer deux d├®ploiements en parall├¿le (API + Web) ÔÇö ils se battent sur `railway.json`.
- **useSearchParams() = Suspense required (2026-05-15)** : Next.js 16 exige que `useSearchParams()` soit dans un Suspense boundary au niveau page. **Solution** : lire `searchParams` dans le server component `page.tsx` (`async function Page({ searchParams })`) et passer la valeur en prop au client component. Ne PAS utiliser `useSearchParams()` dans un client component rendu directement par une page.
- **Header global WCORE (2026-05-15)** : le header visible sur toutes les pages est `TopBar` via `SidebarLayout`, PAS `NavHeader`. Toute notification globale (ex: GM withdrawable tips) doit ├¬tre ajout├®e dans `apps/web/components/TopBar.tsx`. `NavHeader` est legacy/non mont├® dans le layout actuel.
- **GM status UI sans localStorage (2026-05-15)** : pour `/gm`, Header GM et notifications fees, ne pas ajouter de nouvelle d├®pendance ├á `localStorage`. Utiliser API/hook (`useGmContracts`, `/api/gm/status`) et ├®v├®nements runtime (`wcore-gm-done` avec `detail.chain`). Les tuiles GM doivent ├®couter l'├®v├®nement et ne flipper que si `detail.chain` matche leur `chain.key`.
- **GM withdraw notification flicker (2026-05-15)** : `useGmContracts` ne doit JAMAIS vider `contracts` ├á `[]` pendant un refresh ÔÇö les donn├®es existantes restent visibles. `GmWithdrawNotification` garde le dernier count non-z├®ro en `useRef` pour ├®viter que la notif disparaisse pendant les re-fetchs.
- **GM contracts source unique (2026-05-15)** : `/api/gm/my-contracts` renvoie maintenant les balances `creatorBalance`/`platformBalance` avec fallback multi-RPC via `readGmContractBalances()`. Le frontend ne doit PAS refaire un fan-out N appels `/api/gm/contracts/:id/balance` pour construire l'├®tat global. `useGmContracts` est le hook partag├® pour Header, `/gm`, `/profile?tab=gm-contracts` et wallet `ChainCard`.
- **GM contracts multi-par-chain (2026-05-15)** : `contractsByChain` est un `Map<string, GmContractWithBalance[]>`, pas un contrat unique. Une cha├«ne peut avoir plusieurs contrats withdrawables (creator/platform). `/gm` et `ChainCard` doivent mapper tous les contrats pertinents, sinon un `Fees Earned` visible dans `/profile` peut dispara├«tre de `/gm`.
- **GM Contracts profile duplicates (2026-05-26)** : prod peut contenir des rows `gm_contracts` avec variantes de casse (`base` et `BASE`) pour le m├¬me contrat, car certains flows historiques normalisaient en uppercase et d'autres en lowercase. `/api/gm/my-contracts` doit d├®dupliquer par `(chainKey.toLowerCase(), contractAddress.toLowerCase())`, pas par `id`, et fusionner `creatorBalance` + `platformBalance` sur une seule carte. Pour le platform owner, masquer les contrats `role="platform"` sans `platformBalance` retirable, sinon le profil affiche des cartes confuses type `BASE PLATFORM` sans bouton.
- **GM status chainKey casing (2026-05-26)** : `/api/gm/status` doit normaliser les `chainKey` en lowercase avant de fusionner `gm_contracts` et `onchain_gms`. Sinon `gm_contracts.chainKey="base"` + `onchain_gms.chainKey="BASE"` cr├®e deux entr├®es, et la page `/gm` lit `base.gmDone=false` ÔåÆ affiche `Say GM` malgr├® un GM fait aujourd'hui. Test : `apps/api/src/gamification/gm-status.test.ts`.
- **GM Profile current vs best streak (2026-05-26)** : le breakdown Profile par cha├«ne doit d├®river de `onchain_gms` et distinguer `streak` courant de `bestStreak`. Une cha├«ne avec runs s├®par├®s (ex: `METAL_L2` 8j puis 2j) doit afficher `current 2d ┬À best 8d`, tout en gardant les points historiques (`10 GM ┬À 87 pts`). Si le dernier GM est plus vieux qu'hier, `streak` courant doit tomber ├á 0. Test : `apps/api/src/gamification/gm-points.test.ts`.
- **GM on-chain canonical chainKey (2026-05-26)** : les nouvelles ├®critures `/api/gm/onchain` et les rebuilds doivent stocker les `chainKey` en uppercase c├┤t├® DB (`BASE`, `METAL_L2`, etc.), mais les r├®ponses API UI peuvent normaliser en lowercase. Ne pas m├®langer les deux niveaux. `rebuildChainStreakFromOnchain()` doit scanner les logs par chunks pour les RPCs ├á range limit├®e (`CYBER`, `OPENLEDGER`, `STABLE`) et garder un `gmStreak` existant plus ├®lev├® si un rebuild partiel ne couvre pas tout l'historique.
- **GM chainKey canonical (harmonis├® 2026-05-26)** : forme DB canonique = **UPPERCASE** (align├®e sur le registre core). R├¿gles ├á respecter pour TOUTE ├®volution GM :
  - **├ëcrire** un `chainKey` en DB (`gm_contracts`, `onchain_gms`, `user_chain_gms`) ÔåÆ toujours via `canonicalChainKey()` de `@wcore/shared` (jamais la valeur client brute, jamais une cl├® `GM_FACTORIES` lowercase telle quelle).
  - **R├®soudre une factory** ÔåÆ `getFactory()`/`getFactoryAddress()` de `@wcore/shared` (case-insensitive). Ne JAMAIS indexer `GM_FACTORIES[chainKey]` en direct : ses cl├®s sont lowercase et l'acc├¿s est case-sensitive.
  - **Matcher un `chainKey`** dans une requ├¬te Prisma GM ÔåÆ `{ chainKey: { equals: canonicalChainKey(x), mode: "insensitive" } }` (et `findFirst`, pas `findUnique`, sur le composite). C'est ce qui permet de retrouver les rows legacy lowercase **sans migration destructive** : la convergence se fait ├á l'├®criture (writes canoniques) + lookups insensibles. Le dedup read-time (`prepareMyContractsResponse`, `buildGmStatusResponse`) reste le filet d'affichage.
  - **Anti-replay** `OnchainGm @@unique([chainKey, txHash])` : l'unique composite est case-sensitive en Postgres, donc le pr├®-check de replay dans `/api/gm/onchain` est volontairement `mode: "insensitive"` pour bloquer la resoumission d'un GM enregistr├® avant la canonicalisation. Ne pas le repasser en findUnique.
  - **Rebuild/self-heal** `rebuildChainStreakFromOnchain()` doit aussi faire son propre guard case-insensitive par `txHash` avant d'ins├®rer. Depuis `fa17a10`, il pre-fetch les `OnchainGm.txHash` existants pour `(userId, chainKey)` et skip les hashes d├®j├á connus. Ne pas revenir ├á une idempotence bas├®e uniquement sur `@@unique([chainKey, txHash])`, sinon rows legacy lowercase + writes uppercase peuvent double-cr├®diter le score.
  - La r├®solution RPC (`getRpcEndpoints`/`getPrimaryRpcEndpoint`) est d├®j├á case-insensitive (uppercase interne) ÔÇö s├╗re quelle que soit la casse pass├®e.
- **GM action button sizing (2026-05-15)** : sur `/gm`, `Connect to Deploy`, `Coming Soon`, `Deploy`, `Say GM` et `GM Done` doivent utiliser la m├¬me classe visuelle (`h-9 flex items-center justify-center`) pour ├®viter les diff├®rences de taille entre cards.
- **fetchNativePrice fallback 2000 (2026-05-27)** : `fetchNativePrice()` dans `useOnChainGm.ts` utilisait `fetch()` brut avec `return 2000` comme fallback. Si le CORS ├®chouait ou l'API timeout c├┤t├® navigateur, le catch retournait $2000 (prix ETH) ÔåÆ tip = $0.05/$2000 = 0.0000255 ETH au lieu de ~0.000025 ETH sur Polygon. **Fix** : `apiFetch` (g├¿re CORS + cookies) + throw au lieu de fallback. **R├¿gle** : ne JAMAIS utiliser `fetch()` brut pour les API internes ÔÇö toujours `apiFetch` qui g├¿re CORS et le JWT cookie.
- **GM balance threshold (2026-05-27)** : le filtre `/api/gm/random` v├®rifie `balanceUsd >= 0.10` (tip $0.05 + gas margin). Le seuil $0.05 initial ne couvrait que le tip sans la marge gas. Les cha├«nes avec gas cher (Ethereum, Arbitrum) n├®cessitent parfois > $0.10. Le threshold est un compromis ; le tx ├®choue gracieusement si insuffisant.
- **gm-storage.ts centralis├® (2026-05-27)** : les helpers localStorage GM (`lsGmDone`, `lsDeployed`, `lsContractDeployed`, `lsSetContractDeployed`, `lsSetGmDone`, `lsGetBalance`, `lsSetBalance`) sont centralis├®s dans `apps/web/lib/gm-storage.ts`. Ne plus acc├®der ├á `localStorage` directement pour les cl├®s GM (`wc_gm_*`, `gm_bal_*`).
- **GM_PLATFORM_OWNER shared (2026-05-27)** : l'adresse platform owner (`0x17d5187...d080`) est export├®e depuis `packages/shared/src/factories.ts` comme `GM_PLATFORM_OWNER`. Ne plus hardcoder dans les composants web.

### Gotchas WCORE Web ÔÇö Audit sprint v0.2.8 (2026-05-17)

- **SIWE Expiration Time + Chain ID obligatoires (2026-05-17)** : `auth.ts` login retourne `siwe_expiration_missing` ou `siwe_chain_id_missing` si la ligne EIP-4361 correspondante est absente. Tout client SIWE doit donc inclure les deux. `chainId` doit aussi ├¬tre positif (`> 0`). Bypass via lignes optionnelles n'est plus possible.
- **PRICING_CACHE_MAX_ENTRIES env (2026-05-17)** : `MemoryPricingCache` (`packages/core/src/pricing/types.ts`) a maintenant un cap LRU. Default 20k entries par map (prices + markers). Override via env si le profil pricing utilise un vocabulaire plus large ou si on suspecte une fuite (`PRICING_CACHE_MAX_ENTRIES=50000`).
- **GT_THROTTLE_MAX_CALLS / GT_THROTTLE_WINDOW_MS env (2026-05-17)** : `GeckoTerminalPriceSource` throttle est configurable. Defaults 40/60s. Si `SCAN_CONCURRENCY > 1` cause de la starvation (un scan vide le budget GT pour les autres), bump ├á `40 ├ù SCAN_CONCURRENCY`. Mind GT free tier ~30/min ÔÇö d├®passer 60 risque des 429.
- **Single-RPC chains enrichies (2026-05-19)** : 54/62 cha├«nes single-RPC ont re├ºu des RPCs additionnels depuis chainlist.org. 8 restent single-RPC (aucun RPC dispo sur chainlist) : CROSS_MAINNET, ETHO_PROTOCOL, FOGO, HORIZEN_EON, LAYERAI, MITOSIS, RIVALZ, STACK. CYSIC (rpc-evm.cysic.xyz), NUMINE (subnets.avax.network/numi/mainnet/rpc), MOCA_CHAIN (moca-mainnet.drpc.org) enrichis le 2026-05-27. Surveiller ces 8 ÔÇö si leur RPC unique est down, la cha├«ne est enti├¿rement cass├®e.
- **Startup WARN single-RPC chains (2026-05-17)** : au boot de `apps/api`, un log `[startup] N EVM/SVM chains have <2 RPC endpoints` liste les cha├«nes single-endpoint. Ne pas l'ignorer si la liste grandit : la r├¿gle consensus `votes*2 > total` ne peut pas atteindre la majorit├®.
- **ScanRequestBodySchema.customTokens cap 100 (2026-05-17)** : `apps/api/src/schemas.ts` cap `customTokens` ├á 100 entr├®es sur `POST /api/scan`. Au-dessus ÔåÆ `400 invalid_body`. C'est un DoS bound c├┤t├® serveur ; le client doit pr├®-trier.
- **ScanShareBodySchema.expiresAt = ISO datetime (2026-05-17)** : `POST /api/scans/:id/share` exige `expiresAt` au format `z.string().datetime({ offset: true })` (ex : `2026-06-01T00:00:00Z`). Plus de `new Date('garbage')` qui atteint Prisma.
- **user_plan cache Redis (2026-05-17)** : `getUserPlan` cache la r├®solution dans `sharedCache` sous `user_plan:{userId}` (TTL 5 min). `invalidateUserPlan(userId)` expos├® pour les webhooks. Si un autre code change `user.plan` (admin override, migration, etc.) ÔåÆ appeler `invalidateUserPlan` aussi, sinon stale jusqu'├á 5 min.
- **Rate-limit per-address pr├®-auth (2026-05-17)** : `checkRateLimit` accepte un `subKey`. `/api/auth/nonce` et `/api/wallets/nonce` utilisent le `query.address` comme subKey. Donc une flood pour la m├¬me adresse ÔåÆ 30/min total (toutes IP confondues). Un attaquant rotant les addresses peut toujours scale, mais ne peut plus targetter une adresse sp├®cifique au-del├á du bucket d├®di├®.
- **CI security job (2026-05-17)** : `.github/workflows/ci.yml` ajoute un job `security` parall├¿le : `pnpm audit --prod --audit-level=high` + `gitleaks-action@v2` sur full history. Failures bloquent le merge. Si gitleaks remonte du legacy ÔåÆ option soit fix soit `.gitleaksignore` en root.
- **Fire-and-forget discovery writes (2026-05-17)** : `packages/core/src/engines/evm.ts` ne `await` plus les writes discovery cache (token list + block cursor). Gain ~4-8ms/scan. Test `discoverTokensForWallet survives cache write failure` garde l'invariant (les `.catch()` swallowing).
- **Polygon zkEVM GM detection (2026-05-15)** : le contrat GM expose `creator()`, pas `owner()`. Le fallback frontend on-chain doit appeler selector `0x02d05d3f` (creator), jamais `0x8da5cb5b` (owner). `/api/gm/status` doit matcher par `ownerId` OU `creatorAddress`/adresse wallet.
- **Activation GM cha├«ne ÔÇö checklist compl├¿te (2026-05-15, mise ├á jour 2026-06-04)** : ajouter la factory dans `packages/shared/src/factories.ts` uniquement. `ChainCard`, `/gm`, `/dev/deploy`, `useOnChainGm` et `useGmContracts` doivent d├®river l'├®tat via `getFactory()` / `getActiveFactoryChains()` ; les RPCs doivent venir de `@wcore/core`, jamais d'une map GM locale. V├®rifier que la cha├«ne existe dans `wagmi.ts` si elle n├®cessite un switch wallet, et que le chainId match `eth_chainId`.
- **X engagement ÔÇö NE JAMAIS liker nos propres tweets/r├®ponses (2026-05-15)** : Playwright clique souvent le mauvais bouton si on utilise un s├®lecteur global (`button[data-testid="like"]`) sur un thread. **R├¿gle absolue** : cibler l'article exact par `status_id`, v├®rifier que son texte/auteur contient l'utilisateur externe attendu et ne contient PAS `@WCORExyz`, puis cliquer uniquement le bouton like dans cet article. V├®rifier ensuite que le bouton devient `data-testid="unlike"`. Ne jamais suivre automatiquement sans v├®rifier que le bouton `data-testid="follow"` existe et n'est PAS d├®j├á `following`. R├®ponses X : pas d'em dash (`ÔÇö`), pas de template, pas de promesse de call, ton utile et personnalis├®.
- **CM X patterns import├®s de wobblhash (2026-05-17)** : pour @WCORExyz, r├®utiliser seulement les patterns op├®rationnels, pas l'identit├® @wobblhash. Session safe = lecture notifications/feed/search, s├®lection de 5 cibles, max 3 replies externes de qualit├®, 1 mention `@WCORExyz` sauf demande explicite, skip si aucune cible propre. Toujours v├®rifier avant/apr├¿s publication, logger cible + URL + d├®cision, et stopper si X affiche une alerte d'automatisation. Playwright X : utiliser Chrome CDP persistant, `keyboard.type`, ciblage exact par `status_id`, jamais de s├®lecteur global sur un thread.
- **X composer Playwright peut perdre la premi├¿re ligne (2026-05-17)** : apr├¿s ouverture d'un reply composer via X, un overlay peut intercepter le clic/focus. M├¬me avec `keyboard.type`, la premi├¿re ligne peut ├¬tre aval├®e et laisser seulement `.` + le reste du message. **R├¿gle** : avant de cliquer `Reply`, lire le texte r├®el du composer (`innerText`/DraftEditor), v├®rifier qu'il contient la premi├¿re phrase attendue, sinon clear + retaper. Ne jamais retenter apr├¿s publication si le contenu principal est visible, pour ├®viter un doublon spam.
- **X reply Playwright m├®thode fiable (2026-06-06)** : utiliser Chrome CDP d├®di├® `9224` + profil `%USERPROFILE%\chrome-x-interactions-profile`. Flow valid├® : (1) ouvrir le thread exact, (2) v├®rifier aucun article visible ne contient `WCORExyz`/`@WCORExyz`, (3) cibler l'article par texte/statusId, (4) cliquer son `[data-testid="reply"]`, (5) focus DOM direct sur le dernier `[data-testid="tweetTextarea_0"]` via `element.focus()` + `Range`/`Selection`, (6) vider avec `Control+A` + `Delete`, (7) saisir avec vraie `keyboard.type(..., {delay:25})` (PAS `insertText`, qui modifie le DOM sans alimenter l'├®tat React/X), (8) relire le draft complet, (9) cliquer via DOM le dernier bouton visible `tweetButton`/`tweetButtonInline` ou texte `R├®pondre` avec `aria-disabled !== "true"`, (10) recharger le thread et v├®rifier la reply dans les articles. `Ctrl+Enter` et `force:true` peuvent fermer le composer sans publier ou laisser X d├®synchronis├®.
- **X duplicate reply check (2026-05-18)** : avant toute reply, v├®rifier que @WCORExyz n'a pas d├®j├á r├®pondu au m├¬me thread. Ne pas seulement chercher le texte du nouveau draft : une ancienne r├®ponse diff├®rente peut d├®j├á exister dans le thread ou dans `https://x.com/WCORExyz/with_replies`. V├®rifier les articles visibles du thread pour auteur `@WCORExyz`; si une r├®ponse WCORE existe d├®j├á, skip sauf instruction explicite de follow-up. Repros : @Wezx777 avait d├®j├á une reply WCORE du 2026-05-13; @JonaWeb3 `2056080023048577390` a re├ºu un doublon car le script ne cherchait que le nouveau draft. Replies supprim├®es.
- **X Reply button masked by overlay (2026-05-17, mis ├á jour 2026-06-06)** : X peut laisser le bouton `Reply` visible mais intercept├® par `#layers`/`data-testid="mask"`, surtout via `intent/post`. Ne pas cliquer le textarea avec Playwright si le mask intercepte. Focus DOM direct + `keyboard.type` est plus fiable. Pour publier, ne pas se fier ├á `Ctrl+Enter` : s├®lectionner le bouton `R├®pondre` activ├® dans le DOM et appeler `button.click()` depuis `page.evaluate()`. Toujours v├®rifier ensuite le thread cible et `with_replies` si le thread ne montre pas encore la r├®ponse.
- **Native symbols ÔÇö source unique `chain-native-symbols.json` (2026-05-17)** : `getNativeSymbol()` dans `useGmContracts.ts` et `CreatorClient.tsx` lit depuis `apps/web/lib/chain-native-symbols.json` (g├®n├®r├® depuis `@wcore/core` configs). **Ne JAMAIS** ajouter un dictionnaire hardcod├® de symboles natifs ÔÇö le JSON est la source unique. R├®g├®n├®rer avec `npx tsx -e "const {chains}=require('./packages/core/src/chains/index.ts');const map={};Object.values(chains).forEach(c=>{map[c.key.toLowerCase()]=c.CHAIN?.NATIVE_SYMBOL||'NATIVE'});console.log(JSON.stringify(map,null,2));"` > `apps/web/lib/chain-native-symbols.json`. Bug initial : OpenLedger affichait "ETH" au lieu de "OPEN" car le dict hardcod├® n'├®tait pas ├á jour.
- **GM status DB/on-chain reconciliation (2026-05-17)** : si une tx GM on-chain r├®ussit mais le POST `/api/gm/onchain` ├®choue, `/api/gm/status` reste `gmDone=false` et `/gm` peut afficher `Say GM` malgr├® un event on-chain. Ne PAS corriger par seed one-off. Le fix permanent est dans `useGmChain`: quand DB retourne `deployed=true` + `gmDone=false`, appeler `/api/gm/status-onchain?chain=...&address=...` et utiliser `chainGmDone`. Test de garde : `apps/web/__tests__/gm-status-reconcile.test.ts`. Repro initial : Lisk tx `0xe65dccd7b53f7fa20e718e3ac577d52abab3396ce7e032b179784f1e95403692`.

### Gotchas WCORE Web ÔÇö Audit sprint v0.2.9 (2026-05-17)

- **API_URL source unique `getApiUrl()` (2026-05-17)** : `apps/web/lib/api.ts` exporte `getApiUrl()` qui retourne `process.env.NEXT_PUBLIC_API_URL ?? DEFAULT_API_URL`. **Toujours** utiliser `import { getApiUrl } from "@/lib/api"` suivi de `const API_URL = getApiUrl();` dans les composants. Ne JAMAIS ├®crire `typeof window !== "undefined" ? (process.env.NEXT_PUBLIC_API_URL ?? ...) : ...` inline. 23 fichiers consolid├®s, plus aucun inline restant.
- **Catchs non-triviaux ÔåÆ `console.error` (2026-05-17)** : tout bloc `catch` qui swallow une erreur r├®seau (fetch API, RPC, gamification, SSE) doit inclure `console.error("context:", _e)` pour faciliter le debugging. Les catchs triviaux (localStorage, clipboard, cache fire-and-forget, retry loops) peuvent rester silencieux. 48 blocs avec `console.error` dans la codebase.
- **SCAN_CONCURRENCY align├® code/doc (2026-05-17)** : d├®faut **50** dans `apps/api/src/plugins/scan.ts` ET dans `AGENTS.md`. Les deux doivent rester synchronis├®s. Configurable via env `SCAN_CONCURRENCY`.
- **`catch (_e)` convention (2026-05-17)** : quand le `cause` n'est pas utilis├® dans le corps du catch, le pr├®fixer par `_` (`catch (_e)`) pour satisfaire `preserve-caught-error` sans trigger `no-unused-vars`. Le `_` prefix est ignor├® par la r├¿gle `argsIgnorePattern: "^_"`.
- **`CHAIN_KEY_MAP` supprim├® (2026-05-17)** : le mapping de 48 lignes dans `icons.tsx` n'├®tait ni export├® ni import├® (code mort). La r├®solution d'ic├┤nes passe par le manifest local + fallback CDN. Ne pas le recr├®er.

### Gotchas WCORE Web ÔÇö Cache alignment EVM/SVM/Cosmos (v0.2.10)

- **Cache layers align├®es sur les 3 VMs (2026-05-17)** : EVM, SVM et Cosmos partagent maintenant les m├¬mes couches de cache avec des TTL et comportements identiques. Tout nouveau cache ajout├® ├á une VM doit ├¬tre r├®pliqu├® sur les deux autres.
  - **Negative cache** : `empty:{chain}:{address}`, TTL 10 min. Wallet vide ÔåÆ retour imm├®diat `[CACHED_EMPTY]` sans appels RPC/REST.
  - **Native balance cache** : `native:{chain}:{address}`, TTL 1h. Fallback uniquement (pas de short-circuit). ├ëcrit si balance > 0, lu si le consensus RPC ├®choue.
  - **Token balance/accounts cache** : EVM = `bal:{chain}:{contract}:{address}`, SVM = `ta:{chain}:{address}`, Cosmos = `bal:{chain}:{address}`. TTL 1h, fallback uniquement.
  - **Per-token individuel** : `token:{chain}:{contract|mint|denom}:{address}`, TTL 1h. ├ëcrit apr├¿s pricing, fire-and-forget. Permet la lecture d'un solde token unique hors scan.
  - **Cosmos staking** : `del:`, `unb:`, `rew:` ÔÇö `{prefix}:{chain}:{address}`, TTL 1h. Chaque appel REST a son cache fallback ind├®pendant. `stakedAmount` s'accumule correctement m├¬me avec donn├®es cach├®es.
  - **EVM discovery** : `disc:{addr}:{chain}` (token list) + `disc:{addr}:{chain}:block` (curseur incr├®mental). TTL 24h. Fire-and-forget writes depuis v0.2.8.
- **Tous les caches sont fire-and-forget** : `.catch(() => {})` sur chaque write. Les tests d'int├®gration utilisent un d├®lai de 300ms apr├¿s scan avant de lire le cache. Ne jamais `await` un write de cache dans un engine.
- **Format de cl├® coh├®rent** : `{prefix}:{chainKey_lowercase}:{address|contract|denom}`. Toujours utiliser `key.toLowerCase()` pour la partie cha├«ne.
- **Redis cache integration test** (`apps/api/test/cache-integration.test.ts`) : 8 tests qui v├®rifient les ├®critures Redis r├®elles via `sharedCache.get()`. N├®cessite `.env.test` avec `TEST_DATABASE_URL` + `TEST_REDIS_URL` pointant vers Railway. `sharedCache` est export├® depuis `apps/api/src/server.ts` pour inspection. Pas de `app.close()` dans le `after` ÔÇö le test est con├ºu pour coexister avec d'autres suites qui importent `server.js`.
- **Tests unitaires SVM/Cosmos** : 9 nouveaux tests dans `svm.test.ts` (4) et `cosmos.test.ts` (5). Utilisent `MemoryCacheStore`. Couvrent : negative cache, native fallback, token accounts/bank balances fallback, per-token cache, delegations/unbonding/rewards fallback. 123/123 tests passent.
- **Tableau de couverture cache** :

| Cache | EVM | SVM | Cosmos |
|-------|-----|-----|--------|
| Negative (10 min) | Ô£à | Ô£à | Ô£à |
| Native balance (1h) | Ô£à | Ô£à | Ô£à |
| Token balance/accounts (1h) | Ô£à | Ô£à | Ô£à |
| Per-token individuel (1h) | Ô£à | Ô£à | Ô£à |
| Staking (1h) | N/A | N/A | Ô£à |
| Discovery incr├®mental | Ô£à | N/A | N/A |
| Prix | Ô£à | Ô£à | Ô£à |
### Gotchas WCORE Web ÔÇö Audit sprint v0.2.12 (2026-05-27)

- **RedisPricingCache ÔÇö cache prix partag├® entre workers SCAN_CONCURRENCY (v0.2.12)** : `MemoryPricingCache` (in-memory) ne partage pas les prix entre les workers parall├¿les de `SCAN_CONCURRENCY=50` ÔåÆ chaque worker refait les m├¬mes appels DefiLlama/GT/DexScreener ÔåÆ gaspillage API + ralentissement. **Fix** : `RedisPricingCache` (`packages/core/src/pricing/redis-pricing-cache.ts`) impl├®mente `PricingCache` avec Redis comme backend, partag├® entre tous les workers. Inject├® via `sharedPriceCache` dans `getEvmWalletAssets`, `getSvmWalletAssets`, `getCosmosWalletAssets`, et le dispatch. Cr├®├® dans `apps/api/src/plugins/scan.ts` ├á partir de `sharedCache` (ioredis). Fichiers modifi├®s : `dispatch.ts`, `evm.ts`, `svm.ts`, `cosmos.ts`, `pricing/index.ts`, `scan.ts`.
- **Ollama bind ÔÇö `OLLAMA_HOST=127.0.0.1:11434` (v0.2.12)** : Ollama ├®coutait par d├®faut sur `0.0.0.0` (toutes les interfaces) ÔåÆ faille de s├®curit├® (n'importe qui sur le r├®seau local peut appeler l'API). **Fix** : `OLLAMA_HOST=127.0.0.1:11434` dans les scripts de lancement. L'API Ollama n'est plus accessible depuis le r├®seau, seulement localhost.
- **E2E Playwright ÔÇö `waitForFunction(Ôé¼)` fragile (v0.2.12)** : `WalletContent` rend `formatValue(totalEur)` ÔåÆ `Ôé¼0.00` imm├®diatement (totalEur=0 avant scan), donc attendre `Ôé¼` dans `main` textContent matche instantan├®ment avant que les ChainCards soient rendues. **Fix** : attendre `main h2` (ChainCard rend `<h2>{chainName}</h2>`) plut├┤t que `Ôé¼`. Les `<h2>` n'apparaissent qu'apr├¿s r├®ception des r├®sultats de scan.
- **E2E Playwright ÔÇö `main` selector peut matcher 2 ├®l├®ments (v0.2.12)** : Next.js dev overlay rend un 2e `<main>` en cas d'erreur ÔåÆ `page.locator("main")` peut matcher le mauvais ├®l├®ment. **Fix** : scoper les assertions avec `.first()` ou utiliser des s├®lecteurs plus sp├®cifiques (`main h2`, `main table`).
- **E2E Playwright ÔÇö Solana filtered out for EVM address (v0.2.12)** : `WalletContent.matchChains()` filtre les cha├«nes par VM ÔÇö une adresse `0x...` (EVM) ne matchera jamais `Solana` (SVM). **Fix** : utiliser une adresse Solana (`9aT7...`) pour tester les cha├«nes SVM dans les E2E.
- **E2E Playwright ÔÇö TokenTable selector `{exact:true}` vs nested spans (v0.2.12)** : `TokenTable` rend `<span>ETH<span>NATIVE</span></span>` ÔåÆ textContent est `ETHNATIVE`, pas `ETH`. `page.getByText("ETH", { exact: true })` ├®choue. **Fix** : ├®viter `{exact:true}` sur les symboles de tokens, ou utiliser `getByRole("cell")` avec `hasText`.
- **E2E Playwright ÔÇö `page.route` mock pattern (v0.2.12)** : fonctionne parfaitement avec `**` glob dans Playwright. Les mocks doivent matcher `DEFAULT_API_URL` (http://127.0.0.1:4000) ou `NEXT_PUBLIC_API_URL`. `installMockWallet` via `addInitScript` injecte `window.ethereum` avant le montage React ÔÇö fiable pour wagmi.
- **E2E Playwright ÔÇö JWT_SECRET requis pour le webServer API (v0.2.12)** : `playwright.config.ts` lance l'API via `pnpm dev` ÔåÆ a besoin de `JWT_SECRET` dans l'environnement. **Fix** : passer `JWT_SECRET=<value>` au lancement des tests E2E.
- **E2E Playwright webServer ÔÇö timeout 120s minimum (v0.2.12)** : le d├®marrage API + Web + compilation Turbopack peut prendre >60s. `webServer.timeout` ├á 120000 (2 min) recommand├®. Laisser le `reuseExistingServer: true` pour les runs successifs.
- **pnpm install ÔÇö tailwindcss manquant ÔåÆ typecheck fail (v0.2.12)** : apr├¿s un `git clone` frais ou `pnpm install --frozen-lockfile`, `tailwindcss` peut ne pas ├¬tre install├® si le lockfile est stale ÔåÆ `pnpm typecheck` ├®choue avec `Cannot find module 'tailwindcss'`. **Fix** : `pnpm install` (sans `--frozen-lockfile`) pour r├®soudre les d├®pendances manquantes.

### Gotchas WCORE Web ÔÇö Blockscout metadata cache + GM random (v0.2.18, 2026-05-19)

- **Blockscout metadata cache (v0.2.18)** : `discoverTokensFromExplorer` ├®crit maintenant symbol/name/decimals dans le cache metadata (`meta:{chain}:{contract}`, TTL 24h). **Avant** : les tokens Blockscout n'├®taient pas cach├®s ÔåÆ au scan suivant, les RPCs `symbol()`/`name()`/`decimals()` ├®taient re-call├®s et pouvaient ├®chouer ÔåÆ token perdu. **Maintenant** : les metadata Blockscout survivent entre les scans. Fichiers : `explorer-discovery.ts` (cache write), `evm.ts` (passe cache).
- **Metadata cache null non cach├® (v0.2.18)** : `getErc20Metadata` ne cache plus les r├®sultats `token: null`. **Avant** : quand le consensus RPC ├®chouait, `token: null` ├®tait cach├® 24h ÔåÆ retries bloqu├®s. **Maintenant** : le scan suivant re-essaie. Fichier : `metadata.ts` (lignes 67-71, 80-84).
- **GM random scope (v0.2.18)** : `getRandomContract()` utilise `chainKey` quand fourni (ChainCard) pour filtrer par cha├«ne. Le Header ne passe pas `chainKey` ÔåÆ pick parmi TOUS les contrats du site. **Avant** : `getUserContract()` retournait toujours `contracts[0]` (le plus r├®cent = biais├® vers la cha├«ne connect├®e). Fichier : `useOnChainGm.ts`.
- **Header GM balance filter (v0.2.18)** : `/api/gm/random` v├®rifie `eth_getBalance` sur chaque cha├«ne GM en parall├¿le quand pas de filtre cha├«ne. Filtre les cha├«nes o├╣ balance native ÔëÑ $0.05. Fallback si aucune cha├«ne n'a assez. Fichier : `gm-contracts.ts`.
- **UNKNOWN tokens skip du cache (v0.2.18)** : le merge du discovery cache skip les tokens avec `symbol="UNKNOWN"` ou `name="Unknown Token"`. Fichier : `evm.ts` (lignes 272-273).
- **Humanity (H) token registry (v0.2.18)** : `0xcf5104d094e3864cfcbda43b82e1cefd26a016eb` ajout├® au `TOKEN_REGISTRY` Ethereum. RPCs ├®chouaient sur metadata ÔåÆ token UNKNOWN ÔåÆ balance masqu├®e. Fichier : `registry.ts`.

### Gotchas WCORE Web ÔÇö Token logos resolution (v0.2.19, 2026-05-19)

- **CoinGecko 403 Forbidden** : `coin-images.coingecko.com` bloque l'acc├¿s direct aux images avec HTTP 403. **Fix** : utiliser CoinMarketCap (`s2.coinmarketcap.com`) ou TrustWallet comme sources.
- **R├®solution par adresse de contrat (TrustWallet)** : `resolveTokenLogo` utilise TrustWallet en priorit├® 1 (`blockchains/{chain}/assets/{contract}/logo.png`). Couvre ~85-97% des tokens ERC-20 sans ambigu├»t├® de symbole. Fallback CMC par symbole pour les tokens majeurs non-TrustWallet, puis spothq. Fichier : `token-logos.ts`.
- **Automated token logo cache (v0.2.19)** : `token-logo-resolver.ts` r├®sout les logos via `logo:{chain}:{contract}` Redis, puis metadata cache, Blockscout `/api/v2/tokens/{contract}` `icon_url`, DexScreener `info.imageUrl`, TrustWallet, puis fallback. Les succ├¿s sont cach├®s long terme; les misses sont cach├®s sous `logo-miss:{chain}:{contract}` 24h. Ne jamais polluer le cache avec `coin-images.coingecko.com` (403). `assets.coingecko.com` venant de Blockscout est OK.
- **Cache logoUrl stale** : le cache metadata Redis stockait les anciennes URLs CoinGecko. **Fix** : `getErc20Metadata` rafra├«chit le `logoUrl` via `resolveTokenLogo()` lors de la lecture du cache. Fichier : `metadata.ts`.
- **Railway build cache + TS error = silent fail** : quand le build TS ├®choue, Railway garde l'ancien d├®ploiement. **Toujours** v├®rifier `pnpm --filter "@wcore/core" build` en local avant de d├®ployer.
- **Ne JAMAIS mettre `null` dans `Record<string, number>`** : casse le build TS. Supprimer les entr├®es sans CMC ÔåÆ le fallback TrustWallet/spothq g├¿re le reste.
- **Post X Blockscout (2026-05-19)** : `https://x.com/WCORExyz/status/2056711198687539418`. Tag `@blockscoutcom`. Image : `apps/web/public/wcore-blockscout-post.svg` + `.png`. DA : fond sombre, d├®grad├® vert/bleu, logos WCORE + Blockscout centr├®s avec badge `├ù` lumineux, pas de cha├«nes ni texte gris superflu.

### Gotchas WCORE Web ÔÇö DB Recovery + RealT Pricing (2026-05-18)

- **`.env.test` pointe vers prod ÔåÆ DB wipe (2026-05-18)** : `TEST_DATABASE_URL` contenait l'URL Railway prod (`viaduct.proxy.rlwy.net:51381`). `pnpm test` a ex├®cut├® `deleteMany()` sur tous les users prod. **Fix** : `.env.test` pointe vers `localhost`. `apps/api/set-test-env.js` refuse explicitement `localhost`, `127.0.0.1`, `::1` dans `TEST_DATABASE_URL` pour les runs prod.
- **GM history recovery via on-chain scan** : `scripts/recover-gm-history.ts` scanne 500k blocks back via `eth_getLogs` (chunk 10k). Cha├«nes avec RPC limit├®s (sei=2000 blocks, tac/openledger=block range limit) n├®cessitent les APIs explorers (seiscan.io, explorer.tac.build, scan.openledger.xyz) pour trouver les tx hashes.
- **RealT pricing wrong sur Gnosis** : `api.realtoken.community/v1/token/{contract}` retourne ~$50 mais les tokens affichent des prix DexScreener/GT (pools illiquides). `RealTPriceSource` (`packages/core/src/pricing/sources/realt.ts`) a maintenant des logs `[RealT] Fetching/Cache hit/HTTP/Error` + header `User-Agent: WCORE/1.0`. REG (governance token) retourne 401 ÔÇö normal, pas dans l'API. Property tokens retournent 200 avec prix correct. Suspect├® : API bloque IPs Railway.
- **­ƒÜ® bouton Ôëá flag scam** : le ­ƒÜ® dans TokenTable est le **bouton de report** (­ƒÜ® = signaler comme scam, Ô£à = signaler comme l├®gitime). Les RealToken sont correctement whitelist├®s dans `detectScam` (early return `s.startsWith("REALTOKEN-")`).
- **Backup DB Prisma-based** : `apps/api/backup-db.cjs` remplace `pg_dump` (non dispo sur Windows sans PostgreSQL client). Export JSON via Prisma `$queryRaw`. Rotation 7 jours. Scheduled task `WCORE_DB_Backup` mise ├á jour.

### Gotchas WCORE Web ÔÇö 8 GM factory chains en 1 semaine (2026-06-07)

- **Pattern unifie pour activation GM factory** : 8 cha├«nes activ├®es (Core, Flare, X Layer, Shibarium, Degen, Beam, Ronin, opBNB), toutes avec le meme workflow. La verification on-chain prealable est obligatoire : `eth_chainId` (doit matcher le config) + `eth_getCode(factory)` (doit retourner bytecode non-vide, typiquement 1696 bytes pour GmFactory) + count PUSH0 (61 = Shanghai, 0 = pre-Shanghai) + `implementation()` (typiquement 2237 bytes pour GmOnChain) + `eth_getBlockByNumber("latest", false)` pour `baseFeePerGas` (presence = London EIP-1559 actif).
- **Build Shanghai + London EIP-1559 = standard pour 7/8 chaines** : Core (1116), Flare (14), X Layer (196), Shibarium (109), Degen (666666666), Beam (4337), Ronin (2020) utilisent toutes le meme build source deploye sans modification. Seul KCC necessite le build Paris dedie (cf. gotcha 2026-06-05). opBNB est le cas special OP-Stack : `eth_chainId` 0xcc valide, mais `baseFeePerGas` retourne `0x0` sur le latest block (Bedrock EIP-1559 quirk quand idle). Le genesis block (block 1) a un baseFeePerGas 0x53724e0 (~87.5 gwei), ce qui confirme que l'EIP-1559 est active. **Test** : toujours verifier le genesis block, pas juste le latest, pour les chaines OP-Stack.
- **Impl size uniforme = template unique deploye 8 fois** : 2237 bytes sur les 8 chaines pour l'implementation GmOnChain. C'est une signature du template unique deploye sans modification. Toute deviation (2238, 2236, etc.) = probleme dans le pipeline de build ou de deploy.
- **Source de l'ecosysteme RPCs** : `packages/core/src/chains/<KEY>.ts` (config RPC, chainId, native, decimals) est la source unique. Ne JAMAIS creer de map RPC locale dans `apps/api`, `apps/web`, ou `scripts/`. La resolution passe par `getRpcEndpoints()` / `getPrimaryRpcEndpoint()` (`packages/core/src/rpc/endpoints.ts`). Les configs sont alimentees par chainlist.org (warmup boot avec `validateChains` + `warmDynamicRpcEndpoints`). Incident declencheur : deploy GM Ethereum `tx_not_found` car une map GM divergeait des RPC core (cf. gotcha 2026-05-23).
- **Harmonie 7 couches obligatoire pour activation GM** : (1) `GM_FACTORIES` (shared) + (2) `GM_CHAIN_NAMES` (web/gm-chains) + (3) `SOON_CHAIN_CANDIDATES` retrait + (4) `wagmi.ts` const + (5) `wagmi.ts` chains[] array + (6) `EXPLORERS` (lib/explorers) + (7) `DeployClient` (chain-data.ts). Les tests de garde `gm-chains.test.ts` (4/4) + `wagmi-gm-chains.test.ts` (1/1) verifient que tous les `GM_FACTORIES` ont un label et que tous les chainIds sont configures dans wagmi. Le 2026-06-07 a revele un trou : Core avait factories + wagmi + DeployClient mais pas l'explorer entry (`scan.coredao.org`). Corrige par commit `e621bc4`. **Le check d'harmonie 7 couches est manuel, pas automatise**. A ajouter dans `gm-chains.test.ts` pour prevenir les trous silencieux.
- **Verifier URL explorer via HEAD avant commit** : `scan.coredao.org` (Core) HEAD 200 OK. Pattern : `https://{base}/address/{addr}` doit retourner 200 pour une adresse valide (le builder `getExplorerUrl` construit `${base}/address/${contract}`). URLs 404 dans EXPLORERS = liens casses sur /profile, /gm, transactions history. Toujours tester avec un HEAD avant d'ajouter.
- **opBNB explorer special** : `opbnbscan.com` n'est pas un Etherscan fork. Le pattern `/address/<addr>` fonctionne, mais les transactions sont sur `opbnb.bscscan.com` (mirror BNB Chain). Pour les 4 RPCs configures, le primary `opbnb-mainnet-rpc.bnbchain.org` supporte `eth_call` + `eth_getLogs`. Les 3 fallbacks : `opbnb.drpc.org`, `opbnb-rpc.publicnode.com`, `1rpc.io/opbnb`.
- **Impl unique deploye 8 fois = meme bytecode, memes addresses sur chaque chain** : 1696 bytes pour GmFactory, 2237 bytes pour GmOnChain. Cela simplifie le pattern : on peut tester le bytecode sur 1 chain (par ex. Base) et deployer le meme bytecode sur 7 autres sans modification. Le `build.json` a une seule entree `GmOnChain` (Shanghai) qui sert pour les 8 chaines standards. Le deploy selector `pickBuild(build, chainKey, contract)` ne choisit Paris QUE pour `PARIS_BUILD_CHAINS = new Set(["KCC"])`. Toute nouvelle chaine pre-Shanghai = ajouter au Set + bump version build Paris.

### Gotchas WCORE Web ÔÇö X cycle replies + em-dash ban script├® (2026-06-07)

- **Em-dash ban dans posts X = check obligatoire, pas juste visuel** : incident 2026-06-07 : un reply X sur nftbestart contenait un em-dash `(wcore.xyz) ÔÇö no signing...` que tu as du supprimer manuellement. L'agent qui a ├®crit le draft l'a laisse passer malgre la consigne explicite d'AGENTS.md. **Fix** : `scripts/x-cycle/post-replies.cjs` a maintenant un `sanitize()` qui refuse d'envoyer si le texte contient `\u2014` (em-dash), `\u2013` (en-dash), `\u00A0` (non-breaking space), ou `\.\.\.` (ellipsis). Le helper est appele AVANT `postReply()`. Si un pattern interdit est detecte, le script retourne `false` immediatement et log `REFUSE: texte contient patterns interdits: em-dash`.
- **Pattern X reply valide** : 4 regles, (1) PAS d'em-dash, en-dash, NBSP, ellipsis ; (2) `@WCORExyz` majuscules en display, lowercase dans URLs ; (3) ton utile et conversationnel, pas pitch promo ; (4) 1 mention max sauf demande explicite. Toujours verifier le draft reel avant publication (lire le textarea via DOM) ET recharger le thread apres pour confirmer la reply.
- **Wobblhash = compte CM interne, pas d'engagement public requis** : `Wobblhash a repost├® votre post` = amplification interne d'ecosysteme, pas un engagement communautaire externe. Ne PAS liker/remercier publiquement (ca ferait astroturfing). Le compte wobblhash sert de relai CM, et le pattern "CM X imports de wobblhash" a ete importe pour @WCORExyz (cf. gotcha 2026-05-17). Source : `docs/CM-STRATEGY.md` (95 KB, le plus gros doc du repo).
- **Cycle X scan authentic-only** : 90 % des posts X crypto en 2026 sont des shill posts (Hashium, heyaura, QwertiAI, etc.). Ne pas repondre a des shill posts concurrents = astroturfing reputational damage. Les vraies cibles sont les discussions authentiques (sans mention de projet, posant une question reelle sur la douleur utilisateur). Exemples 2026-06-07 : nftbestart (AI + wallet), alphacyl (chain recap MANTRA), christianecun (DeFi second job), liberty_ki76455 (UX critique tool chaining).
- **Deploiement image X post = pas obligatoire avant publication** : les images comme `wcore-post-gm-8-new-chains.png` ou `wcore-post-gm-8-more-chains.png` peuvent etre attachees depuis le filesystem local via Playwright CDP (`page.locator('input[type=file]').setInputFiles(path)`) ou X composer drag-and-drop. Pas besoin de deployer web pour publier le post. La web deploy ne sert que pour rendre l'image accessible via URL publique.
- **Connect flow X cleanup post-publication** : apres publication, toujours verifier (1) le thread cible contient la reply, (2) la page `with_replies` du profil WCORE contient la reply, (3) la notification "your post" apparait. Si un de ces checks manque, la reply n'a pas ete prise en compte par X et il faut retenter.

### Gotchas WCORE Web ÔÇö Sprint audit P0/P1 + 8 GM chains (2026-06-11)

- **Mock fetch sans `ok: true` = retry 2 min sur `/api/gm/onchain`** : la route v├®rifie `resp.ok` avant de lire le receipt (`gm-onchain.ts:77`). Un mock test `{ json: async () => data } as Response` sans `ok: true` est trait├® comme ├®chec RPC ÔåÆ 10 retries avec backoff (~2 min) ÔåÆ timeout de la suite. Toujours mocker `{ ok: true, json: async () => data }`. Sympt├┤me : la suite GM bloque silencieusement apr├¿s les tests deploy.
- **`isAdminAuthorized` ignorait `x-admin-token` si Bearer pr├®sent (corrig├® 2026-06-11)** : l'ancien code retournait `safeEq(bearer, adminToken)` d├¿s qu'un header `Authorization: Bearer` existait ÔÇö un JWT utilisateur + `x-admin-token` valide donnait `false`. Fix : `bearerMatches || headerMatches` (`admin-auth.ts`). Les routes qui exigent `req.user` ET admin (ex: `/api/tickets` admin view) fonctionnent maintenant avec JWT + `x-admin-token`.
- **JWT de test sans `type: "access"` = 401** : l'auth hook exige `payload.type === "access"` (`isUsableAccessPayload`). Tous les helpers de test qui signent des JWT doivent inclure `type: "access"` dans le payload, sinon 401 silencieux sur toutes les routes authentifi├®es.
- **`TxHash` zod = hex strict** : `schemas.ts` exige `/^0x[0-9a-fA-F]+$/`. Les tx hashes de test fantaisistes (`0xdeploy01`, `0xonchain01`) sont rejet├®s en 400 `missing_tx_hash`. Utiliser des hex valides (`0xde0101`, `0x0c01`).
- **DB test locale ├á migrer apr├¿s nouvelle migration** : les suites `share`/`wallet-plugins` ├®chouent avec `The column accessCount does not exist` si la DB locale (127.0.0.1) n'a pas re├ºu `prisma migrate deploy`. Commande : `$env:DATABASE_URL = <TEST_DATABASE_URL>; pnpm --filter @wcore/db exec prisma migrate deploy`.
- **`scam-filtering.test.ts` ne doit jamais importer `server.js`** : importer `server.js` juste pour `buildChainScan` d├®marre Fastify + handles UV ÔåÆ crash `Assertion failed: !(handle->flags & UV_HANDLE_CLOSING)` en suite compl├¿te. Importer `server-helpers.js` (module pur).
- **Tests date-sensitive interdits** : `gm-points.test.ts` utilisait des dates fixes (mai 2026) pour tester le streak "courant" ÔåÆ cass├® d├¿s que la date r├®elle d├®passe. Utiliser des offsets relatifs ├á `Date.UTC(now)` (`utcDay(-1)`, `utcDay(0)`).
- **`api.wcore.xyz` n'existe pas en DNS** : NXDOMAIN sur tous les resolvers. La prod web utilise `NEXT_PUBLIC_API_URL=https://api-production-b5bf.up.railway.app` (variable Railway). Ne pas r├®f├®rencer `api.wcore.xyz` dans la doc tant que le record DNS n'est pas cr├®├®.
- **8 GM chains activ├®es (2026-06-11)** : Gravity (1625), Merlin (4200), Manta Pacific (169), Taiko Alethia (167000), Plasma (9745), HashKey (177), Hemi (43111), HyperEVM (999). Pattern unifi├® confirm├® : factory 1696 bytes / 61 PUSH0, impl 2237 bytes / 58 PUSH0 sur les 8. **Merlin est pre-London** (pas de `baseFeePerGas` ÔåÆ gas legacy) mais Shanghai-capable ÔÇö pre-London Ôëá pre-Shanghai, le build standard marche. Les explorers v├®rifi├®s HEAD 200 : explorer.gravity.xyz, scan.merlinchain.io, pacific-explorer.manta.network, taikoscan.io, plasmascan.to, explorer.hsk.xyz, explorer.hemi.xyz, hyperevmscan.io.
- **SOON_CHAIN_CANDIDATES n'a pas besoin de retrait manuel** : `getSoonChains()` filtre par `!getFactoryAddress(chain.key)` ÔÇö d├¿s que la factory est dans `GM_FACTORIES`, la cha├«ne dispara├«t automatiquement de "Coming Soon". Le test de garde `gm-chains.test.ts` le v├®rifie.
- **`chain-data.ts` (DeployClient) est g├®n├®r├® en avance** : `scripts/extract-deploy-chain-data.mjs` extrait depuis les configs core ÔÇö les 8 cha├«nes y ├®taient d├®j├á avant l'activation. V├®rifier ce layer en premier : s'il est d├®j├á bon, seuls factories/gm-chains/wagmi/explorers restent ├á ├®diter.
- **GM deploy ne doit jamais perdre un contrat on-chain** : si `/api/gm/contracts/deploy` ou le receipt RPC lag apr├¿s la tx, un contrat peut etre deploye on-chain mais absent de DB. Pattern corrige 2026-06-11 : retry receipt cote serveur, retry background cote frontend, puis `syncOnChainContracts()` partage/cible par `/api/gm/status` et `my-contracts`. Avant de redeployer, chercher d'abord l'event `ContractDeployed` sur la factory. Gravity etait le seul cas sans event detecte dans le lot 2026-06-11.
- **Post X 72 GM chains (2026-06-11)** : `https://x.com/WCORExyz/status/2065148361401917832`. Image `apps/web/public/wcore-post-gm-8-more-chains.svg/.png`, script `scripts/build-post-gm-8-more-chains.cjs`. Replies publiees et verifiees : `@Saimo0` (friction multi-outils), `@0xToxo` (historical/current value), `@MacroBombastic` (too many chains / UX). Les shill posts concurrents et posts GM automatises ont ete ignores.
- **Scripts de visuels X doivent rester portables** : ne pas hardcoder `C:/Users/strau/wcore-web` ni un chemin pnpm absolu. Pattern valide : `const ROOT = resolve(__dirname, "..")` et fallback `require(resolve(__dirname, "../node_modules/.pnpm/playwright@.../node_modules/playwright"))` si `require("playwright")` echoue.

### Gotchas WCORE Web ÔÇö 8 GM chains L2s + connector-not-connected fix (2026-06-13)

- **8 GM chains activees (2026-06-13)** : Immutable zkEVM (13371, IMX), Morph (2818, ETH), Mezo (31612, BTC), Reya (1729, REYA), Swell Chain (1923, ETH), Swan (254, ETH), Vana (1480, VANA), Story (1514, IP). **80 GM chains live now** (72 -> 80). Pattern unifie confirme : factory 1696 bytes / 61 PUSH0, impl 2237 bytes / 58 PUSH0, London (baseFeePerGas present) sur les 8. Aucune pre-Shanghai. Vana = single-RPC (`rpc.vana.org`) a surveiller. Explorers verifies HEAD 200 : explorer.immutable.com, explorer.morph.network (redirect depuis morphl2.io), explorer.mezo.org, explorer.reya.network, explorer.swellnetwork.io, mainnet-explorer.swanchain.io, vanascan.io, www.storyscan.io.
- **Les 7 chaines existent dans `wagmi/chains`** : `immutableZkEvm`, `morph`, `mezo`, `swellchain`, `swan`, `vana`, `story` sont toutes importables de `wagmi/chains` (Reya etait deja ajoute manuellement). Toujours verifier `require('wagmi/chains')[name]` avant de definir une `Chain` custom dans `wagmi.ts`.
- **Reya native symbol = REYA, pas ETH** : `chain-native-symbols.json` avait `reya: "ETH"` (faux) alors que la config core dit `NATIVE_SYMBOL: "REYA"`. Corrige. Le tip GM affichait le mauvais symbole. Toujours croiser `chain-native-symbols.json` avec `packages/core/src/chains/<KEY>.ts`.
- **Layers 6+7 souvent deja faites** : `chain-data.ts` (DeployClient) et `chain-icon-manifest.json` + `public/chains/<KEY>.webp` etaient deja presents pour les 8 (script d'extraction proactif). Verifier ces 2 layers en premier : s'ils sont bons, seuls factories/gm-chains/explorers/wagmi restent.
- **`/api/chains` sert depuis `chainList` (toutes chaines, flag `disabled`)** : si une nouvelle chaine n'apparait pas en prod apres deploy, c'est le build/deploy API qui n'est pas fini (pas un filtre). `Cache-Control: max-age=300` peut aussi servir une reponse cachee 5 min. Verifier avec `Cache-Control: no-cache` apres ~3-4 min.
- **Verif on-chain faux negatif via regex PowerShell `\b`** : un check `node -e "...new RegExp('\\\\b'+id+'\\\\b')..."` inline dans PowerShell casse l'escaping et donne des faux "NOT in index.ts". Toujours ecrire le script de verif dans un fichier `.cjs` et le lancer, jamais inline avec `node -e` sous PowerShell 5.1.
- **Post X 80 GM chains (2026-06-13)** : `https://x.com/WCORExyz/status/2065726824659771652`. Image `apps/web/public/wcore-post-gm-8-l2s.svg/.png`, script `scripts/build-post-gm-8-l2s.cjs`. DA identique aux posts GM precedents (grille 4x2, badge WCORE, pill `80 GM CHAINS LIVE`, pills `8 NEW TODAY`/`One template`/`BTC + IP in`). Replies cycle X publiees+verifiees : `@John_Peace1` (information problem / portfolio fragmente), `@TopuWeb3` (read-only visibility first). Toutes les autres cibles du scan contenaient un shill cache (tryquantio, Tria, KAST, odos, ARC/ANIMA, heyaura, GEME) -> ignorees.
- **Reply X scam airdrop (2026-06-13)** : `https://x.com/xZBLUEx/status/2064938376302850317` (thread ZBlue Xaman wallet drained). Texte publi├® : `Sorry you went through that. Most of those scam tokens start as tiny unsolicited airdrops, and a lot of trackers still count them in your balance. We flag those out so they never inflate what you think you actually hold.` 0 mention @WCORExyz, sanitis├® (pas d'em-dash/en-dash/NBSP/ellipsis), v├®rifi├® 1 WCORE reply dans 14 articles.
- **Post-replies automation regex trop large (2026-06-13)** : le check `document.body.innerText` contre `/automated|are you a robot|suspicious activity/i` matche du texte de tweet normal (ex: `@xZBLUEx` reply "triggered their automated compliance"), pas une vraie alerte X. Faux positif = script refuse de poster alors que la page est clean (pas de `data-testid="mask"`, reply button accessible, thread visible). **Fix** : checker uniquement (1) `[data-testid="mask"]`, (2) `role="alert"` / `[data-testid="error"]` banners avec mots-cles specifiques `verify|captcha|robot|unusual activity|automated behavior|suspicious login`, (3) `title` tag avec `suspended|locked|verify|robot`. **PAS** scanner `document.body.innerText` car les tweets contiennent souvent "automated"/"suspicious". Fichier : `scripts/x-cycle/post-replies.cjs`.
- **X composer "2 textareas" -> texte et image dans des composers differents** : `x.com/compose/post` peut avoir DEUX `[data-testid="tweetTextarea_0"]` dans le DOM (modal foreground + inline). Si le script tape dans `[last]` et attache l'image au premier (ou inverse), le user voit l'image sans le texte. Fix : toujours cibler le MEME index pour le texte ET l'image. Pattern fiable : `goto('/home')` -> Escape (discard si confirm) -> `goto('/compose/post')` neuf -> ecrire dans `textareas[0]` -> `fileInput.setInputFiles()` -> verifier `textareas[0].innerText.length > 0 && hasImage` ensemble. Script : `scripts/x-cycle/prepare-post-clean.cjs`.
- **connector-not-connected sur GM on-chain (fix 2026-06-13, commit `baf7b4c`)** : le wallet picker connecte via EIP-6963 raw (jamais wagmi `connectAsync`), donc `isConnected=false` et `useSendTransaction`/`useSwitchChain` throw "connector not connected" (`@wagmi/core`). Le login marche car en raw `personal_sign`. Fix : `lib/onchain-tx.ts` (`switchChainAny`/`sendTransactionAny`) route via wagmi si connector connecte, sinon fallback raw `window.ethereum` (`eth_sendTransaction`/`wallet_switchEthereumChain`). Applique a `useOnChainGm` (sendGm + deployContract) et `useGmContracts` (withdraw). Tests purs : `apps/web/__tests__/onchain-tx.test.ts` (7).

### Gotchas WCORE Web ÔÇö Audit 2026-06-07 transversal (2026-06-07)

- **5 agents en parallele pour audit complet** : structure+historique, code+tests, securite, performance+cost, doc+archi+DX. Chacun produit un rapport detaille. Synthese finale consolidee dans `docs/AUDIT.md`. Score global 8.6/10. 3 P0 + 7 P1 + 16 P2 + 11 P3 identifies.
- **Top 3 P0 a traiter ce sprint** : (1) `AUTH_ALLOW_BEARER=false` en prod (1 ligne dans `.env.production.local`), (2) `/api/gm/status-onchain` amplification RPC (auth stricte + cache), (3) `scan.ts` 612 LOC sans tests de route (creer `apps/api/test/scan-plugin-routes.test.ts`).
- **`@fastify/rate-limit` dependance morte** : declare dans `apps/api/package.json:22` mais non enregistre dans `server.ts`. La rate-limit logic est implementee manuellement (`server.ts:129-179`). Retirer la dep = 40 KB.
- **EVM empty cache historique corrige (2026-06-19)** : l'ancien TTL EVM 1h pouvait masquer un wallet token-only nouvellement funde. Le TTL empty EVM est maintenant 10 min avec liveness check (`canServeEmptyCache`). Pour bypass immediat, utiliser `forceRefresh=true`.
- **RealT cache permanent sans safety TTL** : `realt:price:{contract}` dans Redis ecrit sans TTL, uniquement sur 200. Si un prix devient stale ou pollue, il reste indefiniment. Cap a 7 jours comme defense-in-depth.
- **AGENTS.md = 2 docs en 1** : 979 LOC, premiere moitie documente l'ancien Apps Script, deuxieme moitie (l.468+) documente la stack web moderne. Pas d'index/TOC. Split en `docs/apps-script.md` (archive) + `docs/wcore-web-guide.md` (vivant) prevu P2 backlog.
- **Drift versions `.env.example` vs `DEPLOY.md` vs `AGENTS.md`** : `SCAN_CONCURRENCY` defaut 30 (DEPLOY.md l.394) vs 50 (.env.example l.44, scan.ts:13). Single source of truth = le code (`scan.ts:13`). Mettre a jour `.env.example` (deja fait) et `DEPLOY.md` (a faire).
- **Contracts hardcodes Windows** : `contracts/compile-v0.8.19.js:7, 8, 17, 24-26` et `patch-build-json.js:6, 7, 12, 20` utilisent `C:/Users/strau/wcore-web/...`. Non portable, ne marche pas en CI. Remplacer par `path.join(__dirname, ...)` ou `process.cwd()`.
- **`pnpm lint` exit 1, 34 errors / 8 warnings** : la CI ne fail pas dessus (lint optional), mais signale de la dette. Top fichiers a corriger : `apps/web/components/ConnectButton.tsx` (8 errors dont TDZ), `contracts/compile-v0.8.19.js` (9 errors JS pas linte), `apps/api/src/server.ts` (2 unused imports).
- **Pas de `CONTRIBUTING.md` / `TESTING.md` / `TROUBLESHOOTING.md`** : a creer. 5min onboarding, 5min testing, 10min troubleshooting depuis les gotchas. P2 backlog.
- **Pas de `.nvmrc`** : Node 20 CI / 22 Docker / >=20.10 engines ÔÇö 3-voies mismatch. Ajouter `.nvmrc` avec `20`. P2 backlog.
- **API Docker image 500 MB unpruned** : `apps/api/Dockerfile:49` copie tout `node_modules`. Utiliser `pnpm deploy --filter @wcore/api --prod /app/deployed` ÔåÆ ~200 MB. P2 backlog.


## clasp MCP + Blockchain RPC MCP

Deux MCPs custom wrappers locaux dans `.mcp/`.

### clasp-mcp (`.mcp/clasp-mcp.js`)
Push/pull/deploy .gs files.
- Tools : `clasp_status`, `clasp_push`, `clasp_pull`, `clasp_deploy`, `clasp_logs`, `clasp_run`, `clasp_version`
- **Toujours `clasp_status` avant `clasp_push`**
- **Ne jamais push si `src/` a des changements non committ├®s** ÔÇö `git status` d'abord
- Rappel : `.clasp.json` `rootDir` doit ├¬tre `"src"` pour push, puis remettre `"."` apr├¿s

### rpc-mcp (`.mcp/rpc-mcp.js`)
Debug RPC responses multi-chain.
- **Auto-discovery au boot** : parse `src/*.gs` ÔåÆ toutes les cha├«nes actives
- Tools : `rpc_call`, `rpc_get_balance`, `rpc_get_token_balance`, `rpc_call_price`, `rpc_validate_endpoint`, `rpc_consensus_check`, `rpc_chain_list`
- **`rpc_consensus_check`** reproduit la r├¿gle WCORE `votes*2 > total`

## DefiLlama MCP ÔÇö Cascade Pricing Validation

`defillama-mcp` (`.mcp/defillama-mcp.js`) ÔÇö wrapper local.

### Tools
- `llama_get_price(coins[])` ÔÇö prix courant, format `chain:address` ou `coingecko:id`
- `llama_get_price_historical(coins[], timestamp)` ÔÇö prix historique
- `llama_search_token(query)` ÔÇö r├®soudre symbole/nom ÔåÆ adresse
- `llama_validate_cascade(token, chain, gtNetwork?)` ÔÇö simule DefiLlama ÔåÆ DexScreener ÔåÆ GT
- `llama_batch_prices(tokens[])` ÔÇö batch en 1 appel HTTP
- `llama_quota_status()` ÔÇö compteur du jour par host

### Workflow debug pricing
1. `llama_validate_cascade(token, chain)` ÔåÆ identifier source ├®chou├®e
2. `llama_get_price(coins[])` ÔåÆ v├®rifier valeur brute
3. Si DefiLlama OK mais WCORE faux ÔåÆ bug parsing dans `07_PRICES.gs`
4. Si DefiLlama KO ÔåÆ fallback attendu (DexScreener ÔåÆ GT)

## CoinGecko MCP ÔÇö Cascade Fallback Validation

`coingecko-mcp` (`.mcp/coingecko-mcp.js`) ÔÇö wrapper local.

### Position dans la cascade WCORE
Stablecoins ÔåÆ Cache ÔåÆ DefiLlama ÔåÆ DexScreener ÔåÆ GT Try1/2/3 ÔåÆ Jupiter (SVM) ÔåÆ [CoinGecko, dernier recours]

### Tools
- `gecko_ping()` ÔÇö health check
- `gecko_get_price(ids[], vs_currencies?)` ÔÇö par CG id
- `gecko_search_coin(query)` ÔÇö r├®soudre symbole ÔåÆ CG id
- `gecko_get_contract_price(chain, contract_address)` ÔÇö lookup par contrat
- `gecko_validate_cascade_fallback(chain, address)` ÔÇö simule GT ÔåÆ Jupiter ÔåÆ CG
- `gecko_batch_prices(tokens[])` ÔÇö batch group├® par cha├«ne
- `gecko_quota_status()` ÔÇö compteurs + buckets per-minute

### R├¿gles
- **Quota free tier** : 30 calls/min sans cl├®
- **Pro tier** : set `COINGECKO_API_KEY` ÔåÆ auto-rout├® vers pro-api
- **Batch** : toujours `gecko_batch_prices` pour multi-tokens
- **Jamais deviner un CG id** : `gecko_search_coin(symbol)` avant `gecko_get_price(ids)`
